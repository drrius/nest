import { Note } from "../components/page";
import type { ChoreMenuProps } from "./menu-types";
export function ChoreMenu(_props: ChoreMenuProps) {
  return <Note>Skip and reschedule in the iPhone app.</Note>;
}
