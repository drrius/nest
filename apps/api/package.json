{
  "name": "@nest/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test ../../tests/api/*.test.mjs",
    "dev": "node --env-file-if-exists=.env dev.mjs",
    "build": "node build.mjs"
  },
  "dependencies": {
    "@nest/ai": "workspace:*",
    "@nest/contracts": "workspace:*",
    "@nest/domain": "workspace:*",
    "effect": "4.0.0-rc.115"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  }
}
