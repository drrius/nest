# Saved meals and recipe snapshot audit

Pinned reference: Household OS commit `4a528c96caf41515a70291ccecbba9d7b35e3349`. Read-only source inspection; no production data was accessed.

## Audited storage and behavior

- `20260811180000_meals_groceries.sql:15–42` defines household-owned meal definitions and ordered grocery templates. Definitions contain name, optional recipe URL and notes, archive time and timestamps. Ingredients retain separate name, quantity text, unit text, optional category, note and order. Composite household foreign keys prevent cross-household definition/category references. Quantity/unit text must not be silently parsed or combined.
- The same migration's definition/template RLS and grants at lines 1785–1814 and 1874–1881 permit both current household members to read, insert and update the agreed columns. The old template delete grant is superseded by `20260904231923_meal_library_lifecycle_and_preparation.sql:2–5`, which adds archive state and removes authenticated deletion. Fixture extraction must use the final policy/grant state, not the original deletion permission.
- `20260905075117_meal_template_edit_versions.sql` adds template timestamps and guarantees a strictly advancing microsecond edit version. A definition timestamp alone cannot identify the whole recipe: ingredient edits do not update it. Native recipe selection and editing must include ingredients in their revision identity.
- `save_planned_meal_to_library` in the lifecycle migration validates HTTP(S) links and creates a definition from name/link/notes. It marks slotted entries as grocery-materialized without adding ingredients. Do not reuse that side effect: Nest requires separate explicit ingredient review and durable approval.
- Legacy placement snapshots title and recipe URL, but there are no structured servings, cooking instructions or ingredient snapshot columns. Notes must remain notes. Do not label old notes as verified cooking instructions, invent historical servings, or claim that today's library ingredients were the ingredients in an old planned meal.
- Legacy grocery materialization reads currently active library templates, creates groceries immediately, and sets an entry marker. That implementation is not suitable for Nest's explicit ingredient review or immutable planned recipes.

## Native implementation decisions

Keep existing definition and ingredient identities and history. Add recipe metadata without guessing values for existing rows: unknown legacy servings/instructions remain explicitly unknown. New native saved recipes need servings, ordered ingredients and short cooking instructions, with an optional safe HTTP(S) source link. Do not import web pages automatically or introduce nutrition/macro tracking.

Use a monotonic household library revision advanced by definition and ingredient mutations, including legacy writes, so a selection cannot mix a newer ingredient list with an older definition. Bounded paginated summaries and an authorized recipe detail read must expose exact revision information. Pagination must detect library changes rather than silently drop or repeat changed entries. Recipe edit/archive commands need explicit baselines and actor-private immutable receipts; authorization is checked before replay.

When a saved recipe is placed or used as a replacement, atomically snapshot its complete authorized contents at the requested library revision. Later recipe edits/archive must not rewrite planned meal snapshots. Existing meal/week baselines still protect occupied slots and concurrent household edits. Saving a recipe or placing it never creates groceries. Separate ingredient review operates on the visible planned snapshot and exact approval identity, preserving quantity/unit distinctions and allowing pantry exclusions.

Native detail screens and the private AI read/tool surface must use the same validated authorized recipe contracts. An old planned meal without an ingredient snapshot must say that historical recipe details are unavailable; a separately labeled link may open the current library recipe. Offline recipe detail is limited to the explicitly retained planned snapshots, without creating a new offline editing queue.

## Required verification

Prove recipe/ingredient RLS and composite household constraints using the audited final table shapes; reject foreign categories/definitions and revoked receipt replay. Exercise revision changes from both native and legacy definition/template edits, stable pagination, selection racing archive or ingredient edits, and snapshot immutability after later library changes. Verify full rollback of recipe writes, planned entry snapshots, revision counters and receipts. Test approved ingredients separately for exact quantities/units, pantry exclusions and duplicate/lost-response approvals. Recipe detail, selection, native editing, AI actions and phone accessibility remain unimplemented until their actual flows are verified.

## Read storage boundary

The first additive migration stores optional recipe servings as a positive PostgreSQL int32 count and short instructions as at most 4,000 Unicode code points. Existing rows retain null metadata; personal portion multipliers are a separate preference. No recipe data is backfilled or inferred. Source links are returned as stored data, including unsafe legacy schemes; the later native opener must allow only HTTP(S).

Read pages contain at most 50 active definitions ordered by UUID, with an exact household revision required on continuation. Recipe detail requires that revision and returns at most 200 active ingredients ordered by stored order then UUID; larger historical recipes fail explicitly without partial content. These transport bounds do not delete or rewrite historical rows. All read functions are stable security invokers, so the revision, definition and ingredient queries share the caller's MVCC snapshot. Revision triggers are private, trigger-only definers that cannot be called by clients; updates, archives and administrative deletes all advance the counter atomically. Native save/select commands and immutable planned recipe snapshots are still separate pending work.

The fixture uses the existing audited definition/template table extraction from `legacy-meals/tables.sql`. `legacy-meals/library-policy.sql` adds the final read/insert/column-update policies, archive column and final template version trigger from the migrations cited above. No obsolete delete grant, publication change or legacy UI is imported.

## Native creation command

Creating an explicitly requested saved recipe is an ordinary authorized household action, separate from generated-week and ingredient approvals. The shared draft contains a title, positive base servings, short instructions, optional notes/source, and 1–200 ordered ingredients. Quantity and unit text remain distinct and duplicate ingredient names are not combined. New strings use the native UTF-16 limits; existing stored reads retain the legacy Unicode-code-point limits. Source syntax requires an explicit HTTP(S) authority without credentials, whitespace, backslashes or control characters. Links are never fetched or followed by the command; the native opener separately validates parsability.

Creation locks current membership, the actor/household/operation identity and the exact whole-library baseline before inserting anything. It checks referenced active categories under shared locks, creates server-owned definition/ingredient identities, and stores an actor-private immutable receipt. One definition plus each ingredient advances the library revision; input validation reserves all increments within int64. Recipe data, counters and the receipt share a transaction. Receipt recovery checks current membership before replay and survives later partner edits/archive without recreating the recipe. No plan, grocery or financial command is called.

## Private assistant creation

The assistant dispatches the same authorized recipe transaction through the existing owner-private durable command journal. Strict shared draft codecs exclude actor/household/operation/generated recipe identities. Canonical transcript reconstruction replaces streamed/model creation claims with journal facts; lost acknowledgments recover the original receipt without undoing partner edits or archive.

The existing 64 KiB input and 128 KiB per-turn journal budgets remain. The API returns `native_required` before dispatch for serialized UTF-8 inputs over 48 KiB; the native result opens the recipe form and explicitly says nothing was saved. This is a transient non-write handoff, not a durable recipe receipt; canonical history still retains only journaled write facts. Inputs within the API boundary reserve ample room for JSONB whitespace (at most 200 fixed-shape ingredients), tested at the exact multibyte boundary. Direct database callers still face the exact 64 KiB journal-input validation. Journal capacity failure rolls back recipe rows, ingredients, revision increments and native receipt. No live model or physical-device verification is claimed.

## Native archive command

Archive changes only the definition's archive timestamp and household library revision. Existing ingredients, current/planned meal rows, historical title/link snapshots and groceries are preserved. It does not expose a restore operation. Later legacy/administrative restoration is deliberately not undone by replaying an older archive receipt.

The command requires current membership before immutable receipt replay and otherwise an active same-household definition at the exact library revision. The revision is locked before a nonblocking definition lock so a legacy writer holding the definition cannot form a lock cycle with its revision trigger. Every new archive advances the revision once; already archived/missing/foreign targets and changed baselines conflict without a receipt. Input reserves the int64 increment. API UUIDs are canonicalized before dispatch and the result is bound to actor, household, operation, definition and exact revision. Row-lock/revision contention, rollback, RLS, legacy restoration replay and a real lost-acknowledgment HTTP journey are verified only in disposable fixtures.

## Private assistant archive

The assistant archives through the shared strict definition/revision contract and the existing private command journal. Dispatch canonicalizes the definition UUID and calls the same membership-checked archive transaction. Archive state, native receipt and journal result commit atomically; a final journal failure rolls everything back. Exact replay returns the original receipt even after later restoration. Stale baselines produce durable conflicts, and the SDK turn stops further writes after uncertain or rejected commands.

Transcript reconstruction strips streamed/model archive claims and substitutes the requesting member's journal facts. Native chat says “Recipe archive confirmed” and opens the current library; it does not imply that a historical receipt proves the recipe remains archived. Model guidance requires an explicit unambiguous archive request, fresh library/detail reads and an explanation that ingredients, planned meals/history and groceries remain. Tests exercise concurrent replay, restored state, rollback, foreign/revoked access, forged transcript claims and real SDK/PostgREST lost-response recovery. No live model or physical iPhone verification is claimed.

## Native recipe editing

Editing uses explicit metadata patches rather than forcing a legacy recipe through the new-recipe creation form. Omitted values remain unchanged, including unknown servings/instructions, legacy source links and existing longer Unicode-code-point text. Explicit new text follows the native UTF-16 limits. Null can clear optional fields; it never supplies invented recipe metadata.

A null ingredient selection preserves all ingredients. An explicit ordered list retains selected active same-recipe ingredient IDs with optional field patches, archives omitted active ingredients, and inserts new ingredients with server-owned IDs. An empty list explicitly clears the active ingredient list while preserving archived rows. Quantities and units stay separate; duplicate names are not combined. Unchanged categories may remain archived legacy references; a supplied category must be active and in the current household. Referencing an archived, missing, other-recipe or foreign ingredient is a conflict.

The edit transaction checks membership before replay, locks the exact household library revision and active definition, and locks selected ingredient/category rows against legacy writers. Partial metadata, ingredient changes, revision counters and receipts roll back together. Unchanged rows are not rewritten; a no-op receipt can retain its baseline revision. A full replacement can touch at most 401 rows (one definition, 200 archived ingredients and 200 inserted ingredients). Revision overflow aborts the transaction, and receipts bind the prior revision and bounded exact bigint result. No planned meal, grocery, approval or financial command is called.

The authenticated API canonicalizes operation, definition, retained ingredient and supplied category UUIDs before dispatch, validates actor/household/operation/definition/prior-revision receipt identity and enforces a 2 MiB request body limit. Actual HTTP tests verify a 200-ingredient edit, canonical retry, loss after commit followed by partner archive, rejected oversized bodies and revoked recovery. Native editing controls and the private AI action are separate pending work.

## Native editing draft and recovery

The editor compares raw field values with the exact loaded recipe before normalizing changed text. Untouched unknown fields, longer legacy text, unsafe legacy links and archived category references are omitted from patches. Existing ingredient edits validate only their changed fields; new ingredients validate the complete input. A metadata-only edit sends a null ingredient selection, while reorder/add/remove explicitly sends the chosen list with retained identities and minimal patches. Draft comparison and selection building have direct regression coverage.

The native runtime copies nested attempted patches before dispatch, blocks further edits/reloads while a result is uncertain, and exposes an exact retry. Conflicts keep the old draft visible read-only; explicit reload/discard replaces it only after a successful current read. Account loss hides the recipe and receipt. An acknowledged edit is never resent merely because refresh failed. Navigation warns before losing an unsaved draft or uncertain attempt. Actual HTTP/PostgREST journeys cover lost responses followed by partner archive and fresh editing after a partner conflict; physical keyboard, navigation and accessibility behavior still need an iPhone.

## Private AI editing

The edit tool reuses the native patch and complete-selection contract. The private journal validates strict input, normalizes only supplied UUID identities, invokes the same edit transaction and commits its receipt atomically with the tool result. Omitted metadata and ingredient patch fields are preserved. A no-op can retain the prior revision. Retry replays the original receipt without undoing later edits or archive; current membership is checked before replay.

The API binds actor, household, canonical definition and prior revision. Inputs over 49,152 serialized UTF-8 bytes return a native-library edit handoff before dispatch; the existing 64 KiB JSONB input and 128 KiB turn limits remain. Canonical private history removes invented edit tool claims. Tests exercise fixed budgets, rollback, selection/UUID preservation, tenant isolation, lost responses and blocked subsequent SDK writes. These fixture-backed journeys do not establish live-provider or device execution.

## Selected recipe snapshots and storage commands

The new immutable per-entry snapshot captures the complete authorized saved recipe at a specified household library revision. Its foreign key retains the planned entry, and clients cannot insert/update/delete snapshots. A trigger prevents rewriting captured entry recipe identity, title, URL, notes or leftover relationship; move/remove commands can still change scheduling/removal state. Placement and replacement use separate shared contracts, exact week and library baselines, current membership before replay and actor-bound immutable operation receipts. Replacement invokes the already audited removal/preparation closure inside the same transaction, without inheriting preparation or creating groceries.

Library counter locking plus NOWAIT source-row locks protects capture against concurrent legacy edits. Stored code-point text limits, nullable metadata, ingredient IDs, order and separate quantities/units are preserved rather than forcing the snapshot through new-recipe validation. Planned detail is read separately from the lightweight week list and is bound to its revision. Legacy entries have no fabricated historical snapshot.

The combined fixture adds the exact `meal_grocery_templates` definition from `legacy-meals/tables.sql` and categories to the existing audited routine/removal fixture, whose definition/entry shapes match. It retains real preparation/notification closure behavior. Tests cover 21 evolving recipe selections, loss/replay, unchanged history after library archive and meal move/remove, replacement preparation effects and full rollback, RLS/revocation, input/version boundaries, locks, unknown metadata and 200-ingredient/Unicode limits. HTTP/native/AI selection is a subsequent slice; no production migration is applied.
