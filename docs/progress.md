# Nest progress

Updated 25 September 2026. Authority: [product brief](native-rewrite/product-and-design.md), [architecture](native-rewrite/architecture-audit.md), [implementation plan](native-rewrite/implementation-plan.md).

**Most feature code is implemented; no M0–M9 milestone has satisfied all exit criteria. No actual iPhone journey has run.** Database and HTTP verification uses disposable synthetic data. Bundles, mocked providers and Node SQLite tests are not device acceptance.

## Current delivery state

- Main: `e0c93852b97886ffcfdec07cf77b4acfd6fb60f5`, advanced after clean exact-commit Sol review and successful CI `36176253731`. Feature branch: `codex/native-acceptance-continuation`.
- Main includes account/notification assistant handoffs, settlement approval and direct-save recovery, the mobile CI selection guard, and the corrected meal-context privacy assertion. The failed substring assertion could match digits in an opaque hash; its replacement checks the decoded requester goal and exact profile field allowlist.
- Latest implementation candidate: `54566d2651ea8a9c077f7aedccc136608d08a08f`, receipt device handoffs. GPT-5.6 Sol medium reports CLEAN for that exact commit and independently passed eight focused authorization/result-binding checks. CI `36176773233` remains in progress: formatting, lint, types and focused mobile suites passed; the PostgreSQL suite is running. Pending checks are not approval.
- Main now includes migration 195's owner-only household write barrier and the maintenance-error classification fix. Retained financial-detail reads are also merged; receipt handoffs remain a candidate. Intermediate `ac070a7` must not merge alone: `aeeb6dd` fixes its reproduced offline conflict misclassification.
- The latest GitHub PR check found no open PRs. The owner authorized reviewed feature branches and fast-forward main without PRs. Exact-commit CI and clean Sol medium review still gate merging. No deployment follows a merge.
- The continuation automation remains removed. No purchases, production migrations, scheduler activation, deployment, app retirement or release publication have occurred.

## Latest local verification

- **Recovery rehearsal:** all 54 legacy and 195 native migrations apply to disposable fixtures. The latest full report is `/tmp/nest-adjustment-approval-recovery.json`; security advisors report no findings. Both members retain identical balance/history/expense detail after the committed freeze; outsiders are refused. The final all-household financial snapshot reconciles after all recovery probes. This does not prove hosted infrastructure or complete cutover recovery.
- **Write barrier:** the default-open migration protects current public/private tables; only the database owner can freeze/resume. Coverage validation rejects missing/disabled guards. Five database cases exercise in-flight transaction drainage, retained reads, denied writes/truncation, role restrictions and pre-existing transaction isolation. Auth/Storage, external requests, owner jobs, concurrent DDL and pending-client epoch reconciliation remain outside this proof.
- **Offline maintenance handling:** a real HTTP/PostgREST/SQLite test reproduced that generic SQLSTATE 55000 became a grocery conflict. The unapplied migration now raises `PT503`, classified as unavailable. Grocery and chore intent remains pending through freeze/restart and resumes without duplicate effects. Ten focused cases pass across grocery, chore and barrier tests: `/tmp/nest-freeze-grocery-after.log` and `/tmp/nest-freeze-chore-after.log`.
- **Settlement recovery:** five HTTP/PostgREST/SQLite cases cover consumed/denied approval restart, unresolved staged approval, and direct Save/Cancel restart. Terminal authoritative reads clear metadata without repeating writes; unknown intent remains durable and cannot be replaced by its opposite. Reports: `/tmp/nest-settlement-unresolved-http.log` and `/tmp/nest-settlement-save-freeze-http.log`.
- **Receipt handoffs:** registered tools reauthorize membership and return fixed links to expense entry, grocery expense entry and upload review. They do not choose files, upload/read/remove receipts, grant approvals or post money. Twenty-two focused cases, API/mobile typechecks and scoped lint/format pass. Report: `/tmp/nest-receipt-handoff-tests.log`. Live model selection, actual picker/navigation and device file handling remain unverified.

All reports above use synthetic data. `completeRecovery`, `externalRequestsDrained` and `ownerJobsStopped` remain false. Detailed preceding candidate evidence is preserved at `54566d2651ea8a9c077f7aedccc136608d08a08f:docs/progress.md`; its pending statuses are historical.

## Refund and correction approval recovery — locally verified candidate

The committed recovery freeze now retains the audited owner-only refund/correction approval readers. Six independent proposals cover consumed, denied and pending states for both commands. Exact owner envelopes and full approval rows survive; partner, outsider and anonymous reads fail; every confirm/deny and direct execution call is refused. The final all-household financial snapshot remains identical after those probes. The complete 54/195 rehearsal passes with empty security advisors (`/tmp/nest-adjustment-approval-recovery.json`); scoped lint/format pass. Exact review/CI remain pending. This is synthetic SQL evidence, not native restart, live AI or complete recovery.

The stale Required CI table row identified by Sol is corrected to the currently merged financial-detail recovery commit and its exact successful run.

## Next work

1. Complete the requirement/action audit against actual native screens, authorized commands, registered AI tools and meaningful tests. Historical inventory paragraphs are not current completion evidence.
2. Complete migration cutover/epoch handling and pending-command reconciliation; preserve new financial history during recovery. The bounded fixture below is not complete recovery.
3. Configure an approved isolated backend/provider and run live integration once configuration is supplied.
4. Execute real iPhone/two-member acceptance and a release-mode build, fix findings, then prepare a concrete release/cutover approval package.

## Milestone checklist

- [ ] **M0 — Decisions and execution route.** Tooling, delivery ADR, action inventory, exact dependency pins and linked EAS app identity implemented. CI enforces formatting, lint, typechecking and focused tests. Native smoke procedure exists but has not run. Exit still needs an executable iPhone development build and recorded native evidence.
- [ ] **M1 — Quiet native interactions.** Four-tab development preview, generated artwork, sample chore/checklist interactions, meal replacement and calendar toggles implemented. Every preview uses identified fictional data; release guards prevent presenting it as working data. Owner review, large text, VoiceOver, Reduce Motion and real keyboard/navigation checks remain.
- [ ] **M2 — Authenticated offline/AI slice.** Bearer membership API, chore read/completion HTTP and shared AI tools, transactional receipts, SQLite journal, Effect/AI SDK boundary, durable approval records and owner-private transcript persistence implemented. Native Today → outbox → API integration is merged in PR #15. Cold-start identity recovery is merged in PR #23 and locally verified. Private native chat and SDK/server lifecycle are merged in #25–#27; atomic household command journaling is merged in #28. Mutating assistant tools and canonical native receipt links are merged in #29. Live provider/structured generation, approval-resume integration and an actual native retry/restart journey remain.
- [ ] **M3 — Identity, onboarding and settings.** Native Apple sign-in, SecureStore session adapter, refresh/membership verification and retryable logout merged in PR #14. Private food-profile storage and API are merged in #30–#31; the native form and exact save recovery are merged in #32. Journaled AI food settings are merged in #33; shared cooking storage/API is merged in #34; the native cooking form is merged in #35 and corresponding journaled AI actions are merged in #36. Private memory storage/API/native management/AI actions are merged in #37–#40. Calendar consent/API/native sharing/AI availability are merged in #41–#43. Notification preference storage/API/native controls are merged in #44–#45; corresponding AI actions are merged in #46. Independent quick-start/comprehensive setup and assistant handoff are merged in #47. Meal-entry setup prompts and links from all five reminder editors to notification enrollment are merged. Actual OS permission/enrollment and onboarding acceptance remain unverified. Existing-member Apple linkage and Keychain behavior need device verification.
- [ ] **M4 — Today, chores and groceries.** Real authenticated Today chores, online routine creation/edit/pause/resume/archive/skip/reschedule and corresponding journaled AI actions are merged. Native handover request/recipient acceptance/decline, one-statement coherent reads and readonly offline cache are merged through #71; corresponding private AI handover tools are merged in #72. Groceries support online add/edit/remove, inline add, checked collapse and offline checks. Account-level reconnect and cold-start identity recovery are merged. Grocery grouping is merged in #74; queue/receipt retention bounds are merged in #73. Today now composes real meals, renewals, calendar, variable bills and private financial confirmations; saved-summary discovery is merged. Final aggregation usability and actual two-device fault/acceptance journeys remain. Node SQLite and real PostgreSQL/HTTP evidence do not establish device execution.
- [ ] **M5 — Meals and week planning.** Week board, manual placement/move/replacement/removal, saved-recipe CRUD/selection, leftovers and linked preparation have storage, authenticated API, native UI and private AI implementations. Generated private proposals and explicit exact-revision approval are merged across storage/API/native. Single-suggestion replacement and exact saved-choice generation, durable edits and API are merged; native controls are merged after CI and independent review. Assistant proposal actions and native preview handoff are merged after CI and independent review; separate ingredient review, pantry exclusion and idempotent grocery addition are also merged with API/native/AI coverage. Real provider compatibility/dietary suitability and the full seven-day journey on an iPhone remain unverified.
- [ ] **M6 — Calendar and availability.** Read-only Expo adapter and consent-bound sanitized server snapshots implemented separately. Consent incarnation invalidates delayed requests across membership removal/rejoin. Authorized API, native selection/consent/publisher and assistant availability reads are merged in #41–#43. Local selections and pending consent are account-leased; foreground/reconnect publishing uses a 31-day horizon and 15-minute expiry. Authorized busy snapshots now feed proposal generation and preparation overlap warnings. Personal agenda and partner busy UI are merged with local/controller/HTTP verification. Production Calendar route integration and optional chore/renewal layers are implemented and merged with local verification. Two-device privacy/permission inspection remains; foreground refresh is the implemented behavior and background freshness is not promised.
- [ ] **M7 — Money and recurring automation.** Balance/history/detail, equal/exact/percentage expenses, full/partial settlements, append-only corrections/refunds, receipt selection/upload/recovery/removal, private AI proposals/confirmation and durable financial recovery have local implementations merged after CI/review. Recurring configuration, pause/cancel/resume and their private AI approval flows are merged. Fixed-cycle transaction invariants and explicit variable-cycle entry/Save recovery are merged; variable private approvals and AI proposal handoff are merged after CI/review. Explicit manual linkage, bounded scheduler execution/checkpoints and native cycle history are merged. Legacy rule/draft inventory, direct dismissal and private AI dismissal approvals are merged. Retained draft confirmation and explicit native/AI adoption with old-writer fences are merged; full migration reconciliation remains. Hosted scheduler activation, full migration reconciliation, hosted Storage/provider validation and native device acceptance remain. No scheduled production posting is active.
- [ ] **M8 — Renewals, reminders and push.** Renewal CRUD and reminder editing, corresponding AI actions, all six reminder sources, deterministic summaries, bounded scheduling/checkpoints, push enrollment/recovery/logout handling, delivery tickets/receipts/invalid-token handling and protected notification links are implemented with local evidence. Saved-summary discovery is merged. Hosted workers, real permission/token lifecycle, APNs delivery and cold-launch navigation on an iPhone remain unverified. No actual device enrollment/delivery is claimed.
- [ ] **M9 — Migration and release rehearsal.** The full legacy/native chain and representative synthetic reconciliation are locally verified with explicit infrastructure exclusions. Immutable renewal conversion provenance, retry/rollback and partial shopping/retention cutover are implemented; the latter preserve exact financial history after rehearsal rollback. Complete writer drainage/cutover, pending-command epoch handling, durable rollback preserving post-cutover financial events, authorized existing-data rehearsal, both-member usability acceptance and a release-mode native build remain. Production migration/cutover and TestFlight/App Store publication require separate owner approval.

## Verification evidence and limits

| Area                          | Evidence                                                                                                                                                                                                                                                          | Remaining gap                                                                                                   |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| Required CI                   | Main `e0c9385`: successful run `36176253731`, clean Sol review. Existing workflow checks format, lint, types, focused tests and database suite.                                                                                                                   | Later branch commits require their own passing CI.                                                              |
| Mobile unit selection         | Expanded notification/push/reminder command: 47 cases; session/sign-out: 33 cases locally passed. Guard candidate checks every root mobile unit file is selected exactly once, including a negative omission probe.                                               | Nested HTTP integration tests are separate; selection does not prove runtime behavior.                          |
| Account/notification handoffs | 18 focused cases passed at `d6e9144`; API/mobile types and scoped lint/format pass. Missing/revoked membership denied, only identity/member GETs, fixed successful destination required. Account settings reaches explicit native sign-out via Household account. | Live model selection, native navigation and device permissions unverified.                                      |
| Setup entry                   | Meal setup: five lifecycle cases plus real HTTP/PostgREST journey. Reminder links: destination enrollment/client tests; Sol repeated 12 relevant cases.                                                                                                           | Actual navigation, form-return preservation, keyboard and accessibility unverified.                             |
| Offline recovery              | Real SQLite/native runtime → HTTP → PostgREST cases cover lost responses, restart, actor isolation and recovery while writes are suspended for chores/groceries and expense Save/Cancel.                                                                          | Expo SQLite, actual process death/network reconnection and two-device races unverified.                         |
| Money/auth/privacy            | Focused domain properties, SQL/RLS, concurrency, rollback, approval and transport tests exist across implemented slices; required CI runs them.                                                                                                                   | Synthetic Auth does not establish Apple linkage; local Storage metadata does not establish hosted bytes/access. |
| Migration                     | Full 54-legacy/195-native chain locally rehearsed; exact financial snapshots, retained records, public API fence, eight legacy job pause entry points and recovery probes.                                                                                        | One explicit `pg_net` exclusion, simulated Auth/Storage, no actual cron or dispatched-HTTP drainage.            |
| Native                        | iOS exports and prepared smoke/manual protocols exist in [native verification](native-rewrite/native-verification.md).                                                                                                                                            | Zero physical-iPhone, EventKit, Keychain, APNs, usability or release-binary acceptance.                         |

The committed recovery fixture preserves direct expense, settlement, refund, correction, recurring configuration/state/variable-cycle receipts and pending/consumed expense approval reads. Original owners recover exact results; partner/outsider behavior is checked; new financial writes remain refused. Chore/grocery receipts and full routine/financial snapshots survive the freeze. Final financial comparison runs after recovery probes.

Latest full fixture report: `/tmp/nest-adjustment-approval-recovery.json`; account tests: `/tmp/nest-account-handoff-tests.log`; typechecks: `/tmp/nest-account-api-types.log` and `/tmp/nest-account-mobile-types.log`. Temporary reports are local evidence, not durable release artifacts. Reproduction and bounded coverage are documented in [migration rehearsal](native-rewrite/migration-rehearsal.md).

`completeRecovery`, `externalRequestsDrained` and `ownerJobsStopped` remain false. Other financial approval kinds, legacy draft/adoption recovery, meal proposals, preferences/household commands, attachments and the cutover epoch require further reconciliation. Refusing writes and retaining receipts alone does not prove complete rollback.

## Exact external blockers / owner inputs

- **Development backend and AI:** identify an approved isolated Supabase project and API/provider configuration location. Only `.env.example` files were found. A clarification is already pending; share identifiers/paths rather than secrets. Need existing-member Apple identity mapping, server-only planning configuration and live streaming/structured-generation verification.
- **iPhone execution:** this Linux host has no Xcode/xcrun or Maestro on PATH; EAS Simulator last reported unavailable. Linked EAS identity is `@drrius-dev/nest`, project `aca37c4f-1bcd-4e9a-a695-37e794f97933`, bundle `ch.drrius.nest`. Need a signed development build and test iPhones, Apple signing/provider/APNs access, then both-member calendars/privacy and usability acceptance. No paid build or release is authorized.
- **Hosted workers/Storage:** isolated provider execution, receipt upload/read, scheduled cycles, notification ticket/receipt handling and actual APNs delivery remain unverified. No production scheduler activation is authorized by source merges.
- **CI integration proposal:** clean-reviewed local branch `codex/core-integration-ci`, commit `b7ca4609d7f9a9319d3d60734ad3758e762bb62e`, cannot be pushed because the GitHub OAuth credential lacks `workflow` scope. Owner can grant it with `gh auth refresh -h github.com -s workflow` or provide a suitable credential. Ten proposed core HTTP/PostgREST cases passed locally; they are not claimed as remotely CI-verified. Current CI remains intact.
- **Existing-data rehearsal and release:** representative synthetic fixtures cannot prove exact real-household reconciliation. Bounded existing-data access, production migration/cutover, old-app retirement and TestFlight/App Store publication need separate explicit approval after concrete preparation.
- **Review:** Greptile exhausted its trial allowance. Owner-authorized GPT-5.6 Sol medium review works and is not a blocker. No credits purchased.

## Historical evidence

Detailed incremental verification/PR history previously in this file is retained at Git commit `d6e91440c0df6316070ec06daec073afd2bd6a22:docs/progress.md`. It includes time-specific candidate and pending states and must not be read as current status. This checklist replaces that chronological log so superseded blockers do not obscure the remaining work.
