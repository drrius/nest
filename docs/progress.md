# Nest progress

Updated 21 September 2026. The approved [product brief](native-rewrite/product-and-design.md), [architecture](native-rewrite/architecture-audit.md) and [implementation plan](native-rewrite/implementation-plan.md) remain authoritative. Historical implementation notes are preserved in Git history; this checklist describes current status.

**No milestone has met all exit criteria. No complete native vertical slice or device verification is claimed.** Foundations are merged and independently tested; integration is underway. All database execution so far uses disposable synthetic fixtures. No production data, migrations, purchases, deployments or releases have been performed.

## Current work

**M7 — direct native expense entry; branch `codex/native-expense-entry`.** M5 ingredient/checklist integration is merged after clean review and successful CI. Saved recipes, manual week edits, leftovers and linked preparation span storage, authenticated API, native UI and private AI. They remain device-unverified. Generated preview and explicit approval now span storage, API and native UI in local fixtures; hosted-model and device verification remain unavailable.

Recent deliveries merged and pushed to main after exact-commit CI and clean GPT-5.6 Sol medium review:

- `c2c0314` — strict proposal/approval contracts; CI `35561147879`. Sol's missing saved-selection receipt binding was fixed before merge.
- `d086b29` — retained one-off recipe representation; CI `35561569589`. Fifty-two focused storage/API/cache/HTTP checks passed; no approval writer enabled.
- `e56281e` — bounded structured generation through the real AI SDK using simulated providers; CI `35562298604`. Twenty focused checks passed. Sol's library household-binding finding was fixed before merge.
- `e138420` — private proposal reservation/read/discard; CI `35562751912`. Eleven focused PostgreSQL/context checks passed locally and independently in review; security advisors found no issues.

The current worker increment grants one server claim per proposal, retains immutable completion results, and exposes owner-only deadline recovery. Completion rechecks current food constraints, household membership, cooking choices, saved-recipe content and the exact occupied-week baseline. It writes only private proposal state: no meals, groceries, preparation or financial records. Claim retries never authorize another model call. Interrupted work expires rather than being silently regenerated. A completion retry returns its historical receipt; current proposal reads remain separate.

Local evidence: 23 focused PostgreSQL cases pass, including ten new cases covering concurrent claim/completion, expiry, revocation, strict generated/saved content, mixed occupied slots, constraint changes, receipt rollback and ten completion/discard races. Security advisors report no issues; the ten new cases also pass after the final household-lock change. Merged as `666c752` after successful exact-head CI `35564138556` and clean Sol medium signoff (17 independently passing PostgreSQL cases). Local main was fast-forwarded and pushed; no PR was created. No hosted provider, production migration or device execution occurred.

The API candidate exposes authenticated generate/read/recover/discard routes with strict shared response binding. It reserves an immutable operation, reads fresh authorized context/week/busy availability and a bounded saved-recipe shortlist, then obtains the one-time worker claim before model dispatch. A changed fingerprint/week stops before generation. Retries recover stored state, cancellation leaves deadline-recoverable work, and neither missing credentials nor provider failures invent a successful preview. Read/recovery/discard work without model credentials. The optional planning secret is server-only, redacted and separate from caller authentication; no credentials were installed.

Twenty-four focused API/generation/actual HTTP–PostgREST–PostgreSQL cases pass. Nine new HTTP journeys cover concurrent model dispatch, both lost claim and lost completion acknowledgments, cancellation/late results, changed constraints before and during generation, familiar-only missing recipes, exact discard, current authorization and no business-record writes. Two new boundary cases reject mismatched worker/receipt/content responses. API/contracts typechecks, scoped lint/format and diff checks pass. Merged as `675eb53` after successful exact-head CI `35565014287` and clean Sol medium signoff (21 independently passing focused checks). Main was fast-forwarded and pushed without a PR. Provider outputs/Auth gateway translation are simulated; no live model, native UI, hosted deployment or device validation is claimed.

The native candidate adds a protected week-preview route reachable from Meals, a saved-recipes-only switch, virtualized proposal/ingredient views and explicit discard/recovery actions. It saves only account/lease-scoped request metadata in SQLite, never private recipe content or planning context. A dedicated authenticated reservation endpoint lets the phone persist the returned proposal ID before requesting any model work. Restart loads/reconciles the stored request without automatically generating. Exact discard confirmation and receipts prevent an interrupted attempt from acting on a different revision. Account changes hide the private preview and stale leases cannot dispatch generation after reservation.

Forty focused native/API/database checks pass, including eleven new client/runtime/SQLite cases and three native client → HTTP/API/AI SDK/PostgREST/PostgreSQL journeys. Coverage includes lost reservation/generation/discard responses, SQLite restart, current membership, storage failure before dispatch, changed local request identities and no meal/grocery writes. A virtual clock verifies generation's 180-second client bound while ordinary requests retain fifteen seconds. Mobile/API types, scoped lint/format and iOS export pass (`/tmp/nest-native-proposal-export`, bundle `3ae78d6cda7f2e5b7e9650969d0a19e9`); export is packaging evidence only. Merged as `9ac2609` after successful exact-head CI `35566631802` and clean Sol medium signoff (all 14 new native/SQLite/HTTP cases independently passed). Main was fast-forwarded and pushed without a PR. Native controls, back gestures, accessibility, hosted provider and physical iPhone execution remain unverified.

The approval storage candidate posts the exact ready proposal revision atomically into retained shared meal snapshots. It rechecks current owner membership, food/cooking/roster fingerprint, the whole week baseline and destination occupancy; expiry is checked after blocking writes. Immutable operation receipts survive later week edits, while a different approval or discard cannot reuse the same intent. Suggested recipes receive null library provenance and stable ingredient IDs without becoming saved recipes; saved recipes retain their reviewed body and library revision across later edits/archive. No groceries or preparation are created.

Forty-one focused PostgreSQL checks pass, including ten new approval cases covering every nonempty configured slot set, occupied-slot preservation, two-owner contention, approve/discard races, late expiry, food locks, strict inputs, private helper/receipt access and full receipt-failure rollback. Supabase security advisors found no issues on the disposable approval fixture. API typechecking and scoped lint pass. Merged as `04cf4c0` after exact-head CI `35567490168` passed and Sol medium independently signed off with 20 passing PostgreSQL cases. Main was fast-forwarded and pushed. Automatic approval review initially rejected a combined CI-read/merge command because the displayed CI evidence was still pending; a separate authoritative read proved success and the same authorized merge then succeeded without requesting owner approval. The native approval confirmation/recovery and ingredient review are still unfinished; this storage boundary is not a completed user flow. No production migration or device/provider execution occurred.

The API candidate adds explicit authenticated `POST /v1/meals/proposal/approve`, accepting only operation ID, proposal ID and displayed revision. The shared Effect adapter forwards the caller token, strictly decodes and binds the immutable receipt, and needs no model or server planning secret. Generation and assistant tools cannot implicitly invoke it. Query/identity/recipe injection is rejected; errors remain finite and private/no-store. Receipt recovery uses the original operation even after later week edits and rechecks current membership.

Twenty-five focused API/schema/native/HTTP cases pass, including four new actual PostgreSQL/PostgREST approval journeys and two strict adapter cases. They cover lost committed acknowledgment, competing owner proposals, private access denial, changed constraints, response identity forgery and no grocery/preparation side effects. API types, scoped lint/format and diff checks pass. Merged as `972ca2c` after successful exact-head CI `35568183131` and clean Sol medium signoff (20 independently passing API/PostgREST cases). Main was fast-forwarded and pushed. No live model, hosted deployment or iPhone execution is claimed.

The native approval candidate adds an explicit confirmation explaining that the displayed meals become shared while groceries remain separate. Both approval and discard alerts capture proposal ID and revision, so an older alert cannot act on a different preview with the same revision. The phone saves the exact approval operation in account/lease-scoped SQLite before dispatch. Older metadata with no approval intent remains readable. Approval and discard intents cannot coexist; unknown outcomes block unrelated actions. Restart only recovers current state; explicit continuation retries the same approval only if no terminal outcome is known. A definite conflict clears only that approval and requires a refreshed explicit decision. Confirmed approval opens the selected saved week.

Forty-four focused native/cache/SQLite/API/PostgREST checks pass, including seven new native client/runtime/storage cases and two actual native approval journeys. Lost committed acknowledgment and SQLite restart recover without re-posting; partner week changes refuse approval, forged receipt mappings never fabricate success, and metadata/storage/access failures remain recoverable. Mobile types, scoped lint/format and iOS export pass (`/tmp/nest-native-approval-final-export`, bundle `ab890e5d6b75fb9dc84afc3d66b25283`). Export is packaging evidence only. Merged as `8ba897f` after successful exact-head CI `35569029723` and clean Sol medium signoff (23 independently passing native/SQLite/HTTP checks). Main was fast-forwarded and pushed. Native alert interaction, navigation, VoiceOver and physical iPhone execution remain unverified. No live model, deployment or production migration was used.

The single-meal model candidate extracts the existing canonical choice binding and independent dietary check without changing full-week generation behavior. Internal replacement validates the owner/household/week envelope, ready/unexpired state and one still-empty target slot, then returns only that entry with its original ID/date/slot. The model sees the previous recipe and other meal titles as context, but receives only one output slot. Favorite selection retains an exact freshly authorized saved recipe and uses only the suitability check; it cannot rewrite the selected recipe or carry over another meal's calorie estimate. Same saved recipes or identical source content are rejected as replacements. No route, edit reservation, write transaction or UI is enabled by this internal model function.

Twenty-two focused simulated-provider/real-SDK/API/PostgREST checks pass, including eight new single-meal cases. They verify unchanged input/other entries, exact favorite retention, private projection, missing/stale/foreign evidence before spending, extra/wrong slots, unsafe/unknown checks, familiar-only behavior, cancellation and oversized review failure. API types and scoped lint/format pass. Merged as `175594e` after exact-commit CI `35569753701` and clean Sol medium review (22 independently passing checks). Single-edit operation persistence, current-constraint commit checks, HTTP/native selection and corresponding assistant actions remain unfinished; no hosted provider, semantic dietary-safety proof or device execution is claimed.

Remaining M5: finish checklist capacity/provenance integration and audit acceptance, plus live-provider and device acceptance of generation/approval. Semantic dietary adequacy and live model/schema/budget compatibility require an approved provider configuration; simulated-provider tests do not establish those properties. M6–M9 and earlier device/acceptance gates remain incomplete.

### Durable single-suggestion edits — storage candidate

The single-meal generator merged as `175594e` after successful exact-commit CI `35569753701` and clean GPT-5.6 Sol medium review (22 independently passing checks). Main was fast-forwarded and pushed under the owner’s standing branch-review workflow. No PR or automation was created.

The current edit-storage candidate reserves a strict owner-bound replace or saved-choice command and a fixed two-minute deadline. A proposal revision permits one pending edit, and an operation permits one server worker claim. Completion replaces only the target entry, preserving all other reviewed snapshots, and advances the private proposal revision once. Exact saved selections are captured and revalidated; stale food/cooking/roster/week state fails without rewriting the preview. Terminal results are immutable across later edits, approval or discard. Authorization precedes every historical retry. No active meals, groceries or preparation records are written.

Local verification passes 25 PostgreSQL cases covering the new storage behavior and existing approval regressions, including eight edit/approval-or-discard races, concurrent retry/claim, tenant and owner isolation, malformed commands, recipe substitution, familiar-only enforcement and atomic rollback. Three additional checks prove deadline expiry after a blocking week lock, in-flight partner preference changes and immutable replay after another edit/approval. Security advisors found no issues on the disposable fixture. API typechecking, scoped lint, full formatting and diff checks pass. Merged as `adea948` after successful exact-commit CI `35571099738` and clean Sol medium review (22 independently passing edit/approval cases). Main was fast-forwarded and pushed without a PR. The edit API, native controls, assistant actions and separate ingredient review remain unfinished. All database execution uses synthetic disposable fixtures; no production migration, live provider or device verification occurred.

### Authenticated suggestion edit API — candidate

Authenticated reserve, edit and recovery routes share the strict owner-bound command and durable storage. Reservation/recovery require no model credentials. Execution reads current authorized preferences, week and availability before acquiring the one-time worker claim; mismatched context stops before model spending. Saved choice reads the exact recipe directly, including recipes beyond the first fifty-entry shortlist, and performs only the suitability check. Replacement uses the bounded two-stage generator. Neither path approves the proposal or posts shared meals/groceries. Request cancellation leaves deadline-recoverable state; lost claim acknowledgment never starts a second provider run, and lost completion recovers the immutable result.

Thirty-two focused API/SDK/PostgREST cases pass, including eight new real HTTP/database edit journeys and three new adapter/worker forgery cases. Coverage includes exact-target retention, saved selection beyond fifty recipes, concurrent requests, lost claim/completion, unsafe/provider failures, owner/tenant/current membership, stale constraints before and during execution, cancellation and unchanged original preview on failure. API types, scoped lint, full formatting and diff checks pass. Merged as `8d990c3` after successful exact-commit CI `35571833763` and clean Sol medium review (all 32 checks independently passed). Main was fast-forwarded and pushed. Provider outputs remain simulated; native editing controls, assistant proposal tools, separate ingredient confirmation and physical-device/hosted-provider acceptance remain unfinished.

### Native suggestion editing — candidate

The private preview now offers Generate a replacement and a paginated saved-recipe picker for each entry. The action captures proposal ID, displayed revision and entry ID; a stale picker cannot target a newer preview. Account/lease-bound SQLite stores only the immutable edit command before dispatch, with approval/discard/edit intents mutually exclusive. Restart reads current proposal and edit status without sending a new model request. Unknown/pending outcomes block approval and other edits. Confirmed completion refreshes the canonical preview before clearing intent; failures preserve the original suggestion. An invalid or conflicting selection clears only after the API confirms that no operation exists, so incomplete/same-recipe choices do not trap the screen. Native HTTP uses expo/fetch and the existing bounded generation transport.

Thirty-four focused native client/runtime/SQLite/real HTTP checks pass, including nine new native cases and two new actual native → API → SDK → PostgREST → PostgreSQL journeys. Tests cover stale target capture, metadata-before-dispatch, lost completion/restart, unregistered intents, one-time suitability checks, immutable account leases, conflicting approvals, storage failure, invalid saved choice, stale library and current membership revocation. Mobile types, scoped lint, full formatting and diff checks pass. The final iOS export passes at `/tmp/nest-native-edit-final-export` (bundle `374d215486e0009f1da7694a9d18412b`), which is packaging evidence only. Merged as `ca7637b` after successful exact-commit CI `35572810680` and clean Sol medium review (all 34 checks independently passed). Main was fast-forwarded and pushed without a PR. Native controls, keyboard/navigation, large text and VoiceOver remain device-unverified. Provider responses are simulated. Assistant proposal actions and separate ingredient confirmation remain unfinished; M5 is not complete.

### Opening a proposal from an assistant handoff — storage/API candidate

The owner-only open command recovers the original generation receipt together with current proposal content, including subsequent edits or approval. This gives a native handoff its original durable request identity instead of inventing a new generation operation. Current membership and private proposal ownership are checked before historical receipt lookup. Expired unfinished generation recovers failure; opening never claims a model worker, posts a plan or needs model credentials. The authenticated API strictly binds receipt, current envelope and requested owner/proposal. An index restricts lookup to the unique original generation reservation.

Fourteen focused PostgreSQL/adapter/real HTTP checks pass, including three new storage cases, one response-forgery adapter case and one actual HTTP handoff journey. Existing generation recovery tests remain green. API types, scoped lint, full formatting/diff checks and disposable security advisors pass. Merged as `a7a5837` after successful exact-commit CI `35573412090` and clean Sol medium review (14 independently passing checks). Main was fast-forwarded and pushed without a PR. The native deep-link adoption and assistant generation/edit/discard tools still need implementation; this boundary alone does not finish that flow.

### Native proposal handoff — candidate

The private proposal route accepts a strict proposal ID and Monday week. It reads the original generation receipt and current preview without reserving or generating again. SQLite adopts only account-scoped request metadata after an atomic comparison with the previous operation; concurrent or unresolved generation/edit/approval/discard intents cannot be overwritten. An existing proposal keeps its saved intents. Wrong-week/owner responses, stale account leases and storage failures cannot reveal a newly opened preview. A blocked handoff remains available after the previous request is finished and reset.

Focused verification includes native client/SQLite restart, strict link parsing, original-operation adoption, concurrent approval protection, wrong-week/account rejection, lower-revision different previews and retryable storage failure. An actual native → HTTP → API → PostgREST → PostgreSQL journey opens an externally generated preview after a lost read and explicitly approves it with no additional model call. Mobile types, scoped lint/format and iOS export pass (`/tmp/nest-native-handoff-export`, bundle `6719dc2dbf14983e22e4430597f53208`); export is packaging evidence only. Merged as `bc52c21` after successful exact-commit CI `35574414221` and clean Sol medium review (all 27 focused checks independently passed). Main was fast-forwarded and pushed. Assistant proposal tools, separate ingredient review, hosted provider and physical iPhone acceptance remain unfinished.

### Private assistant proposal journal — storage candidate

The gated journal extension reserves generation, replacement and exact saved-recipe choices using server-owned operation IDs, and journals explicit discard. All four dispatch through the existing authorized native transactions. Generation and edit records remain immutable reservation facts after worker completion; current proposal/edit reads are separate. The model cannot inject operation, actor or approval fields, and plan approval is absent from the accepted vocabulary. Canonical conversation history replaces fabricated proposal writes with committed journal records. Native effects and receipts roll back if journal insertion fails.

Eleven PostgreSQL cases pass, including seven new proposal cases for concurrent replay, post-completion immutability, exact saved selection, owner/tenant/revocation checks, malformed authority, terminal conflicts and generation/edit/discard rollback. Four existing preparation-journal regressions remain green. Disposable security advisors found no issues; migration functions remain below the configured length limit. Merged as `7b732ce` after successful exact-commit CI `35574922152` and clean Sol medium review (all 11 checks independently passed). Main was fast-forwarded and pushed. This is storage preparation only: assistant tools, post-journal provider dispatch, bounded current-state reads and proposal cards still need implementation. No live provider, production execution or physical-device verification occurred.

### Assistant proposal tools and native approval handoff — candidate

Private assistant tools now generate a proposal, replace one suggestion, choose an exact saved recipe, discard, and read current proposal/edit state. Shared strict contracts reject model-supplied operations, identity and approval. Model work starts only after the authorized reservation is committed to the private journal; a failed/lost journal acknowledgment cannot dispatch a provider. Replay retains the original operation and one-time worker claim. Missing planning credentials reject new model work before journaling. Tool results retain the journal's reservation fact; fresh read tools establish completion or failure. Native cards open the exact private proposal/week, and approval is available only through the existing native confirmation.

Forty-one focused SDK, receipt-binding, native-card and actual PostgREST/HTTP checks pass. Seven new real integration journeys cover lost journal acknowledgment, lost worker completion, one-time exact saved choice, single-target replacement, private/revoked access, missing credentials, malformed authority, rollback and assistant generation → native card → HTTP preview → explicit approval. No shared meal is posted before that approval. Existing SDK cancellation/history and preparation-journal integration regressions pass. API/mobile types, scoped lint, full formatting and diff checks pass. Merged as `4859b90` after successful exact-commit CI `35575785316` and clean Sol medium review (all 41 checks independently passed). Main was fast-forwarded and pushed. Provider outputs are simulated; physical iPhone navigation, accessibility, real-provider behavior and separate ingredient review remain unverified/incomplete.

### Separate ingredient review — contracts, domain and storage candidate

The [ingredient boundary audit](native-rewrite/ingredient-review-boundary.md) rejects legacy automatic materialization. New shared contracts address retained meal/ingredient identities, revision-bound pages, explicit quantities/units and per-source receipts. Pure selection reconciliation preserves exclusions and member edits; new/replaced or already-added sources stay unselected. No ingredient-name merging or unit conversion occurs. Grocery text codecs now count Unicode code points like PostgreSQL, allowing imported quantities/units to remain readable and editable without truncation.

Gated storage reads at most 100 retained ingredients per page and reports leftovers, missing recipes and empty ingredient lists separately. Addition locks the exact saved week, validates each source, atomically adds only selected rows and retains meal provenance. Household-wide source identity deduplicates concurrent additions even under different operations or actors. Existing groceries are never overwritten; receipts distinguish newly added and previously added sources and survive later checking/editing/removal or meal removal. Current membership is required for historical replay. Archived same-household categories become uncategorized; foreign categories fail. No proposal, shopping session or financial record is created.

Thirty-five focused property/contract/PostgreSQL/grocery-regression checks pass, including 1,000 generated reconciliation cases, complete 4,200-ingredient capacity, pagination, Unicode, quantity/unit preservation, pantry exclusion, same-operation replay, different concurrent quantities, generated one-off recipes, stale/foreign/leftover sources, full rollback and revoked replay. Disposable security advisors found no issues. API/mobile/contracts/domain types, scoped lint, full formatting and diff checks pass. Merged as `310855c` after successful exact-commit CI `35577909509` and clean Sol medium signoff (all 35 checks independently passed). Main was fast-forwarded and pushed. The native review/durable draft/confirmation, checklist provenance and assistant handoff still need implementation. This storage boundary is not a finished grocery-list flow; physical-device verification remains outstanding.

### Ingredient review HTTP API — candidate

Authenticated POST read/add routes share the ingredient contracts and caller-token database functions. Pages bind household, exact week revision and forward-only cursor; addition receipts bind actor, household, operation, baseline and every selected source in order. Unknown fields and fabricated/merged mappings fail closed. The add route accepts bounded full-week bodies up to 8 MiB; ordinary review reads retain the smaller request bound. No provider credentials are needed and no proposal approval is implicit.

Twenty focused contract, adapter, PostgreSQL and actual HTTP/PostgREST checks pass, including three new transport journeys: 200 sources across two pages with a batch exceeding the ordinary body bound, committed-response loss with immutable replay after a partner correction, and stale/revoked/foreign/malformed/oversized denial without writes. Four new adapter cases cover caller forwarding, UUID normalization, response binding and error distinctions. API typechecking and scoped lint pass; Effect advisory suggestions remain. Merged as `ba27524` after successful exact-commit CI `35578805010` and clean Sol medium signoff (all 20 checks independently passed). Main was fast-forwarded and pushed. Native selection persistence, confirmation/retry controls, checklist provenance and AI handoff remain next; no physical device, live provider, production migration or deployment was used.

### Native ingredient recovery — client and SQLite candidate

The account-bound native client uses Expo fetch through the existing authenticated session adapter and strictly binds review pages and addition receipts. SQLite stores only source identities, reviewed quantities/units, selections and exact confirmed commands, scoped by actor/household/week and current account lease. Compare-and-swap sequence checks protect newer exclusions from stale controllers. An uncertain request freezes draft replacement; acknowledgment validates every source and clears the selected flags atomically. Storage failure leaves the original request recoverable. This is online request recovery, with no offline queue entry or automatic replay.

Eight focused checks pass: three native client cases, four actual SQLite cases and one native client/SQLite → HTTP/API/PostgREST/PostgreSQL journey. Coverage includes lost committed acknowledgment followed by restart, unchanged partner corrections, strict identity/cursor/receipt binding, storage rollback, stale editors, duplicate sources, revoked membership, actor isolation and lease replacement. Mobile types and scoped lint pass. Merged as `ee46d61` after successful exact-commit CI `35579470006` and clean Sol medium signoff (all eight checks independently passed). Main was fast-forwarded and pushed. The native controller and screen still need connection; no native interaction/device verification is claimed.

### Native ingredient review — screen/controller candidate

A protected week-specific screen is reachable from Meals and an approved proposal. It loads all revision-bound pages before enabling changes, shows retained meal provenance and missing/leftover source explanations, and virtualizes the ingredient rows. Native switches select only needed ingredients; a separate native quantity/unit editor preserves unknown values and exact text, warns before discarding unsaved edits and remains open on failed saves. Confirmation captures the saved draft sequence, stages the exact selected request in SQLite, then calls the real API. Refresh preserves exclusions and text edits; new/already-added sources remain unselected. Uncertain additions freeze edits and recover only through an explicit same-operation retry.

Seventeen focused native client, controller, actual SQLite and native → HTTP/API/PostgREST/PostgreSQL checks pass. Eight controller cases include incomplete-page refusal, stale alerts/quantity editors, refresh reconciliation, storage-before-dispatch failure, cancellation, definite conflicts and revoked access; the actual controller restart journey preserves a partner correction and never duplicates groceries. Mobile types, scoped lint, full formatting and diff checks pass. iOS export succeeded at `/tmp/nest-native-ingredient-export`, bundle `19df9ee804aa0da1ab0b1e28d8674592`; this is packaging evidence only. CI and exact-commit Sol review remain required. Physical iPhone controls, alerts, back gestures, keyboard reachability and VoiceOver remain unverified. Checklist provenance and the corresponding assistant handoff are still next; M5 is not complete.

### Ingredient assistant handoff — candidate

The private assistant can read revision-bound retained ingredient pages and open a chosen week’s native review using the same current-membership and caller-token reads. Its tool guidance and chat instructions explicitly require native selections, pantry exclusions, quantity edits and separate confirmation; ordinary grocery additions must not bypass that review. The strictly decoded result card opens only the fixed native route and says nothing was added. No ingredient mutation tool or approval bypass is exposed.

Forty-two focused assistant-card, real SDK, cancellation/history and actual SDK-tool → native controller → HTTP/PostgREST checks pass. Three new cases prove correct week handoff, zero grocery writes before explicit native confirmation, current/foreign/revoked authorization, stale revision denial, injected authority rejection and malformed card refusal. API/mobile/AI types and scoped lint pass. CI and exact-commit Sol review are pending. Provider outputs are not live-model verification; physical iPhone navigation remains unverified.

Native ingredient review merged as `babfc69` after successful CI `35580755267` and clean exact-commit Sol medium rereview. The initial CI formatting failure was corrected before merge. Assistant ingredient handoff merged as `64841e4` after successful CI `35581148173` and clean Sol medium review (42 independently passing checks). Both commits are pushed to main; no PR or automation was created.

### Complete grocery snapshot and meal provenance — candidate

A valid full ingredient week can add 4,200 items; the previous API/native/cache limit of 500 would then make the checklist unavailable. The candidate replaces the table read with one authorized atomic JSON snapshot, avoiding PostgREST row truncation without introducing an arbitrary larger row limit. Household-bound category and originating-meal joins expose only retained display provenance. Native validation and atomic SQLite replacement accept the complete snapshot; the virtualized checklist labels source meals. Older cached rows remain compatible. Checked/claimed rows remain visible, removed/purchased history stays excluded, and the offline retry policy is unchanged.

All 46 focused API, PostgreSQL, native checklist and actual HTTP/SQLite cases pass. The capacity journey adds 4,200 retained ingredients through the real native client/API, reads and caches all rows, reopens SQLite, and checks an item through the existing synchronization flow while preserving source provenance. Other cases cover current membership, tenant isolation, corrupt foreign source links, deterministic order, strict response fields, bigint versions, lost acknowledgments and existing grocery edits. Disposable security advisors report no issues. API/mobile typechecks pass. The iOS export passes at `/tmp/nest-grocery-source-export` (bundle `1c03b44667a9a295595e9da8f7a9abbd`), packaging evidence only. Exact-commit CI and Sol review remain pending; native label readability and interaction remain device-unverified. No production migration or deployment occurred.

Sol found that an out-of-range legacy meal date could invalidate the whole checklist. The snapshot now projects unsupported provenance dates as null while retaining the source title, ID and slot; actual PostgreSQL regressions cover both infinities, BC/year-10000 dates and the supported year boundaries. Updated exact-commit CI and clean rereview are required.

Grocery snapshot/provenance merged as `1a9db7a` after successful exact-commit CI `35582888645` and clean Sol medium rereview (19 independently passing focused checks). Main was fast-forwarded and pushed without a PR. No production migration or device verification occurred.

### Local Calendar agenda — read boundary candidate

The separate local agenda adapter reads existing EventKit occurrences and explicitly projects personal title/location/notes for on-device display. It has no API, persistence, sharing, attendee lookup or write calls. The existing sanitized busy adapter is unchanged. Effect reads check permission and selected-calendar availability before and after event retrieval, sanitize failures, snapshot caller inputs and honor cancellation. Cancelled/unselected events are excluded, free events remain visible, recurrence instances remain distinct, and duplicate conflicting instances fail rather than silently choose one. Absolute all-day/multi-day bounds and zero-duration events are preserved. The installed SDK 57 Swift getters can return null title/notes despite TypeScript declarations; those display values normalize to empty text.

Twenty focused new/existing calendar cases pass, including 1,000 generated window, exact-bound and duplicate/order properties. Mobile typechecking and scoped lint pass. This is an internal read boundary only: real Calendar UI, account-owned selection/runtime, date navigation, partner presentation and device handoff remain next. The existing tab is still a labeled preview. Real EventKit data, permission prompts, two-device privacy and native behavior remain unverified. CI and Sol review of this increment remain required.

Agenda review identified an avoidable permission race during the final asynchronous calendar enumeration. A failing controlled regression reproduced private rows returned after revocation; the reader now checks permission after enumeration as well. Updated exact-commit CI and Sol rereview remain required.

The corrected agenda reader `592249d` merged after successful exact-commit CI `35583602716` and clean Sol medium signoff (23 independently passing checks). Display selection `52760e6` merged after successful CI `35583924093` and clean Sol medium signoff (49 independently passing SQLite/calendar checks). Both are pushed to main without PRs.

### Local agenda display selection — persistence candidate

A separate account/household/lease-scoped SQLite selection stores only the calendar IDs chosen for personal display. It neither grants sharing consent nor changes existing busy-sharing selections. Empty selection and first-use absence are distinct, and private event/calendar text is rejected rather than persisted. Stale leases cannot read or overwrite another account's selection; malformed stored data fails explicitly. The native settings/runtime still need to use this store.

Eleven focused SQLite/sharing cases pass, including three new cases for real restart, independent sharing choices, account/household switches, stale/suspended lease refusal, strict input/metadata rejection, corrupted storage and a failed update retaining the previous selection. Mobile types, scoped lint, formatting and diff checks pass. CI and Sol review remain pending; no Calendar UI or physical-device verification is claimed.

### Native personal agenda — UI/controller/device-handoff candidate

The Calendar tab now reads the selected iPhone calendars instead of showing fictional events. It offers native date selection, previous/next/today navigation, a virtualized local-calendar picker with saved switches, permission/retry guidance and virtualized event rows with title, location, notes and exact bounds. Shared iCloud events remain in selected device calendars; Nest creates no event database. Viewing calendars does not opt them into busy sharing. Calendar names and events stay on-device. Background/tab blur and disposal clear personal rows and picker metadata; canceled late reads cannot restore them. Foreground/focus and a foreground minute refresh recheck permission and current account lease.

The private assistant offers an authorized `openCalendarAgenda` handoff with a strict fixed-route result card; it cannot read private event details, grant permissions or change sharing. Existing settings and availability tools retain current-membership checks. No EventKit writes are exposed.

Twenty-two focused native/SQLite/projection/handoff/day-window cases pass, plus four real SDK/PostgREST availability/handoff cases. Tests include restart, first-use versus empty choice, storage failure/retry, stale account leases, disposal/background/foreground races and 5,000 generated civil-day windows across five timezones, including DST, midnight transitions and a skipped civil date. Mobile/API/contracts types, scoped lint and full formatting pass. iOS export succeeded at `/tmp/nest-native-agenda-fixed-export`, bundle `c9a54511349de6b5d80a8a60a19dffa9`; packaging only. CI and exact-commit Sol review remain required. Real EventKit, native picker/VoiceOver, focus/AppState behavior and two-device privacy inspection remain unverified. Partner busy presentation and optional chore/renewal layers remain unfinished; M6 is not complete.

Sol found that date navigation after an initial native-calendar load failure could clear the error and present an empty successful agenda. A controlled regression reproduced the failure; runtime date/read guards and disabled date controls now require a loaded, permitted catalog. The same test verifies denied permission and successful refresh recovery. Updated exact-commit CI and Sol rereview are required before merge.

Personal native agenda `478ffd0` merged after successful exact-commit CI `35585342163` and clean Sol medium rereview (19 independently passing focused native/SDK checks). The false-empty load-recovery finding was fixed before merge. Main is pushed; no PR, production operation or device verification occurred.

### Partner busy agenda — merged

The Calendar agenda now combines virtualized personal rows and distinctly labeled partner busy blocks. It consumes the existing authorized sanitized snapshot API and shared domain freshness rules. Whole-day and partial-day coverage are explicit; a lunchtime capture can show afternoon evidence while the morning remains unknown. Missing, expired, future, disjoint or ambiguous partner evidence never implies free time. Empty fresh coverage is qualified as no shared busy blocks, not guaranteed availability. Only time ranges, capture time and expiry appear for the partner.

A separate account-leased partner runtime handles remote loading, manual/foreground/reconnect refresh, exact expiry timers, errors, revocation and canceled background reads without requiring device-calendar permission. No shared snapshots are persisted. Date navigation can change the combined agenda when personal access is unavailable, while preserving the personal error/permission state and never manufacturing an empty successful personal read. The assistant already uses the same authorized availability boundary; no private event read tool is added.

Twenty-two focused domain, controller, SQLite and privacy cases pass, including 1,000 new partial-coverage properties and the existing availability properties. Four actual native HTTP/PostgREST/PostgreSQL journeys pass, including a new partner journey for lost read recovery, partial coverage, sanitized projection, opt-out removal and revoked membership. The first integration attempt used an overly short unit settling loop; activation now returns its actual read promise and the HTTP journey awaits it. Mobile/domain types, scoped lint and formatting pass. The iOS export passes at `/tmp/nest-partner-agenda-final-export` (bundle `7206a7ca9604e5e8ddfcf11b01bd2eea`), packaging only. Merged as `9b35be1e8fe4d1a0d17b933f0be37d962ff42b74` after successful exact-commit CI `35586482003` and clean Sol medium signoff with 23 independently passing checks. Local main was fast-forwarded and pushed without a PR. Actual two-phone sharing, UI/focus/network behavior and payload inspection remain device-unverified. Optional chore and renewal layers remain unfinished; M6 is not complete.

Standing workflow: use feature branches, exact-commit successful CI and clean **GPT-5.6 Sol medium** adversarial review, then fast-forward local main and push. PRs are optional under the owner's explicit authorization. Deployment, production migration, purchases and release remain separately gated. The continuation automation was removed; do not recreate it without a new need. Historical delivery notes below retain the verification state at the time of each increment; later merge records supersede their pending-CI wording.

**Earlier M5 delivery notes.** Native saved-recipe browsing and creation have storage, authenticated API, native UI and private AI implementations. One-off meal placement/removal/move/replacement also span these layers. None of these flows is physical-device verified.

Leftover storage `cbcaeca` and API `7f4941d` are merged and pushed to main after successful exact-head CI `35553583407` / `35553642143` and clean GPT-5.6 Sol medium signoffs. The storage review caught an unnecessary rejection of legacy earlier-day unslotted ideas; the corrected commit restores that compatibility and adds a regression. Sol independently passed fifteen storage/selection cases and all five API leftovers/selection HTTP journeys.

The native leftover branch adds an account-scoped client, source/destination week runtime, protected screen, later-day empty-slot confirmation and a board action for original meals. Exact uncertain requests remain frozen; confirmed writes stay latched through authorization loss and cannot resend after recovery. Conflicts require reloading both weeks. Navigation warns about unresolved writes; no offline queue is added. Existing planned details display the copied historical recipe.

Local verification: thirteen focused client/runtime cases and two real HTTP/PostgREST/PostgreSQL journeys pass, including a native cross-week lost-acknowledgment retry after library archive, retained quantities, unchanged source revision, no new groceries and revoked access. The new integration fixture initially used a root Effect import unavailable in this workspace; it now resolves the mobile package dependency and both journeys pass. Mobile types, scoped lint/format and diff checks pass. iOS export compiled 1,967 modules (bundle `bd7e5d0906e62ef751995856a62659f6`); this is packaging evidence only. Exact-head CI and Sol review are pending. Physical iPhone interaction/accessibility and the corresponding private AI leftovers tool remain incomplete; M5 is not complete.

Sol independently passed all thirteen native cases and both real HTTP journeys, and found misleading confirmation copy for legacy/one-off meals without a retained recipe. The copy now explicitly says recipe details are retained only when available and missing ingredients remain unknown. Updated-head CI and rereview are required.

Native leftovers are merged as `aad90b1` after successful exact-head CI `35554362943` and clean Sol medium rereview of the corrected confirmation copy. Main was fast-forwarded and pushed. No iPhone execution is claimed.

Private AI leftovers now use the shared strict native input/receipt contracts and the same atomic storage command through the private journal. Tool guidance requires an explicit original source meal, a later empty slot and fresh exact source/destination baselines; it forbids generated-plan approval bypasses and invented historical ingredients. Receipt matching binds actor, household, source, destination and exact same-week/cross-week bigint revisions. Valid native result cards open the new leftover's retained details; uncertain results return to the week for reconciliation. The additive migration extends the finite dispatcher and canonical transcript handling without changing prior actions.

Local verification: all 153 selected AI journal/API/schema/native-card/SDK-PostgREST checks pass, including four new database cases and actual SDK committed-response loss, exact replay after archive, private actor recovery, partner reads, terminal stale conflict and revocation. API/mobile/SDK types, scoped lint/format and diff checks pass. Security advisors report no issues on the disposable full leftovers/journal fixture. SQL functions remain within the 80-code-line limit. Exact-commit CI and Sol review are pending; no live model, device, production migration or deployment was used. M5 proposal approval, preparation and separate ingredient review remain incomplete.

AI leftovers `7d72db3` are merged and pushed to main after successful CI `35554715535` and clean exact-head Sol medium signoff with 21 independent focused checks.

The next preparation slice has a deliberate [legacy audit](native-rewrite/meal-preparation-audit.md) and strict shared create/receipt contracts. It preserves one linked one-off task per meal, instructions and household assignment, exact meal-week baselines and microsecond routine versions. Three focused contract tests pass, including 1,200 generated civil-date/receipt-version round trips and identity, Unicode, date and assignment rejection. Contracts typecheck and scoped lint/format pass. This is a contract increment only: atomic storage, authorization/RLS, API/native/AI preparation actions and real journeys are not yet implemented or verified. No milestone exit criterion is claimed.

Preparation contracts `bc49ad8` are merged after successful CI `35554945406` and clean Sol medium review. The reviewer independently checked the pinned legacy semantics and all three contract cases.

The next gated storage candidate creates one linked preparation task with a real one-off routine/occurrence, instructions, meal priority and assignment. Current membership is locked before receipt replay; new writes require exact meal-week baselines, current source identity and absence of any prior linked preparation. It reuses audited routine insertion, adds no notification consent or groceries, and keeps meal content/revision unchanged. Receipt failure rolls back routine, occurrence, activity and link. Completed preparation and old replay receipts survive meal removal.

Local storage evidence: nine new PostgreSQL cases pass, including concurrent identical/different requests, ten create/remove races, actual completion/removal replay, exact rollback, private receipt RLS, stale/foreign/removed inputs, assignment denial, Unicode boundaries, anonymous/direct-write denial and unsupported snapshot rejection. Eight existing removal regressions also passed. Two initial test expectations were corrected: synthetic legacy activity FKs must be cleared to simulate membership revocation, and a removal that wins the race correctly creates no task. Scoped lint/format/diff checks pass; disposable Supabase security advisors report no issues. SQL functions are at most 53 code lines. Exact-head review and CI are pending. Preparation API/native/AI/read/edit flows remain unfinished; no device or production execution occurred.

Preparation storage `327f370` is merged after successful CI `35555304112` and clean Sol medium signoff. Sol independently passed all seventeen preparation/removal database cases.

The preparation API slice adds authenticated POST `/v1/meals/preparation/create` and GET `/v1/meals/preparation` at an exact meal-week baseline. Strict creation validates canonical actor/household/operation/entry, unchanged week revision and explicit due date. The read uses a stable invoker RPC under existing RLS, distinguishes no task from an absent meal, retains completed task details and preserves nullable/longer legacy instructions and exact routine microseconds. Duplicate/unknown query parameters are rejected. The static HTTP method table moved out of `createHandler` to keep the function within its configured length limit; no route behavior changed.

Verification: all 189 selected API and real meal HTTP/PostgREST cases pass, including five new API validation cases and two preparation HTTP journeys. These cover 4,000-character multibyte creation, receipt loss/replay after a later title/instruction edit, full legacy astral-text reads, partner/foreign/revoked access, stale baselines, completion and removed-meal absence. API types, scoped lint/format/diff and isolated security advisors pass. Exact-head CI and Sol review remain pending. Native preparation controls and corresponding AI actions are next; preparation edit/instructions support and device acceptance remain unfinished. No deployment, production data or live provider was used.

Preparation API/read `5a16324` is merged after successful exact-head CI `35555770319` and clean Sol medium signoff. Sol independently passed twenty-two focused API/method/HTTP checks and confirmed stable invoker/RLS reads and unchanged existing routes. Main was fast-forwarded and pushed.

Native preparation clients and runtime are now implemented on the feature branch. The clients bind full create/read identities, exact revisions and due dates; the runtime reads the current week, exact preparation and authorized routine roster before enabling creation. It freezes a deep copy of uncertain instructions/assignment, blocks duplicate existing-task creation, latches acknowledgments privately through account verification and rejects post-acknowledgment stale/missing task details. This increment adds no offline writes.

Six focused native transport/runtime cases pass, including wrong-scope absent reads, switched credentials, mutable draft retry, authorization recovery, older routine versions and canceled publication. Mobile types and scoped lint/format pass. The runtime uses the existing bounded routine-list read for the household roster. Native form/detail controls, owner/navigation lifecycle and real native-to-HTTP journeys are still unfinished; this increment is not a completed native slice and has no device evidence. Full-branch Sol review and CI verification remain outstanding before merge.

Native preparation now has a protected board-linked screen, authorized task detail and native title/instruction/date/responsibility form. Drafts survive ordinary reloads, uncertain requests are read-only until exact retry, and leaving unsaved or unresolved work warns before discarding retry details. Account denial clears scoped details. Existing and completed preparation cannot be recreated. The owner lifecycle freezes its target, cancels on last unsubscribe and recreates safely on resubscription. Date-only tasks do not invent a timed calendar overlap verdict.

Local verification: nine native transport/runtime/owner/draft cases and all four actual preparation HTTP/PostgREST/PostgreSQL journeys pass. Two new native-runtime journeys cover committed response loss followed by partner completion, immutable replay, no duplicate task, revocation, stale meal edits, explicit reload and removal. Mobile types and scoped lint pass with advisory Effect suggestions. Native form gestures, keyboard, navigation alert, VoiceOver and physical-device acceptance remain unverified. Creation/read AI tools and preparation editing remain next. Exact-head CI and Sol medium review are still required before this branch merges.

Sol found that preparation unnecessarily depended on the bounded full routine list: more than 200 active routines prevented even viewing a valid linked task. The fix adds an authenticated roster-only endpoint/client using the existing strict household-member reader and moves preparation to that bounded two-member read. Fifteen focused checks now pass, including a real fixture with 201 active routines that rejects the full list while preparation creation and partner detail reading still succeed. Foreign household, missing actor, duplicate/extra members and wrong method checks pass. API/mobile types and scoped lint pass. Updated exact-head CI and Sol rereview are required. The earlier iOS export passed at `/tmp/nest-native-preparation-export` (bundle `5d321efcb97f17cf52372ba9edcf8338`); it predates the roster fix and remains packaging-only evidence.

Native preparation `1f8f4ed` is merged and pushed to main after clean exact-head Sol medium rereview and successful CI `35556925982`. The review finding was fixed before merge; Sol independently verified native modules and all five real HTTP journeys, including the 201-routine regression. No physical-device verification is claimed.

Private assistant preparation creation and reading now share the native strict contracts and authorized storage/read services. Creation enters the atomic private journal with actor-owned identity and immutable recovery; tool guidance requires explicit intent, exact current meal/preparation reads, a clear due date and current member IDs. It creates date-only household work, never money, permission or reminder consent. Result cards open current preparation; uncertain results request reconciliation. New finite meal validation/dispatch helpers keep SQL functions below 80 code lines while preserving prior actions and canonical history.

Verification: 210 affected API/schema/native-card/database/SDK-PostgREST checks pass, including four new journal cases, strict receipt binding and an actual SDK committed-response-loss journey followed by partner completion. Full journal failure rolls back routine, occurrence, activity and native receipt. Forged transcript parts, partner private-journal recovery, malformed assignment, stale baselines and revocation are covered. The first broad run exposed a missing `service_role` in an older synthetic fixture; adding that standard role fixed the fixture and all selected regressions now pass. API/mobile/SDK types, scoped lint and disposable Supabase security advisors pass. No live model, phone, deployment or production migration was used. Exact-head CI and Sol review remain pending; preparation editing and the remaining M5 proposal/ingredient approval scope are still incomplete.

Sol's assistant review found that preparation guidance still used the bounded full routine list to identify assignees, recreating the 201-routine limitation for explicit AI assignments. The fix exposes `readHouseholdRoster` through the same authenticated roster-only service and points preparation guidance to it. Both preparation SDK/PostgREST journeys pass, including 201 valid routines, successful assignment to the returned partner ID, foreign-household denial and revoked roster access. API/SDK types and scoped lint pass. Updated exact-head CI and clean Sol rereview remain required.

Assistant preparation `c43b2f4` is merged and pushed to main after successful exact-head CI `35557511734` and clean Sol medium rereview. Sol independently passed all nine focused checks, including bounded roster assignment at 201 routines. No live-provider or physical-device execution is claimed.

The preparation editing branch adds strict partial-update contracts and authenticated POST `/v1/meals/preparation/edit`, backed by a gated atomic command. Edits bind both exact week revision and routine microsecond version, preserve the linked occurrence, distinguish omitted instructions from explicit null, and use the audited legacy CAS engine. Finished preparation accepts text corrections while retaining date/assignee/history. Actor-bound receipts replay unchanged after later edits or removal; new edits to stale/removed targets conflict. The command locks the meal and linked occurrence before the routine, rechecks current membership before receipt recovery, and maps contention to conflict.

Verification: all 42 selected preparation/routine-edit/API/HTTP checks pass, including eight new database cases, sixteen edit-versus-completion/removal races, 1,200 generated exact-version receipt checks and a real multibyte edit/lost-response/partner-completion HTTP journey. Injected receipt failure rolls back the task, occurrence, legacy receipt, notices and activity. Foreign targets, private receipts, anonymous/direct writes, unsupported isolation and revocation are covered. The HTTP revocation fixture initially hit a legacy inbox foreign key; fixture-only notice cleanup corrected that test setup. API types, scoped lint and isolated Supabase security advisors pass; new SQL functions have at most 52 code lines. Exact-head CI and Sol review remain pending. Native editing controls, corresponding assistant edits, meaningful availability guidance and device verification are still unfinished.

Sol's edit review found that the receipt codec accepted an unchanged routine timestamp, although the audited update trigger always advances every edit by at least one microsecond. The invariant now requires a strictly newer version, and generated contract/API tests explicitly reject equality as well as backwards values. Updated exact-head CI and clean rereview remain required.

Preparation editing storage/API `6883279` is merged and pushed to main after successful exact-head CI `35558090849` and clean Sol medium rereview. The timestamp invariant finding was fixed before merge; Sol independently verified the affected five cases and retained its prior 42-case database/API/HTTP review evidence.

Native preparation editing now has strict transport/receipt validation, an account-owned retry runtime and protected editor linked from preparation detail. The form sends only changed fields, keeps omitted legacy title/instructions intact, supports explicit clearing, disables finished-task scheduling/responsibility and rejects archived editing. Existing legacy-length text can remain visible without coercing it into a new-input limit. Conflicts require explicit reload; reload confirms draft discard, leaving warns for unsaved/uncertain work, exact retries keep the original patch and both baselines, and confirmed edits stay latched through account recovery. Successful reload creates a fresh form baseline.

Local verification: nineteen selected native preparation and actual HTTP checks pass, including eight new client/runtime/draft/owner cases and a native client/runtime → HTTP/PostgREST/PostgreSQL lost-acknowledgment journey followed by a partner correction. It reads the current corrected task while retaining the original receipt, prevents resend and clears revoked scoped data. Mobile types and scoped lint pass. iOS export at `/tmp/nest-native-preparation-edit-export` (bundle `b8231ec4e10738e80821e3fd7c8d9ff6`) passed before the final legacy-input display-limit adjustment; this is packaging evidence only. Exact-head CI and Sol review remain pending. Native keyboard, navigation prompts, finished-task controls and accessibility are unverified on a physical iPhone. Matching assistant edits and meaningful availability guidance remain outstanding.

Native preparation editor `db075c2` is merged and pushed to main after successful exact-head CI `35558461168` and clean GPT-5.6 Sol medium signoff. Sol independently passed all nineteen focused native/HTTP cases. Physical-device verification remains outstanding.

Private assistant preparation editing now shares the strict partial-update contract and native atomic transaction through the actor-owned journal. Exact meal-week and routine-version baselines prevent stale edits; omitted instructions remain intact and explicit null clears them. Tool guidance requires requested changes, current preparation and roster reads, preserves finished-task history and makes no financial, reminder or timed-availability claims. Canonical transcript output and native result cards reflect saved receipts rather than model assertions.

Local verification: all 218 selected AI database/API/schema/native-card/SDK-PostgREST cases pass, including four new journal cases and a real committed-response-loss → partner correction → immutable original retry journey. Coverage includes full native/legacy rollback on journal failure, private actor recovery, strict nested patches, finished scheduling conflicts, malformed receipts, current partner reads and revocation. The initial new fixture omitted the native routine-edit helper migration; adding that test prerequisite resolved the fixture failure. API/mobile/SDK types pass; scoped lint/format and disposable Supabase security advisors pass. A receipt matcher was factored to share meal-baseline validation within the configured complexity limit. Exact-head CI and Sol review remain pending. No live provider, native device or production execution occurred; M5 proposal/ingredient approvals and meaningful availability guidance remain incomplete.

Assistant preparation editing `1475bcf` is merged and pushed to main after successful exact-head CI `35559038642` and clean Sol medium signoff. The reviewer independently passed all eight new focused cases, including the real SDK/PostgREST journey. No PR was required under the owner's standing branch-review workflow.

Preparation creation and due-date changes now reuse the existing on-device Zurich-day calendar read before saving. Busy and unknown results offer an explicit save-anyway choice; text-only edits skip the calendar check. The warning identifies its scope as the current phone's selected calendars and does not infer partner availability or a precise timed conflict. Forms stay locked while a warning is open; dismissal, navigation, account-scope replacement or abort prevents a stale save callback. Current loaded/ready consent and selection must agree, and changes during the read reduce the result to unknown. No permission prompt, event upload, new consent or calendar write is introduced. Assistant preparation guidance uses the existing authorized fresh busy-interval tool for the due date and preserves nonblocking warnings.

Local verification: twenty-five calendar/preparation cases pass, including three new warning/consent cases, cancellation/late acceptance, 1,000 generated Zurich day boundaries, DST and existing native preparation retry tests. API/mobile typechecking, scoped lint/format and iOS export pass (`/tmp/nest-preparation-calendar-export`, bundle `4b4ca4de768835a2a9ef75be935cc99e`; export predates the final controller-reset guard). Native alert gestures, EventKit permission behavior, keyboard/accessibility and end-to-end phone execution remain unverified. Exact-head CI and Sol review are pending. No live provider or production operation occurred; remaining M5 work includes structured proposal approval and separate ingredient review.

Preparation calendar guidance `bccec8d` is merged and pushed to main after successful exact-head CI `35559484906` and clean Sol medium signoff. Sol independently verified twenty-five cases. Physical EventKit and native alert interaction remain unverified.

The next M5 boundary is a server-only planning-context projection; see the [privacy boundary](native-rewrite/meal-planning-boundary.md). It reads both members' dietary constraints/portions and shared cooking choices while including only the requesting member's calorie goal. Missing setup stays null. Invoker RPCs are denied to anonymous/authenticated clients and require the trusted server role plus current actor membership in a two-member household. The Effect reader independently authenticates the bearer and household before sending a separate redacted secret to its fixed endpoint, binds the decoded response and sanitizes failures. No public API route, chat tool or client export exposes raw context. A timezone-stable fingerprint covers profile changes and membership incarnation for later approval checks; proposal approval enforcement itself is not yet implemented.

Thirty focused database/API/real HTTP checks pass, including four new projection cases, two credential/validation cases and one actual internal-reader/PostgREST journey. Client RPC denial, private calorie filtering, incomplete setup, revoked/foreign membership, four database timezones, strict upstream scope and safe failures are covered. Existing food/cooking RLS and five preparation HTTP journeys still pass. API types, scoped lint and disposable security advisors pass. The fixture models only Supabase's secret-key gateway translation; PostgreSQL/PostgREST and the application reader are real. No hosted key or model was used. Exact-head CI and Sol review are pending. Structured generation, private proposal lifecycle/approval and ingredient confirmation remain incomplete; a server-only development secret and approved Gateway configuration will be needed for live integration.

Private meal-planning context `e3c54f5` is merged and pushed to main after successful exact-head CI `35560196750` and clean Sol medium signoff. Sol independently passed all seven new focused database/API/PostgREST cases. Hosted secret-key and live-provider integration remain unverified.

The proposal contract increment defines private generation/ready/failure/approved/discarded states, recipe-bearing dated slots, retained saved-recipe provenance, familiar-only enforcement and optional per-serving calorie estimates. Inputs separate generation, single-slot replacement, saved-meal choice, discard and explicit native approval. Actor/household, hidden content and approval injection are rejected by strict codecs. Approval receipts require distinct entries/slots and exact bigint proposal/week revision deltas. Public proposal envelopes omit raw dietary context and fingerprints.

Five focused contract/SDK cases pass, including 1,000 generated week/slot/bigint-revision cases and the installed AI SDK schema adapter rejecting injected approvals and identity. Contracts/API types, scoped lint/format and diff checks pass. This is a schema increment only: proposal storage, bounded structured generation, constraint validation, native preview/approval and separate ingredient review are still unfinished. Generated recipe retention also needs an explicit extension of the current saved-recipe snapshot representation. Exact-head CI and Sol review remain pending; no live model, device or production execution occurred.

Latest reviewed deliveries, all fast-forwarded and pushed to main after exact-head CI and clean GPT-5.6 Sol medium review:

- `ecd3520` — atomic recipe creation storage; CI `35544477532`, 18 independent database checks.
- `7813db7` — authenticated recipe creation API and rejected-upload transport fix; CI `35544909156`, 14 independent checks plus blocked-first-chunk socket probe.
- `9846371` — native recipe creation; CI `35545493560`, 17 independent checks including four actual native-client/runtime → HTTP/PostgREST/PostgreSQL journeys. iOS export compiled 1,927 modules (bundle `e0a0eb74e6a9aa0aae4c6f772ec635c4`); packaging only.
- `7d5ca65` — private AI recipe creation; CI `35546048393`, 161 affected local checks and 40 independently selected review checks. Atomic journal replay/rollback, exact UTF-8 handoff bounds, canonical history and tenant/actor isolation verified against disposable fixtures. No live provider was used.

Recipe archive storage and authenticated POST `/v1/meals/recipe/archive` are merged as `ee77f30` after successful CI `35546520129` and clean exact-head Sol medium review. All 11 focused checks passed, including database contention/rollback, strict API receipts and actual HTTP/PostgREST journeys. Archiving hides the library definition while retaining ingredients, existing planned meals/history and groceries. Lost acknowledgments replay their original receipt without undoing a later restoration. Contract/API types, lint/format and disposable security advisors passed.

Native archive confirmation is merged as `cc1559c` after successful exact-head CI `35547194145` and clean Sol medium review. All 12 focused local and independent checks pass, including two native runtime/client → HTTP/PostgREST/PostgreSQL journeys and recipe-creation regressions. Mobile types and iOS export pass (1,933 modules, bundle `47758c9a0083443e4108ef07ce664cf7`); export is packaging evidence only.

Private AI recipe archive is merged as `198fd54` after successful exact-head CI `35547640689` and clean Sol medium review. All 167 local and independently repeated affected checks pass, including private journal rollback/replay, forged transcript removal, revocation/tenant isolation and two real SDK/PostgREST journeys. No live provider or device execution was performed.

Recipe-edit storage and authenticated POST `/v1/meals/recipe/edit` are merged as `dd648a7` after successful exact-head CI `35548373121` and clean Sol medium review. All 34 local and independently repeated focused checks pass: ten edit database cases, sixteen creation/archive regressions, three edit API cases and five actual HTTP/PostgREST journeys. Contract/API types, scoped lint and isolated security advisors passed. Explicit patches preserve omitted legacy metadata; ingredient selection retains IDs, archives removals, preserves distinct quantities/units and never rewrites planned/grocery history.

Native recipe editing is merged as `e18af0a` after successful exact-head CI `35549142431` and clean Sol medium review. The protected current-recipe editor uses native text fields and a virtualized ingredient list, permits explicit add/edit/remove/reorder, preserves unknown metadata and unchanged longer legacy fields, and builds minimal shared patches. Its account/target-scoped runtime deeply retains uncertain attempts, keeps a read-only draft after conflicts until explicit discard/reload, advances the draft generation only after successful reads, hides data on authorization loss and never resends acknowledged edits after refresh failure.

All 20 focused local checks pass, including eight new draft/client/runtime cases, ten creation/archive regressions and two native draft/runtime/client → HTTP/PostgREST/PostgreSQL journeys. Mobile types and scoped lint pass. The iOS export compiled 1,944 modules (bundle `9c4bfa59c78ed8e1b7d9a13d0655f9bb`); this is packaging evidence only. All 20 checks were independently repeated by Sol. Native keyboard, navigation and accessibility execution remain unverified. The corresponding private AI edit action, snapshot-backed recipe selection, leftovers, proposals/approval and ingredient review are still outstanding M5 work. No device or production execution occurred.

Private AI recipe editing is merged as `4245d3b` after successful exact-head CI `35549824753` and clean Sol medium review (179 independently selected checks). It shares the native strict patch/ingredient-selection contract and transaction, binds receipts to actor/household/definition/prior revision, and records results in the private journal atomically. Omitted metadata and ingredient fields stay omitted; UUID normalization touches supplied identities only. Large UTF-8 inputs hand off to the native library editor before dispatch. Canonical history strips invented edit claims, and uncertainty stops later writes.

Local verification: 157 affected AI/database/API/SDK/native-card/actual PostgREST checks pass after correcting fixture setup and column assertions; the final eight edit database cases were rerun successfully. Coverage includes concurrent replay, later edit/archive, no-op revision, full rollback, private/revoked recovery, foreign ingredient/category conflicts, canonical identities, fixed input/per-turn budgets and native handoff. API/mobile types, scoped lint/format and isolated security advisors pass. CI and exact-head review passed for the AI edit delivery. No live model, physical device or production execution occurred. Snapshot-backed recipe selection, leftovers, proposals/approval and separate grocery ingredient review remain incomplete.

Saved-recipe selection storage is merged as `84c359f` after successful exact-head CI `35550696809` and clean Sol medium rereview. New placement/replacement commands check exact library and week revisions, capture the complete authorized recipe and ordered ingredients, preserve nullable legacy metadata, and retain actor-bound retry receipts. Replacement uses the existing removal/preparation engine in the same transaction. A stable planned-detail read returns retained snapshots or explicit unavailable historical detail; it never substitutes current library ingredients. Captured entry content is guarded against legacy rewrites while move/remove behavior remains available. No groceries are created.

Local storage verification: 24 database cases pass (11 new snapshot/selection cases plus 13 replacement/removal regressions), covering a generated 21-slot week with distinct recipe revisions, immutable replay after edit/archive/remove, actual preparation rollback, tenant/RLS/revocation, strict inputs, source-row lock conflicts, 200-ingredient/legacy-Unicode bounds and overflow. Contracts typecheck, scoped lint/format and isolated security advisors pass. Sol independently passed 28 selected database checks and found one extra EOF blank line in the fixture; it is removed. Updated-commit CI and rereview passed. Native selection/detail caching and private AI selection remain unfinished; no device or production execution occurred.

Authenticated recipe selection and planned-detail HTTP endpoints are merged as `0a4845f` after successful exact-head CI `35550999033` and clean Sol medium review: POST `/v1/meals/recipe/place`, POST `/v1/meals/recipe/replace`, and GET `/v1/meals/planned-recipe`. Selection validates canonical operation/definition/source identities, actor/household, exact library baseline and one/two-step bigint week revision. Planned reads bind household, requested entry, week and exact revision and validate retained recipe coherence. Unknown/duplicate read parameters and injected command content are rejected; all responses remain private/no-store.

Local API verification: 27 focused checks pass — four receipt/input/read validation cases, eleven storage cases, two new real HTTP/PostgREST selection/replacement journeys and ten existing one-off HTTP route regressions. Tests prove lost-response replay after library archive, historical detail preservation, partner reads, foreign/revoked denial and no new groceries. API types, scoped lint/format and diff checks pass. Sol independently passed 16 API/HTTP checks; exact-head CI passed. Native UI/cache and shared AI selection tools remain incomplete; no device/provider/production execution occurred.

Native recipe-selection work is underway on `codex/native-recipe-selection`. Strict native placement/replacement/planned-detail clients and session transport adapters are implemented. A lease-scoped SQLite planned-detail cache retains 32 recent entries per account, separates week identities, preserves newer absent/removal results against older replies and snapshots caller-owned inputs before asynchronous writes. Eleven actual client/SQLite checks pass (two native receipt/input cases, five new cache cases and four existing week-cache regressions). Mobile types and scoped lint pass for the implementation so far. The following increment adds planned-detail runtime/UI and read-client tests; recipe-selection UI/runtime and actual HTTP/native flows remain unfinished. This is not a completed native slice. No device evidence or production execution occurred.

The native planned-detail screen and runtime now connect the meal board to retained recipes, with a virtualized ingredient list, separately labeled current-library navigation and explicit unknown historical ingredients for older/one-off entries. Refresh first reads the current week revision, then requests exact planned detail. Saved content stays visibly stale until refreshed; newer cached removals cannot be overwritten, authorization/lease denial hides data across later failures, and canceled/disposed reads cannot publish or save late results. Owners recreate runtimes after subscription teardown.

Thirteen focused native client/SQLite/runtime checks pass, including five new lifecycle cases and exact planned-read transport/binding validation. Mobile typechecking and scoped lint/format pass. An iOS export compiled 1,954 modules (`910d34206850f6e3b9ddef97ee375c49`); this is packaging evidence only. This branch is not yet independently reviewed or merged. Recipe-selection confirmation UI/runtime, actual native-to-HTTP journeys and AI selection remain unfinished. Physical navigation, accessibility and offline-device behavior remain unverified.

Native recipe selection now has a paginated saved-recipe picker, full ingredient/instruction preview, explicit online placement/replacement confirmation, destination validation and account-scoped runtime ownership. Add/replace meal forms link into selection. Conflict recovery requires explicit reload and reselection; uncertain commands retain the exact operation and baselines, while confirmed commands cannot resend after refresh failure or authorization loss. Replacement explains retained history/groceries and skipped open preparation. Navigation warns before discarding selected or uncertain work.

Verification: 12 focused native runtime/client checks pass (four new selection cases, five planned-detail lifecycle cases and three transport cases). Four real HTTP/PostgREST tests pass, including two new native runtime/client journeys with PostgreSQL and SQLite: lost acknowledgment after archive, immutable ingredients, replacement history, unchanged groceries and authorization revocation. Mobile typechecking and scoped lint/format pass; iOS export compiled 1,960 modules (`706c6a9543c9b1d8e6673ab53430b223`). Export is packaging only. This increment is awaiting exact-commit CI and independent Sol medium review; no physical native interaction is verified. Corresponding private AI recipe selection remains outstanding before this action is a full vertical slice.

Native selection and planned details are now merged as `8825abf` after successful exact-head CI `35552324020` and clean GPT-5.6 Sol medium signoff. Sol inspected all 30 changed files and independently passed the four focused native test files and four HTTP/PostgREST journeys. Main was fast-forwarded and pushed before the AI branch delivery. This establishes reviewed implementation and fixture verification, not physical-device acceptance.

Private AI recipe selection is underway on `codex/ai-recipe-selection`: shared placement/replacement input and receipt codecs, actor/household/recipe/source/week/library revision binding, explicit-request-only tool guidance, canonical native planned-detail links, and an additive transactional journal dispatcher migration are implemented. The journal generates operation IDs; model inputs cannot supply identity or recipe content. Canonical transcript handling recognizes both new mutations. Existing isolated journal fixtures load the updated dispatcher.

Seventeen focused API/database checks pass: one exhaustive selection receipt/input binding case, three new journal concurrency/replay/rollback/authorization cases, eight edit regressions and five one-off replacement regressions. The first new fixture attempt lacked the native placement validator; it was corrected to compose audited selection storage with the private journal and rerun successfully. Contract/SDK/API/mobile types and scoped lint pass. Supabase security advisors report no issues on the disposable full selection/journal fixture. Still outstanding before review: planned-detail AI reads, actual SDK→HTTP/PostgREST selection journeys, canonical transcript/strict-input/terminal-conflict tests and native result-card checks. This AI increment is not independently reviewed or merged; no live provider/device/production execution occurred.

AI recipe selection now also exposes an authorized `readPlannedRecipe` tool, preserving historical ingredients and explicit absent/unknown states at the exact week revision. Native confirmed result cards open retained planned details, while invalid/uncertain results return to the week for reconciliation. Two actual SDK→HTTP/PostgREST journeys verify exact lost-journal-response replay after library archive, captured ingredient reads, partner-read versus private-command authorization, revocation, replacement history and per-turn stop-on-conflict behavior. No live model was used.

Expanded journal cases verify forged placement/replacement transcript claims are stripped, actor/operation/content injection is rejected before journaling, and a recorded stale-recipe conflict remains terminal after restoration. The affected API/journal/SDK/native-card run passed 204 checks; the one new schema test initially failed to import because a workspace alias resolved to an old worktree. Its import now points explicitly to this checkout, and it plus the three native recipe-card checks pass on rerun. Thus all 205 selected cases have passing evidence. API/mobile/SDK typechecking and scoped lint/format pass. Awaiting exact-commit CI and Sol medium review; no physical device, production or live-provider verification is claimed.

Private AI selection and planned-detail reads are merged as `1be8944` after successful CI `35553024350` and clean exact-head GPT-5.6 Sol medium review. Sol independently passed the focused API/schema/card files, all eight new database/SDK-PostgREST cases and nineteen prior dispatcher regressions. Main was fast-forwarded and pushed. Manual saved-recipe selection now spans storage, API, native UI and private AI, with fixture evidence but no device/live-model acceptance.

Leftover storage is implemented on `codex/meal-leftovers-storage`. Deliberate audit of legacy `20260811180000_meals_groceries.sql` and `20260905072131_serialize_leftover_source_dates.sql` confirmed active earlier-day source meals, no leftover chains and copying source title/link/notes/definition. The new command preserves those rules, checks exact source and destination week baselines, serializes with source changes/removal, retains actor-scoped replay receipts and copies only an existing retained recipe snapshot. It never substitutes the current library, creates groceries or inherits preparation. A different source week is unchanged; same-week placement increments the common revision once. Legacy one-off ingredients remain explicitly unknown.

Fourteen focused database checks pass (eight new leftover cases and six snapshot-selection regressions): concurrent replay, later library archive, cross-week counters, source type/date/tenant/revocation, full receipt-failure rollback, legacy unknown detail, generated eighteen later-day slots and source-removal/placement races. An initial removal assertion attempted a forbidden direct client table update; it now exercises the actual authorized removal command and passes. Contract types, scoped lint/format and disposable security advisors pass. Leftover storage awaits exact-commit CI and Sol review; corresponding API/native/AI flows are not implemented yet. No production or device execution occurred.

Sol's leftover-storage review reproduced an extra source restriction: the initial command rejected retained Monday idea entries with no slot, although the audited legacy command permits them as earlier-day sources. The restriction is removed without adding an ideas UI, and an explicit legacy-source regression is added. Storage rereview and updated-commit CI are required before merge. API work continues independently on its own branch.

The legacy-source review fix is committed as `cbcaeca` on the storage branch, with all nine leftover storage tests passing. Exact-head rereview and CI are pending; the dependent API branch includes that correction.

Authenticated POST `/v1/meals/leftovers` is implemented. It accepts the shared strict source/destination contract, normalizes source/operation identities and binds the receipt to the current actor/household, source ID, both weeks, target date/slot and exact bigint counters. Cross-week placement preserves the source counter; same-week placement increments the common counter once. Small meal commands now use an explicit route map, with unknown paths rejected rather than falling through to placement.

API verification: four new input/receipt/error cases and three move regressions pass; five actual HTTP/PostgREST journeys pass (one new leftovers case plus four recipe-selection regressions). After the storage correction, all five new API/HTTP cases were rerun successfully. The new HTTP case proves committed-response loss/replay, original source ingredients after library archive, no new groceries, injected-content rejection, stale/foreign denial and revoked receipt recovery. API types and scoped lint/format pass. No native leftover UI/client or private AI leftover action yet; API delivery awaits review/CI, and storage has not been merged while its updated gates remain pending.

The owner-authorized delivery route uses reviewed `codex/` branches without requiring PRs: clean exact-head Sol medium signoff and successful CI precede local fast-forward and push main. Greptile exhausted its trial/credit cap. No merge implies deployment or migration permission. The unwanted continuation automation remains removed; no duplicate has been created.

## Milestone checklist

- [ ] **M0 — Decisions and execution route.** Tooling, delivery ADR, action inventory, exact dependency pins and linked EAS app identity implemented. CI enforces formatting, lint, typechecking and focused tests. Native smoke procedure exists but has not run. Exit still needs an executable iPhone development build and recorded native evidence.
- [ ] **M1 — Quiet native interactions.** Four-tab development preview, generated artwork, sample chore/checklist interactions, meal replacement and calendar toggles implemented. Every preview uses identified fictional data; release guards prevent presenting it as working data. Owner review, large text, VoiceOver, Reduce Motion and real keyboard/navigation checks remain.
- [ ] **M2 — Authenticated offline/AI slice.** Bearer membership API, chore read/completion HTTP and shared AI tools, transactional receipts, SQLite journal, Effect/AI SDK boundary, durable approval records and owner-private transcript persistence implemented. Native Today → outbox → API integration is merged in PR #15. Cold-start identity recovery is merged in PR #23 and locally verified. Private native chat and SDK/server lifecycle are merged in #25–#27; atomic household command journaling is merged in #28. Mutating assistant tools and canonical native receipt links are merged in #29. Live provider/structured generation, approval-resume integration and an actual native retry/restart journey remain.
- [ ] **M3 — Identity, onboarding and settings.** Native Apple sign-in, SecureStore session adapter, refresh/membership verification and retryable logout merged in PR #14. Private food-profile storage and API are merged in #30–#31; the native form and exact save recovery are merged in #32. Journaled AI food settings are merged in #33; shared cooking storage/API is merged in #34; the native cooking form is merged in #35 and corresponding journaled AI actions are merged in #36. Private memory storage/API/native management/AI actions are merged in #37–#40. Calendar consent/API/native sharing/AI availability are merged in #41–#43. Notification preference storage/API/native controls are merged in #44–#45; corresponding AI actions are merged in #46. Independent quick-start/comprehensive setup and assistant handoff are merged in #47. Progressive prompts at real feature entry, notification OS permission/enrollment and actual onboarding device acceptance remain. Existing-member Apple linkage and Keychain behavior need device verification.
- [ ] **M4 — Today, chores and groceries.** Real authenticated Today chores, online routine creation/edit/pause/resume/archive/skip/reschedule and corresponding journaled AI actions are merged. Native handover request/recipient acceptance/decline, one-statement coherent reads and readonly offline cache are merged through #71; corresponding private AI handover tools are merged in #72. Groceries support online add/edit/remove, inline add, checked collapse and offline checks. Account-level reconnect and cold-start identity recovery are merged. Grocery grouping is merged in #74; queue/receipt retention bounds are merged in #73. Full Today aggregation as later slices land, and actual two-device fault/acceptance journeys remain. Node SQLite and real PostgreSQL/HTTP evidence do not establish device execution.
- [ ] **M5 — Meals and week planning.** Week board, manual placement/move/replacement/removal, saved-recipe CRUD/selection, leftovers and linked preparation have storage, authenticated API, native UI and private AI implementations. Generated private proposals and explicit exact-revision approval are merged across storage/API/native. Single-suggestion replacement and exact saved-choice generation, durable edits and API are merged; native controls are merged after CI and independent review. Assistant proposal actions and native preview handoff are merged after CI and independent review; separate ingredient review, pantry exclusion and idempotent grocery addition are also merged with API/native/AI coverage. Real provider compatibility/dietary suitability and the full seven-day journey on an iPhone remain unverified.
- [ ] **M6 — Calendar and availability.** Read-only Expo adapter and consent-bound sanitized server snapshots implemented separately. Consent incarnation invalidates delayed requests across membership removal/rejoin. Authorized API, native selection/consent/publisher and assistant availability reads are merged in #41–#43. Local selections and pending consent are account-leased; foreground/reconnect publishing uses a 31-day horizon and 15-minute expiry. Authorized busy snapshots now feed proposal generation and preparation overlap warnings. Personal agenda and partner busy UI are merged with local/controller/HTTP verification. The optional native chore layer is merged after CI and clean review; renewals await M8. Production Calendar route integration, optional background refresh and two-device privacy/permission inspection remain.
- [ ] **M7 — Money and recurring automation.** Pure integer-CHF rules and durable exact-payload approval boundary implemented. Append-only ledger services/native UI/tools, expense/settlement/correction workflows, grocery expense handoff, approved recurring rules/scheduler and exact migration reconciliation remain.
- [ ] **M8 — Renewals, reminders and push.** Owner-specific daily-summary/time and item-reminder preferences have storage/API/native controls (#44–#45); corresponding assistant actions are merged in #46. Renewal/reminder services, permission enrollment, scheduler/outbox, token lifecycle and physical APNs delivery remain.
- [ ] **M9 — Migration and release rehearsal.** Audited tenancy prerequisites and isolated targeted migration fixtures implemented. Full legacy migration-chain rehearsal, representative data mappings/reconciliation, old-writer cutover/rollback, both-member acceptance and release-mode native verification remain. Production migration/cutover and TestFlight/App Store publication require separate owner approval.

## Historical verification evidence

| Boundary              | Implemented and locally verified                                                                                                                                                                                             | CI / native limits                                                                                       |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| Toolchain             | TypeScript 7 + @effect/tsgo, Effect v4, Oxlint/Oxfmt; six lint-contract cases enforce 400 lines/file, 80 code lines/function, complexity 10 and Effect diagnostics                                                           | Merged foundation CI passes. No native execution inferred                                                |
| Identity/chore HTTP   | 22 API tests: ten bearer membership, nine chore/tool cases and three bounded/cancellable request-body cases. Revalidates membership per tool invocation; rejects mismatched UUID receipts; excludes paused/archived routines | Merged #13 current-head CI passed. Fixture Auth is not hosted Supabase Auth                              |
| Actual PostgREST      | Disposable PostgreSQL + pinned PostgREST 16.3 test exercises real RLS/RPC, read/completion, replay, revocation, UUID canonicalization and stale-date conflict                                                                | Locally passed; deliberately targeted outside routine CI. Supabase Auth and legacy closure are synthetic |
| Chore receipts        | 12 PostgreSQL cases: authorization, actor-only receipts, atomic rollback, duplicate/partner races, stale dates and revoked membership                                                                                        | Merged #4 CI passed; full legacy recurrence/window engine unverified                                     |
| Grocery checks        | 12 PostgreSQL cases: idempotency, compatible convergence, ABA/version conflict, grants/RLS, legacy claim preservation and rollback                                                                                           | Merged #10/#16–#20 with review + CI; native/API integration locally tested, device execution remains     |
| Action approvals      | 10 PostgreSQL cases: identity, exact payload, denial/expiry, concurrent consume, rollback and baseline guard                                                                                                                 | Merged #9 CI passed. Synthetic financial write only; no ledger/UI approval journey                       |
| Busy sharing          | 15 PostgreSQL cases: consent revocation, freshness, immutable captures, privacy validation, latest capture, membership incarnation and both reproduced delayed-request attacks                                               | Merged #11 with clean independent rereview + CI. No EventKit/server journey                              |
| Private conversations | 10 PostgreSQL cases: owner isolation, revision conflicts, eight simultaneous first saves, immutable replay, receipt rollback, retained history and baseline guard                                                            | Merged #12 with clean independent review + CI. SDK-part validation/stream ownership/reconnect remain     |
| Fixture lifecycle     | Six real-process PostgreSQL cases: failed startup, fallback shutdown, timeouts, idempotent cleanup and SIGINT/SIGTERM                                                                                                        | Included in merged CI. SIGKILL/host failure cannot run cleanup; uncertain shutdown retains directory     |
| CHF domain            | Six tests, including four seeded 1,000-case properties for integer amounts, rounding/allocation and zero-sum histories                                                                                                       | Merged #5 CI passed. No live ledger posting                                                              |
| Offline journal       | 13 file-backed SQLite cases: scoped sessions, pending overlays, immutable attempts, retries, conflict ordering and restart                                                                                                   | Merged #6 CI passed. Node SQLite adapter; Expo SQLite/device/background behavior unverified              |
| Calendar adapter      | 11 focused/property cases including 1,000 generated interval sets, DST/all-day clipping, missing access, free/cancelled events and private-text stripping                                                                    | Merged #7 CI passed. Actual calendars/permissions unverified                                             |
| AI SDK adapter        | Ten tests covering Effect schemas/tool execution, error masking, cancellation, streaming fixture and floating-Effect regression                                                                                              | Merged #8 CI passed. No live model/provider, chat UI or durable generation lifecycle                     |
| Native identity       | Eleven session tests, including actual pinned Supabase SDK with injected storage read/delete failures and successful cleanup retry. Older hydration/auth events cannot restore hidden identity                               | Merged #14 with clean rereview and current-head CI. No actual Apple/Keychain execution                   |

An earlier recorded CI database run passed **158 cases** across independent disposable clusters; subsequent slice verification is recorded below. These are not a complete migration-chain rehearsal. Local PostgreSQL is 18.6, extracted from a verified signed package; CI uses its installed PostgreSQL binaries. Focused Supabase security-advisor checks previously passed for the approval/grocery/busy/conversation fixtures; this is not a production audit.

Native Metro exports succeeded for the preview (1,340 modules) and Apple-session source before the logout fix (1,544 modules). Bundles are packaging evidence only. No Maestro, physical iPhone, EventKit, Keychain, APNs or release binary has been executed. Native verification procedure: [native-verification.md](native-rewrite/native-verification.md).

## Review and PR record

PRs **#1–#51 are merged** into `drrius/nest` through main `5d6ac42`. Implementation merges in this task used passing latest-head CI and resolved conversations; #50 separately updated the Expo app identity. PRs #1–#9 had explicit clean Greptile reviews. Greptile then reported its **50-credit trial cap**. The owner authorized independent adversarial subagent review as the fallback; no credits were purchased and no pending/silent review was treated as approval.

| PR                                            | Scope                                                              | Exact clean fallback review head           |
| --------------------------------------------- | ------------------------------------------------------------------ | ------------------------------------------ |
| [#10](https://github.com/drrius/nest/pull/10) | Grocery check receipts                                             | `46d842d3de3a365bb947c22e8dfe681ef01f9df3` |
| [#11](https://github.com/drrius/nest/pull/11) | Busy sharing; membership-lifetime privacy fix                      | `54048ad3a5fb31bb9838e5b24ffb66dbc782ccb6` |
| [#12](https://github.com/drrius/nest/pull/12) | Private conversations                                              | `58edf49dc02d2ea225543a9bbf9bdf4e31d4922c` |
| [#13](https://github.com/drrius/nest/pull/13) | Authorized chore API/tools; canonical UUIDs and paused-routine fix | `7d95edc1474a246b98cdda1986b8fb19aac98eab` |

PR [#14](https://github.com/drrius/nest/pull/14) merged after independent clean rereview of `33221899f367ebea8b3f729f87b733b120be25bd`, eleven passing session tests, both latest-head CI runs and no unresolved conversations. Failed Keychain logout is explicitly pending/retryable until credential readback confirms removal. Actual device sign-in remains unverified.

**Merged PR #15:** connected native Today chore read/completion, shared API/AI contracts, durable replay, scoped snapshots, conflict recovery and a runnable loopback-only development API. Fifteen focused native transport/SQLite/controller cases, fourteen session cases, 25 API cases, 13 journal regressions and seven domain cases pass locally. A real Node HTTP → API → PostgREST → PostgreSQL test with file-backed SQLite restart proves lost-response retry without duplicate closure, removed-item conflict and revocation denial. Zurich midnight/DST is retained from the audited legacy engine. iOS export passes; no device execution is claimed. Exact head `e164c3858ac7ac32403451844c6c6549ca6b9043` received clean independent rereview; both CI runs passed and all conversations were resolved before squash merge.

Remaining M2/M4 gaps include offline cold-start membership recovery, automatic connectivity retry/retention bounds, native/device fault journeys, real routines/groceries and AI chat/provider streaming. The live route covers chores only and remains separate from the fictional four-tab preview.

Merge automation has no deployment/migration step. The unwanted overnight continuation automation remains removed; no duplicate has been created. Active implementation continues in this task.

## Exact external blockers and required owner/device inputs

1. **iPhone execution:** Linux has no Xcode/xcrun. EAS Simulator reported unavailable. PR #50 linked project `@drrius-dev/nest` (`aca37c4f-1bcd-4e9a-a695-37e794f97933`) with bundle `ch.drrius.nest`. The earlier quota check belonged to the previous project and does not establish availability for this identity. No build was started by this task. Need a signed development build installed on a test iPhone (or an available iOS runner); signing/device enrollment requires the owner's Apple access. No paid build is authorized.
2. **Live identity/backend:** Need an isolated development backend/API and Apple provider setup that preserves the existing two members' identities. Confirm App ID grouping/audience before connecting real accounts. Public configuration is documented in `apps/mobile/.env.example`; no server secrets belong in the app. No production database has been provisioned, read or migrated for these tests.
3. **Live AI:** Need an approved provider/key and budget configuration for the isolated API to verify streaming, structured proposals and approval-resume. No paid request or service purchase has been made for this integration.
4. **Device acceptance:** Eventually both members' iPhones, real selected calendars and permission states are required for privacy, background retry, secure-storage failure recovery, push delivery and usability/accessibility acceptance.
5. **Review service:** Greptile cannot review further while trial credits are exhausted. Independent fallback is authorized and functioning, using **GPT-5.6 Sol with medium reasoning** at the owner’s request; this does not block source progress. CI and clean exact-head review remain mandatory.
6. **Production/release:** Existing financial history and excluded legacy records stay intact. Full fixture reconciliation and a concrete cutover/release plan must precede separate approval. No merge grants permission to migrate, retire the web app or publish.

These gaps do not block independent implementation. Current work implements M5 recipes and planning, then continues through calendar, money, reminder and migration workflows.

## PR #15 review corrections

Independent review found that a later network failure could clear a prior authorization denial, and warm foreground revalidation unmounted the loaded chore screen offline. Denial now stays blocked until successful authorized work. Same-actor warm revalidation retains only the previously verified in-memory identity during transient failures; logout, actor changes and definitive denial clear it. Added direct verifier/subscription and controller regressions. Self-review also added an expected-household header checked against verified API membership plus household-bound response envelopes, preventing a same-actor household change from contaminating another cache. All corrections passed exact-head independent rereview and CI before merge.

Rereview also found transient credential refresh failures incorrectly mapped to session loss. The credential adapter now separates retryable refresh/storage availability from confirmed missing/rejected credentials. Two adapter-to-controller regressions prove warm offline enqueue and sticky confirmed loss; all 15 chore tests pass locally.

## Grocery implementation and review evidence

**Merged #16–#17:** online add/edit/remove RPCs, exact receipts, current membership/version/category checks, shared Effect contracts, authorized API reads/mutations and six corresponding AI SDK tools. All 23 grocery PostgreSQL cases, 33 API cases and real PostgREST journeys passed locally. Fixture security advisors were clean. Exact review heads were `ca247ebe68247bba2e3e1cbafae47a4fc64fa21d` and `113b1a6f34c0b1b2e4446cdb41ffc13165b5bf84`; both current-head CI runs passed before each merge. Lists fail closed above 500 groceries/100 categories rather than silently truncate; pagination remains a release gap. Legacy claimed items require session reconciliation before removal.

**Merged #18:** one account-scoped SQLite owner/lease across native screens. Serialized account changes suppress old opens, wait for cleanup and retain failed-close resources for explicit retry. Six ownership cases plus journal/chore regressions passed. Clean review head `f555f36956fe96070cdcf145a49a479aad537724`, passing CI; native lifecycle still needs device verification.

**Merged #19:** real Today → grocery checklist, native checks, durable queue overlays, restart/exact retries and explicit conflict discard. Review corrected three-toggle ancestry, multiple mounted controllers, and compatible partner acknowledgments incorrectly rebasing a stale opposite intent. Persisted rebase evidence now distinguishes an own applied transition from an unseen compatible version jump; old journals upgrade conservatively. Independent review passed 51 affected tests and both real HTTP/PostgREST/PostgreSQL/SQLite journeys. Clean final head `38984e7be7a767b7812863e6a641a69278ba552c`; both CI runs passed, e2e label applied, no unresolved conversations. iOS export passed; no phone execution.

**Merged #20:** native online grocery add/edit/remove forms, exact account-scoped attempts retained before dispatch, explicit retry after cancellation/lost response/restart, and matching receipt cleanup. They never enter automatic offline checking sync. Eleven focused editor/transport/SQLite cases and a real creation-loss/restart/edit-conflict/removal journey passed. Review fixed removal retry target labels and verified display metadata stays outside the server command; old attempts show an identifier fallback. Keyboard insets enabled, without claiming actual keyboard/gesture/VoiceOver execution. Clean final review head `bf8d391e95c674d1a259334681c1cec94f505a58`; both CI runs passed and all conversations resolved. Export: `/tmp/nest-native-grocery-editor-export`.

**Current `codex/grocery-checklist-controls` branch:** inline online add reuses the retained-attempt controller, exposes previous attempts for review, and preserves separate draft operation identity. Completed checks collapse only after confirmation; pending/conflicted items remain visible. Matching save-operation identity triggers refresh and clears only the submitting inline field; full forms retain one identity for delayed duplicate callbacks. All 63 affected tests pass locally, including the pending/conflict visibility regression; types/lint pass. Export: `/tmp/nest-checklist-controls-export`. Native worklet/input clearing, same-name repeated additions, keyboard and navigation still require execution on a phone; see [grocery device checks](native-rewrite/grocery-device-checks.md). Current-head CI and independent review remain pending. Category grouping, automatic reconnect, cold-start offline identity and retention remain incomplete.

PR #21 merged after clean independent review of `cdfc06442db4cf1fe000c67ba17f6b7a4827a102`, both current-head CI runs and resolved conversations. Reviewer independently passed 10 focused controller/storage/visibility cases and checked installed Expo input/worklet APIs. No native execution is claimed.

**Merged #22:** the account provider owns both shared chore/grocery controllers independent of screen mounts. Pinned `expo-network@57.0.2` prompts authorized sync on foreground/reconnect; delayed initial readings cannot override newer events, duplicate online events are ignored, and disposed/background listeners cannot trigger retries. Manual/foreground retry remains available if native monitoring fails. Retained online grocery edits are never automatically sent. Review found a lost reconnect trigger when an earlier request failed afterward; the loop now honors queued retries after failure. A process-lifetime connectivity hub avoids cancelling the pinned iOS monitor across account subscriptions while dropping old account callbacks. Nine new event/SQLite/sync cases pass; all 72 affected tests pass locally. Types and the initial iOS export pass locally (`/tmp/nest-reconnect-export`). Actual iPhone reachability/background callbacks remain unverified. Exact-head independent rereview signed off on `649b68668c5ce59bf0278d740a9456cc45e709a8`; both CI runs passed, all conversations were resolved, and the source-only squash merge is `9b7de03`. Updated export: `/tmp/nest-reconnect-fixed-export`. Connectivity is only a hint, never proof of API reachability or authorization; see [Expo Network](https://docs.expo.dev/versions/latest/sdk/network/). Cold-start offline identity and queue retention remain separate gaps.

**Merged #23:** protected credentials and last verified household identity share one serialized Keychain record. The adapter preserves existing raw SDK sessions, stores no additional token copy, strips identity on actor replacement and removes both on SDK logout. Cold recovery handles expired-token network failures without interpreting the SDK's null INITIAL_SESSION as confirmed logout. A durable logout marker prevents recovery after interruption and rejects late token writes; explicit sign-in reopens persistence. Live commands still use current SDK credentials and server membership checks. Tests cover real pinned SDK transient/definitive refresh behavior, account/logout races, schema migration and SQLite restart isolation. All 101 affected session/offline/chore/grocery tests pass locally, including 29 session cases. Review caught malformed JSON blocking sign-in/logout recovery; invalid JSON now fails closed and permits explicit replacement. Review also caught the logout marker skipping remote revocation; a pre-logout token is now retained only for the best-effort revocation call, with online and unavailable-network SDK regressions. Self-audit fixed warm cached identity surviving a denial while protected cleanup was pending. Types/lint/format and iOS export (`/tmp/nest-cold-identity-export`) pass. Clean independent rereview of `76dc8e4be197b651b5330949b6a2d618dedc48e1` passed all 29 session tests; both exact-head CI runs passed and all conversations were resolved. Squash merge: `2244f23`. Actual Keychain/device execution remains unverified. A failed Keychain write cannot promise durable logout intent until retry succeeds; the current screen stays hidden and logout is not reported complete. Queue retention remains a separate gap.

**Merged #24:** additive private turn ownership claims each prompt atomically with the saved user message. Retries return the existing turn without another claim; one conversation cannot run competing turns. Finalization binds a server-assigned assistant ID and transcript revision, persists one completed/interrupted result, and preserves immutable replay after later work. A two-minute ownership deadline rejects late completion; expired/interrupted turns never automatically reacquire generation. Existing transcript saves cannot overwrite running history. Sixteen new PostgreSQL cases and ten existing conversation cases pass with the combined migrations; all 92 database cases pass. Supabase security advisors report no issues on the disposable fixture. API/SDK model execution, tool-invocation persistence, approval resume and native chat remain unimplemented; this is storage evidence, not a working AI stream. Independent review signed off on `43bac2d233fb30b9e87131151b304d2373b6e9ae` after independently passing all 26 focused cases. Both exact-head CI runs passed, review conversations were resolved, and squash merge is `da37186`. No production migration executed.

**Current `codex/private-ai-stream` branch:** authenticated private-history/turn reads and SDK streaming now use the durable turn claims. Server-only Gateway configuration has no default model and fails closed when absent. Streaming exposes the real authorized chore/grocery read tools only; mutating tools await invocation journaling. The server loads canonical history after claiming, validates supported SDK parts, bounds context/steps/output/time, masks provider failures and persists completed/interrupted responses. Explicit deadline recovery never starts a model. Five SDK regression cases and four actual HTTP/PostgREST/PostgreSQL/SDK transport journeys pass locally, covering private history, command reads, no duplicate generation, cancellation, abandoned claims and invalid/unconfigured requests. Types/lint pass. CI/review remain pending. Native chat UI, live Gateway credentials/model selection, hosted Auth and device execution, mutation recovery and approval resume are still required; no live-model or native completion is claimed.

PR #25 review corrections: the independent adversarial review found that the pinned SDK's model-level default error callback could log raw provider errors, and optional client household headers allowed tool reads to drift after a membership move. The prepared SDK call now suppresses raw error logging separately from safe SSE errors. Every tool revalidation is bound to the household verified at turn start. Five SDK and five real PostgREST integration cases pass, including log capture and a mid-turn household move with no client scope header. Typecheck/lint pass; updated-head CI and rereview remain required.

PR #25 merged as `e5afc6d` after both exact-head CI runs passed, all review conversations were resolved, and independent rereview explicitly signed off on `54b95c5a59defea6b54279970c56c3506baca8a2`. Greptile reported the trial cap again. The reviewer independently passed all ten SDK/PostgREST cases. Source-only merge; hosted model and native execution remain unverified.

**Current `codex/private-chat-discovery` branch:** owner-private conversation discovery returns bounded pages of metadata without loading transcript content into the list or storing it locally. Stable creation-time/UUID cursors preserve PostgreSQL microseconds and avoid duplicate/shifted pages when new chats arrive or old chats change. Partner/other-household cursor anchors are indistinguishable from missing ones. Three real PostgreSQL/PostgREST cases and five existing stream journeys pass locally; types/lint pass. Malformed upstream and cursor-binding regressions are included in routine CI. Native conversation list/chat transport and device checks remain next; this is API discovery, not a completed chat UI.

PR #26 merged as `e769f56` after clean independent signoff on `845cf701743601c1274a7f7a5e322762ff9abc7f`, both current-head CI runs and no outstanding review conversations. The reviewer independently passed all five discovery cases; Greptile again explicitly reported the trial cap.

**Current `codex/native-private-chat` branch:** authenticated Today now links to native private conversation discovery and chat. Pinned AI SDK React `4.0.109`/AI `7.0.106` uses Expo streaming transport and the inferred agent message type. The SDK owns message state; the application reconciles server revision/turn ownership before another prompt. No local transcript or AI queue is created. Explicit retry retains an unclaimed exact prompt operation; saved/lost responses are reloaded without regeneration, and running claims block sends until saved or explicitly recovered after the deadline. Native Send/Stop/Reload, unsaved-text leave confirmation and links to real chore/grocery screens are implemented. Lists/chat resources release private state on unmount and reacquire after subscription replay. Five client/resource checks, actual HTTP/SDK/PostgREST journeys and five server SDK regressions run locally; 29 session regressions pass. Types/lint/format pass and iOS export succeeds at `/tmp/nest-native-chat-export`. CI and exact-head review are pending. This is a read-only assistant slice: write-tool journaling, approvals/resume, structured meal output, memory consent, live Gateway/provider verification and actual iPhone execution remain incomplete. See [assistant device checks](native-rewrite/assistant-device-checks.md). Expo's installer rejected `workspace:*` for the existing shared type package, so that local workspace link was added with pnpm; external packages were installed through Expo and remain exactly pinned.

PR #27 merged as `fb1cb62` after clean independent adversarial signoff on `227ffc8799be8c4b66cceee13dde2a57fa06f841`, both current-head CI runs and no outstanding conversations. Greptile explicitly reported its trial limit. The reviewer independently passed all 15 focused client/SDK/PostgREST cases. Live provider and actual iPhone execution remain unverified.

**Current `codex/ai-command-journal` branch:** additive private command receipts execute chore completion and grocery mutations through existing authorized SQL commands in the same transaction as their immutable AI result. Repeated exact calls return the saved result; changed calls, wrong owners/households, revoked membership and expired turns cannot cause another effect. Turn finalization serializes with in-flight commands and reconstructs write parts from committed facts after process loss, replacing untrusted SDK write claims. New turns reserve transcript space; per-turn command count and byte bounds roll back effects on overflow without deleting history. Sixteen real PostgreSQL fixture cases pass, including concurrent duplicates, effect/receipt rollback, capacity and recovery. Disposable fixture security advisors report no issues. CI and independent exact-head review remain pending. Chat still exposes read tools only: SDK write registration, shared Effect adapter, native receipt cards and provider/device journeys are subsequent work. No production migration executed.

PR #28 adversarial review found a pre-migration capacity gap: an already-running old claim could accept a new command despite having no room for its recovered assistant message. Execution now checks recovery headroom before every new effect while preserving exact receipt replay. The added upgrade regression starts full claims under the old migration, applies the journal migration, verifies that no command effect is committed, and safely interrupts without changing history. All 17 focused journal/upgrade cases pass; the initial branch's full 108-case database suite passed. Updated-head review and CI are required before merge.

PR #28 merged as `7235066` after clean independent rereview on `f33ea7d36e6fd24f6a58e19b66c62d9d752adb70`, both current-head CI checks and no outstanding conversations. Greptile again reported the trial cap. The reviewer independently passed all 17 journal/upgrade cases. No production migration applied.

**Current `codex/assistant-household-actions` branch:** the native assistant can perform requested chore completion and grocery add/edit/remove/check through the durable SQL journal and shared native command/receipt contracts. Model inputs omit server-generated retry identities. Effect executors revalidate membership within the bound household, serialize writes and stop queued mutations after failure or interruption; the SDK stops before another model step after a failed tool. Lost-response finalization restores canonical committed receipts. Native receipt links distinguish success, conflict and unknown outcomes; incomplete write history cannot be silently discarded. Six new real SDK/API/PostgREST journeys pass, including dropped HTTP response after commit, parallel-write suppression and revoked membership. Five existing server journeys, five native SDK/HTTP roundtrips, 16 SDK cases and seven native assistant cases pass locally. Types/lint and iOS export (`/tmp/nest-ai-actions-export`) pass. CI and exact-head adversarial review are pending. No live provider or physical-device execution; financial approvals, meal generation and private memory remain incomplete.

PR #29 adversarial review reproduced a parallel validation gap: SDK rejection occurred before the executor latch, allowing a later valid write in the same step. The SDK's tool-call repair hook now closes the write guard before queued execution and declines repair. Regressions cover malformed known calls and unknown tools followed by a mutation; neither creates a journal/effect or another model step. Known uncertain writes remain fail-closed; never-executed unknown error parts are omitted from saved responses so they cannot poison future history. Nine mutation integration cases (including subtests) pass, and restored history validates. Updated-head CI and independent rereview remain required.

PR #29 self-audit also connected chore refresh to screen focus, matching groceries, so returning through a completion receipt does not keep the previously mounted Today list stale. Existing account-scoped refresh/retry behavior remains authoritative; actual iPhone navigation still needs verification.

PR #29 merged as `27059d0` after clean independent final signoff on `b8e2de41069c0eaaaa909d700f241021104c956e`, both exact-head CI runs and no outstanding conversations. Greptile reported its trial cap. The reviewer independently checked both invalid-first and valid-first/invalid-second ordering and found no remaining mutation leak. Final iOS export: `/tmp/nest-ai-actions-final-export`; actual device/provider execution remains unverified.

**Current `codex/private-food-preferences` branch:** an additive owner-private food profile stores explicit dietary restrictions/dislikes, optional calorie goal and portion preference separately from household-readable member fields. Missing rows remain unconfirmed setup. Current membership gates reads, updates and immutable retry receipts; scoped transaction locking and exact expected revisions prevent lost edits, while receipts avoid copying private profile content. Ten PostgreSQL cases cover partner/outsider/anonymous denial, independent member records, concurrent retries/conflicts, bounds, immutable replay, revocation and transaction rollback. All 119 database tests pass; disposable fixture security advisors are clean. CI and independent review are pending. This is the storage boundary only: reusable native settings/onboarding forms, API/Effect commands, corresponding AI tools, household cooking preferences and server-only meal-planning projection remain unfinished. No production application or private profile caching.

PR #30 merged as `d466e67` after clean independent signoff on `584415db461aba3dca4e83317d629f3926521fd4`, both exact-head CI checks and no outstanding conversations. Greptile again reported the trial cap. The reviewer independently passed all ten food-profile database cases. Storage remains source-only and production application is gated.

**Current `codex/food-preferences-api` branch:** shared Effect schemas and authenticated owner-only read/save routes connect the private profile storage. Reads distinguish unconfigured setup, validate complete result ranges and bind actor/household; saves bind returned operation/revision and reject extra identity fields. A route-specific 64KiB request ceiling accepts valid multilingual preference lists while all other commands retain 8KiB limits. Three real PostgREST journeys prove private reads, exact retries after dropped HTTP acknowledgments, conflicts, revocation and body/schema limits. Four routine service tests exercise malformed upstream scope/ranges/receipts and fail-before-dispatch validation; existing bounded/cancelled-body tests pass. Native forms, progressive onboarding and journaled AI settings tools remain next; no profile cache or device/provider verification is claimed.

PR #31 merged as `23b2923` after clean independent signoff on `29e56933cbbc8a6c69f96e3e3cfdb3f244f6bc03`, both exact-head CI checks and no outstanding conversations. Greptile again explicitly reported its trial cap. The reviewer independently passed all 42 API/integration cases.

**Current `codex/native-food-preferences` branch:** Today links to a reusable native private food form with restrictions/dislikes, optional calorie goal and portions. Missing setup stays visibly unconfirmed. Fresh credentials and validated owner/household/revision receipts gate online saves. Account-scoped memory retains the immutable attempt after an unknown outcome; retries cannot change its identity or input. Confirmed saves reload canonical current preferences, conflicting edits need an explicit reload, and failed reloads never reopen stale editing. Denial/disposal clears private state; no draft/profile enters SQLite. Same-member foreground verification retains the form, while account changes remount it. Twelve focused client/runtime/parser/lifecycle cases and 29 session regressions pass. Two real native-client → HTTP API → PostgREST/PostgreSQL journeys prove dropped committed acknowledgment replay, cold reopening, concurrent form conflict and membership revocation. Types/lint/format and iOS export (`/tmp/nest-native-food-export`) pass. CI, independent exact-head review and actual iPhone keyboard/back-gesture/VoiceOver verification remain pending. Journaled AI profile tools, onboarding and shared cooking preferences are still incomplete.

PR #32 merged as `ac06031` after clean independent signoff on `247f691fc71bf01282c2672db241522f16e551bc`, both exact-head CI checks and no outstanding conversations. Greptile explicitly reported the trial cap on this head. The reviewer independently passed all 14 food client/runtime/integration cases and checked form bindings against installed Expo UI source.

**Current `codex/assistant-food-preferences` branch:** the private assistant reads only the requesting member's food profile and uses a new journaled command to save explicitly requested changes. Inputs reuse native codecs without exposing operation IDs; the shared SQL command retains membership, revision and immutable receipt checks. A strict nested SQL validator prevents JSON coercion and caller identity injection. Canonical turn recovery replaces SDK claims with committed food receipts, and native result links open the same food form. Seven new PostgreSQL cases plus all 16 existing journal cases pass; four new real SDK/API/PostgREST journeys plus all nine household-action integration cases pass. Two API receipt/household-binding checks, eight native assistant cases and 16 SDK tests pass. Types/lint/format, disposable fixture security advisors and iOS export (`/tmp/nest-ai-food-export`) pass. CI and exact-head independent review remain pending. No live provider, actual iPhone or production application is claimed. Shared cooking settings, onboarding, private memory and meal planning remain incomplete.

PR #33 adversarial review reproduced a Unicode contract mismatch: SQL accepted 61 astral characters (61 code points) while the shared JavaScript schema rejected them (122 UTF-16 units). This made the stored profile unreadable and recovered journal input invalid for the actual SDK. The additive migration now aligns text bounds and blank-only validation with the shared codec, including ECMAScript whitespace. Direct native/journal RPC regressions reject the invalid boundary without any effect; 60 astral characters remain readable and canonical SDK history validates. A generated 65-case Unicode/whitespace comparison proves SQL/shared-codec agreement. All nine food-journal database cases and five food SDK integration journeys pass. Updated-head CI and independent rereview remain required.

PR #33 merged as `5f9608e` after clean independent rereview on `d694026846df9be9346360caa98cdb5d0d09773a`, both exact-head CI checks and no outstanding conversations. Greptile explicitly reported the trial cap on the corrected head. The reviewer independently passed 30 cases covering the Unicode fix, canonical SDK history and journal regressions.

**Current `codex/shared-cooking-preferences` branch:** shared household cooking notes and configurable breakfast/lunch/dinner visibility are stored separately from private food profiles. Both members can read and save with exact revision checks; receipts remain actor-bound and immutable. Missing setup is distinct from saved empty cooking notes. Current membership locks serialize revocation with writes, household locking prevents competing first saves, and failed receipt insertion rolls back the setting change. Shared Effect schemas enforce unique nonempty slot sets and bounded UTF-16 notes. Authorized API envelopes bind household/actor/revision; a route-specific 16KiB body ceiling supports the complete 2,000-character notes contract. Eight PostgreSQL tests, all 43 API cases and three actual PostgREST integration journeys pass. Types/lint and disposable fixture security advisors pass. Native cooking form, journaled AI action and onboarding remain next; CI and independent exact-head review are pending. No production migration, private profile projection or device verification performed.

PR #34 merged as `cf26fd1` after clean independent signoff on `2fe44f47076482ddf1731ed13abd524581365d9f`, both exact-head CI checks and no outstanding conversations. Greptile again reported the trial cap. The reviewer independently passed all 54 cooking/database/API/integration cases.

**Current `codex/native-cooking-preferences` branch:** Profile and settings separates personal food preferences from shared household cooking. The native cooking form edits bounded notes and breakfast/lunch/dinner toggles, distinguishes unconfigured setup and confirms unsaved dismissal. Food and cooking use one account-bound HTTP adapter, memory-only save/retry runtime and recovery panel, while keeping separate schemas, field forms and endpoints. Same-identity refresh keeps the form stable; changed accounts remount it. Three cooking transport/runtime cases, all 12 private food regressions and 29 session regressions pass. Four real native-client/HTTP/PostgREST journeys pass, including an acknowledgment lost before a newer partner edit: exact retry returns the original receipt and loads the newer canonical settings without overwriting them. Types/lint/format and final iOS export (`/tmp/nest-native-cooking-final-export`) pass. CI and independent exact-head review are pending. Actual keyboard/back-gesture/Switch/VoiceOver execution remains unverified. Corresponding journaled cooking AI actions and onboarding remain unfinished.

PR #35 merged as `0be70ad` after clean independent signoff on `bfda48ad211de7b45f02ea0fde0bab635a5e20c9`, both exact-head CI checks and no outstanding conversations. Greptile explicitly reported the trial cap. The reviewer independently passed all 48 affected native client/runtime/session/integration tests, including four real HTTP/PostgREST journeys. Actual iPhone interactions remain unverified.

**Current `codex/assistant-cooking-preferences` branch:** private assistant tools read shared household cooking settings and save explicitly requested changes through the native command inside the atomic turn journal. Exact revisions, generated retry identities and actor-bound receipts prevent lost partner edits and ambiguous replays. Nested SQL validation rejects coercion, caller identities and malformed slot sets. Canonical recovery restores committed facts; native result links open the same cooking form. Seven new PostgreSQL cases and all 25 existing food/household journal cases pass. Five new actual SDK/API/PostgREST journeys and 14 existing journeys pass, including shared reads without private food data, revoked membership, dropped acknowledgment halting queued writes and a later explicit turn after a partner conflict. Two API receipt/household checks, nine native assistant tests and 16 SDK regressions pass. Types/lint/format pass. CI and exact-head independent review remain pending. No live provider, actual iPhone or production application is claimed. Onboarding, private memory and calendar/notification settings remain unfinished.

PR #36 merged as `c7f98f3` after clean independent signoff on `c187c7a19ce37736f87080966497c498bf58554d`, both exact-head CI checks and no outstanding conversations. Greptile again reported the trial-credit cap. The reviewer independently passed 65 affected tests. Shared cooking settings now have native and assistant implementations with local/CI verification; actual iPhone/provider execution remains outstanding.

**Current `codex/private-memory-storage` branch:** owner-only memory rows and hash-only operation receipts reuse the exact-payload approval boundary. Every addition/change consumes a current approval for the same actor, household, operation, memory ID, revision and text in the same transaction as the write. Deletion needs no approval, clears active text and retains only a version marker to reject stale resurrection. Prior saved-operation receipts replay without restoring deleted content. Reads/receipts require current membership; owner locking serializes competing edits, first saves and the 64-active-memory bound. Shared codecs bound nonblank text to 1,000 UTF-16 units. Memory API, native inspection/confirmation/deletion UI and assistant proposal/read/remove tools remain unimplemented; no current assistant loads memory. Existing private conversation/approval history is separate from active saved memory and is not erased by a memory deletion. Production application remains gated.

Private-memory storage verification: ten new PostgreSQL cases and all ten existing approval cases pass, including concurrent saves/edits, atomic rollback, explicit confirmation, expired/denied approval rejection, owner isolation, deletion/replay, active-memory capacity and in-flight membership revocation. A generated 58-case Unicode/whitespace comparison agrees with the shared codec. Types/lint/format and disposable-fixture security advisors pass. CI and independent exact-head review are pending; no API/native/provider verification is claimed for this storage-only increment.

PR #37 merged as `a093e14` after clean independent signoff on `35720d61531e89d5c519fddc812d3483f8132a6f`, both exact-head CI checks and no outstanding conversations. Greptile reported the trial-credit cap. The reviewer independently passed all 20 storage/approval tests. This remains storage evidence, not a finished memory feature.

**Current `codex/private-memory-api` branch:** authenticated owner-only active-memory list, exact proposal read/create, native decision and deletion routes expose the shared memory contracts. Proposals do not save active memory. Native confirmation now decides approval, consumes it and writes memory/receipt atomically; a conflict or receipt failure leaves consent pending. Replayed confirmation survives receipt expiration without restoring deleted content; denial is immutable and exactly retryable. Strict reads validate owner, command/version, payload, complete ranges and unique active IDs. Five new PostgreSQL confirmation cases and ten storage regressions pass. Five real HTTP/PostgREST journeys cover the complete proposal/confirm/edit/delete flow, private access, lost proposal/confirmation acknowledgments, denied/expired/stale consent, capacity conflicts without partial approval, body limits and revocation. All 49 API tests pass; types/lint/format pass. Disposable-fixture security advisors pass. Unrepresentable NUL/lone-surrogate input is rejected before proposal dispatch. CI and independent exact-head review are pending. Native memory UI and assistant tools remain next; no device/provider verification or production application claimed.

PR #38 merged as `015c092` after clean independent signoff on `2198bacd75a3cedacafa7676b14af1c0393e8bcf`, both exact-head CI checks and no outstanding conversations. Greptile again reported the trial cap. The reviewer independently passed 69 tests: 49 API, 15 database and five real PostgREST journeys.

**Current `codex/native-private-memory` branch:** Profile and settings opens a private saved-memory list with add/edit review, explicit exact-text confirmation/denial and deletion. An approval deep link loads the owner-authorized proposal without approving it. Confirmed/expired proposals are inspectable and dismissible. The account-bound Effect client validates scopes and receipts; the memory-only runtime freezes uncertain proposal/decision/removal attempts, prevents contradictory actions and reloads after acknowledgment instead of resending. Conflict reloads refresh the current record and stale proposals can be declined. Denial clears private rows/proposals and resets the form generation; disposal aborts and prevents late callbacks from restoring state. Nine native transport/runtime/lifecycle cases and 29 session regressions pass. Two native-client → actual HTTP API → PostgREST/PostgreSQL journeys pass, including a confirmation acknowledgment lost before another device deletes the memory: replay returns the original receipt without restoring the text. Types/lint/format and iOS export (`/tmp/nest-native-memory-export`) pass. CI and independent exact-head review are pending. Actual iPhone text entry, navigation, confirmation controls and VoiceOver remain unverified. Assistant memory read/proposal/delete tools are still next; no live-provider or production application claimed.

PR #39 merged as `78d9934` after clean independent signoff on `354935c0cd665f748d645312e68a110cee74be71`, both exact-head CI checks and no outstanding conversations. Greptile reported its trial-credit cap. The reviewer independently passed 40 native memory/session/real HTTP integration tests. Actual iPhone execution remains outstanding.

**Current `codex/assistant-private-memory` branch:** the assistant reads only current owner-private saved memory, proposes explicitly requested additions/edits and deletes requested entries through the atomic command journal. A proposal creates a server-generated pending approval, never active memory or consent; its result opens the exact native confirmation. AI tools cannot decide or approve. Exact schema validation, actor/household/content/revision receipt binding, immutable replay and canonical recovery protect the handoff. Seven PostgreSQL cases cover consent injection, isolation, concurrency, deletion, rollback and recovered proposals. Four actual SDK/API/PostgREST journeys cover native confirmation before saved reads, deletion, lost acknowledgment halting queued writes, partner privacy and revoked membership. The simulated model is test infrastructure, not live-provider verification. All 140 focused journal/turn/API/native-assistant/SDK integration cases pass locally. Types/lint/format and disposable-fixture security advisors pass. CI and exact-head adversarial review are pending. Onboarding, calendar/notification settings and remaining milestones are still incomplete; actual phone/provider execution and production migration remain gated.

Assistant-memory iOS export passes at `/tmp/nest-ai-memory-export` (bundle `d5aa93be0888856a3942f0684ff977d3`). This is compilation evidence only. All 39 journal tests pass after the shared migration-list extraction.

PR #40 merged as `05ac1d8` after clean independent signoff on `5b004e0c4f33552556c1146c92e05ef5bcb5f99e`, both exact-head CI checks and no outstanding conversations. Greptile reported its trial-credit cap on that exact commit. The reviewer independently passed 69 affected tests. Private memory now has native and assistant implementations with local/CI evidence; phone and live-provider execution remain unverified.

**Current `codex/calendar-sharing-api` branch:** authenticated endpoints expose disabled-by-default calendar consent, consent-bound capture generation, sanitized atomic busy publication and fresh household snapshots using the existing reviewed database boundary. Shared contracts reject personal metadata, coercion, unordered/overlapping/out-of-coverage intervals and oversized captures. The API verifies complete household-scoped reads, unique actors, capture lifetime/consent, exact consent acknowledgments and 15-minute freshness; missing/expired data supplies no availability evidence. Seventy API/database cases pass locally (55 API and 15 existing consent/RLS cases). Native calendar picker, local selection persistence, publication lifecycle and AI availability use remain unfinished. This is API evidence, not device Calendar completion. Production application remains gated.

Calendar API verification also passes five actual HTTP/PostgREST journeys: default/opt-out privacy, metadata rejection, lost consent/publication acknowledgments, capture/membership supersession, expired snapshots, and the full 512-interval boundary with overflow preserving the prior snapshot. Types/lint/format pass. CI and independent exact-head review are pending; no native or live-provider verification is claimed.

PR #41 merged as `d6fc8a8` after clean independent signoff on `7fcb30ad02348c41e19b491cfa1a217b9e7ac78d`, both exact-head CI checks and no outstanding conversations. Greptile again reported the trial-credit cap. The reviewer independently passed 24 affected API/database/real PostgREST tests.

**Current `codex/native-calendar-sharing` branch:** Profile and settings exposes calendar read permission, local calendar selection, explicit busy-sharing opt-in, refresh and opt-out. IDs/selections stay in account-leased SQLite; pending consent attempts are durable and exactly retryable, while cold restart does not automatically grant opt-in. Current consent incarnation/version gates publishing. Permission revocation or missing calendars stages disabled consent and removes published data when confirmed; unknown outcomes block publication. The Effect client binds every acknowledgment to the account and capture. Foreground/reconnect and five-minute active refresh are wired; no background guarantee. The operational horizon is 31 days with the existing conservative 15-minute expiry, shorter than the audit's initial suggested defaults. Actual device refresh/permissions still need verification.

Native calendar verification: 84 calendar/session/SQLite/integration regressions pass, including three native-client → real HTTP API → PostgREST journeys with cold restart after lost opt-in, another device's opt-out, publication lost acknowledgment and membership revocation. Eleven focused client/runtime cases also pass after adding exact receipt/account checks and late-load disposal. Local projection tests exclude only the current user's declined attendance and discard attendee metadata. Types/lint and iOS export (`/tmp/nest-native-calendar-export`) pass before the final status-copy/freshness adjustment; final checks, CI and independent exact-head review remain pending. Actual iPhone permission prompts, switches, long calendar names, navigation, VoiceOver and EventKit recurrence/DST/restricted calendars remain unverified. Real agenda/partner presentation, AI availability use and notification settings are unfinished.

Final native-calendar verification passes all 88 focused tests, types/lint/format and a fresh iOS export at `/tmp/nest-native-calendar-final-export` (bundle `f53ce0b27d922b9f2f13ed77da538793`). Consent changes clear stale displayed expiry. A revocation racing another device's settings no longer claims global sharing is disabled when current server consent remains enabled. CI and independent review remain required.

PR #42 merged as `ad6373c` after clean independent signoff on `cc7ec9504218312f494d8d807c4ae10049e2ea84`, both exact-head CI checks and no outstanding conversations. Greptile reported its trial-credit cap. The reviewer independently passed 28 affected native calendar/SQLite/real HTTP integration tests. Actual iPhone UI and EventKit behavior remain unverified.

**Current `codex/assistant-calendar-availability` branch:** assistant reads reauthorize each invocation and use complete current-household membership plus fresh opted-in snapshots. A pure shared availability rule returns busy/free only for valid, fully covered, unexpired evidence; absent/expired/future/out-of-range evidence is unknown. Busy intervals are clipped to the requested range, and free is explicitly limited to opted-in calendars. A native settings handoff grants no OS permission or sharing consent and performs no refresh. The native card opens the real settings route. Four actual SDK/API/PostgREST journeys use the audited tenancy/RLS baseline and cover missing partner evidence, expiration, out-of-coverage queries, opt-out versus historical results and revoked membership. One thousand generated property cases validate overlap, clipping and expiry. All 100 focused API/domain/SDK/native-assistant/integration tests pass; types/lint/format and iOS export (`/tmp/nest-ai-calendar-export`, bundle `c765d339ddcde845ad2c9d1a18ad2024`) pass. CI and independent exact-head review remain pending. Simulated model tests do not establish live-provider behavior; device Calendar agenda/partner rendering, background scheduling, onboarding and notification settings remain incomplete.

PR #43 merged as `3b777ff` after clean independent signoff on `d53a5c8298d459f2171c5af3c361ec17282e9303`, both exact-head CI checks and no outstanding conversations. Greptile reported its trial-credit cap. The reviewer independently passed 79 API/domain/native/SDK/PostgREST tests, including 1,000 property iterations. Actual provider and iPhone behavior remain unverified.

**Current `codex/notification-preferences` branch:** per-person daily-summary enable/time and item-reminder enable choices have owner-only storage and authenticated read/save routes. Missing setup remains absent, never consent. The daily-summary time is explicitly Europe/Zurich; all opt-ins must be supplied. Audited legacy digest SQL/UI had an enabled default and non-idempotent upsert; those behaviors and browser push plumbing were not copied. Native device enrollment, actual notification scheduling/delivery, native controls and assistant actions are still unfinished. This storage/API increment does not claim notification completion.

Notification verification: 83 focused tests pass (63 API, 11 new PostgreSQL, nine actual HTTP/PostgREST notification/food/cooking journeys). Exhaustive 1,440 valid wall-clock times and malformed boundary strings agree with the shared codec and SQL. Tests cover partner/tenant/anonymous isolation, denied direct writes, concurrent duplicates and CAS conflicts, immutable old retries after a newer opt-out, receipt rollback, bigint overflow and membership revocation serialization. Types/lint/format and disposable-fixture security advisors pass. CI and exact-head independent review remain pending. No production data, delivery registration or device verification was performed.

PR #44 merged as `ca5c187` after clean independent signoff on `8b3628990a859cb0b2048ae43b96fb5bd413852d`, both exact-head CI checks and no outstanding conversations. Greptile explicitly reported the trial cap on that commit. The reviewer independently passed all 83 API/database/real HTTP tests. Notification delivery remains unfinished.

**Current `codex/native-notification-preferences` branch:** Profile and settings opens owner-specific daily-summary and item-reminder choices, both initially off for missing setup. The native time picker preserves a wall-clock choice in Europe/Zurich without shifting with the phone's travel timezone. The account-bound client and shared memory-only preference runtime validate identity and exact receipts, freeze uncertain writes, reload canonical values after acknowledgment and expose conflicts/reverification. Navigation warns before discarding unsaved input/retry details. UI explicitly distinguishes saved preferences from OS permission and delivery, which remain unfinished.

Native notification verification: 50 focused tests pass (four notification client/runtime/time cases, 12 food, three cooking, 29 session and two real native-client/HTTP/PostgREST journeys). These include all 1,440 minute choices, rejected malformed times, immutable retry after another device opts out, private partner reads, CAS conflict and membership revocation. Types/lint/format pass. iOS export, CI and independent review are pending. No actual iPhone picker, switch, back gesture, Dynamic Type or VoiceOver verification is claimed; permission enrollment, scheduler/outbox/delivery and corresponding assistant actions remain unfinished.

Native notification iOS export passes at `/tmp/nest-native-notification-export` (bundle `74beed0a701c03552119b0211a45b093`). This is compilation evidence only; actual native interactions and delivery remain unverified.

PR #45 merged as `60b0316` after clean independent signoff on `87b6ff1918a0351b5f181acccdb894e7fab0afd1`, both exact-head CI checks and no outstanding conversations. Greptile again reported its trial-credit cap. The reviewer independently passed all 50 native client/runtime/session/integration tests. Actual iPhone controls and notification delivery remain unverified.

**Current `codex/assistant-notification-preferences` branch:** the assistant reads only the requesting member's notification choices and saves explicit changes through the native command inside the atomic private turn journal. It must preserve unspecified settings and ask for missing required choices, never assume opt-in. Daily-summary time is Europe/Zurich. Receipt cards open real private settings and do not claim OS permission, enrollment or delivery. Strict nested SQL rejects coerced booleans, extra identities/operations and invalid times; current membership, CAS, immutable replay and canonical recovery remain enforced. Shared revision parsing now rejects trailing line terminators consistently with the SQL journal.

Assistant notification verification: 168 focused API/journal/native-assistant/SDK/integration cases pass before the revision tightening, including five new actual SDK/API/PostgREST journeys. Eight new PostgreSQL cases cover isolated partner choices, owner-device CAS, rollback, forged SDK output, revoked membership and an old AI retry after a newer native opt-out. Generated revision boundary tests additionally reject whitespace on 1,003 canonical values. Disposable-fixture security advisors pass. Final types/lint/format/export, CI and independent exact-head review remain pending. Simulated models are test infrastructure, not live-provider evidence; real iPhone controls, permission enrollment and notification delivery remain unverified and unfinished.

Final assistant notification verification passes 118 affected API/native preference/memory/SDK/database tests after strict revision parsing, plus types/lint/format and fresh iOS export (`/tmp/nest-ai-notification-final-export`, bundle `abb2e1a196229d455d31eb90ed57d03d`). CI and independent exact-head review are still required. No real provider or device execution is claimed.

PR #46 merged as `26063a5` after clean independent signoff on `c5d9dfc38799314e03f5b56bafc8d4e44b7a5d5c`, both exact-head CI checks and no outstanding conversations. Greptile reported the trial-credit cap for that commit. The reviewer independently passed 129 distinct SQL/API/native/SDK integration tests. Notification preferences now have native and assistant implementations; delivery and actual device/provider verification remain unfinished.

**Current `codex/progressive-setup` branch:** a verified member can choose Start quickly (real Today) or Set up everything (protected setup overview), and revisit setup through settings. Navigation never saves defaults or consent. The overview links the existing editable food, cooking, notification, calendar and private-memory forms. Its authorized status read returns only the caller's food/notification configured flags and the explicitly shared cooking flag; calendar access is device-specific and memory optional. Permission/delivery is never inferred from saved preferences. Returning to the overview refreshes status, while failed reads stay unknown. Account-scoped runtime cancellation/disposal and sticky authorization denial protect stale results. Assistant readSetupStatus uses the same service; openSetup is a strict navigation-only handoff.

Setup verification: 132 API/native/session/SDK/integration regressions pass, including three real native-client or SDK → HTTP API → PostgREST journeys. A further lifecycle regression explicitly covers late response after disposal; the five focused setup client/runtime/owner cases pass. Types/lint/format and iOS export (`/tmp/nest-setup-export`, bundle `0f7f1d2b5d9eb0c6d57e2f4dea993a2d`) pass. CI and exact-head independent review remain pending. Actual iPhone navigation, controls, Dynamic Type and VoiceOver remain unverified. Progressive prompts at real Meals/Calendar entry will be connected as those production screens are implemented; the fictional four-tab preview does not count. No additional persistence, production application or permission request occurs merely by opening setup.

PR #47 merged as `d26c8fc` after clean independent signoff on `3706f36bda44b9ca772c4cd480f094555679c3fe`, both exact-head CI checks and no outstanding conversations. Greptile reported the trial cap. The reviewer independently passed 133 affected tests. Actual iPhone setup navigation remains unverified.

**Current `codex/audited-routine-rules` branch:** deliberately audited pure recurrence, assignment and closure planning from legacy commit `4a528c9`; provenance and exclusions are in `docs/native-rewrite/routine-rules-audit.md`. No legacy screen or infrastructure was copied. Civil years before 100 and duplicate member IDs are corrected. Nineteen domain tests pass, including nine new routine cases and 4,000 seeded property runs. One isolated PostgreSQL parity test passes across 2,120 inputs for both first-date and successor functions. Domain types and scoped lint pass (Date construction advisory warnings remain for civil-date arithmetic). CI and exact-head independent review are pending. This is a rules boundary only: full transactional recurrence, routine CRUD, native UI and corresponding AI tools remain incomplete; no device verification or production migration occurred.

PR #48 merged as `9739b04` after clean independent signoff on `49e5b0d05cc88b55f595ab83fc99d1732d521233`, both exact-head CI checks and no review conversations. Greptile reported the trial cap. Independent verification passed 19 domain tests plus the 2,120-input PostgreSQL parity test.

**Current `codex/routine-command-contracts` work:** strict Effect contracts cover the agreed recurrence presets, shared/assigned/alternating responsibility, create/edit baselines, pause/resume/archive and skip/reschedule. Edit versions retain PostgreSQL microseconds; title, date, UUID, integer and unknown-field checks reject malformed inputs when decoded strictly. These contracts do not establish household membership or usable future date windows: server commands must authorize actors/assignees and validate date arithmetic against the actual anchor. No arbitrary new interval cap is introduced beyond legacy PostgreSQL int32 representation. Native routine management and the real transactional engine are still incomplete. All 72 API regressions pass, including five new routine decoding cases and 1,004 exact microsecond-version round trips. Repository types, lint and formatting pass. CI and exact-head independent review remain pending.

Routine contract review found unpaired UTF-16 surrogates could reach PostgreSQL JSONB parsing. The title decoder now rejects those using the same Unicode-aware pattern as private memory while preserving valid astral characters. Six focused routine cases pass after the fix; updated exact-head review and CI are required.

PR #49 merged as `697e66d` after the Unicode finding was fixed and independent rereview signed off on `f69205becb3dd3b118bcfd9790b25028ab1b3512`. Both exact-head CI checks passed and no conversations remained. Greptile reported its trial cap on the updated head; the reviewer independently passed all six focused contract tests.

**Current `codex/routine-creation-storage` work:** gated migration `20260920082522_native_routine_creation.sql` creates real legacy-compatible routine/current/preview rows with strict input validation, current membership locks, exact two-member assignment, actor-bound immutable retry receipts and atomic activity. No reminder consent is created. Audited legacy insertion/date/table subsets form the disposable fixture; source choices and limitations are in `docs/native-rewrite/routine-creation-audit.md`. Nine database tests pass, including 1,000 generated real creations, plus a 143-input Effect/SQL validation parity case. Disposable security advisors report no issues. API/native/AI integration and the full closure/edit engine are still unfinished; CI and independent review are pending. Concurrent local Expo app configuration changes are outside this database slice and are preserved separately.

Final focused routine storage verification passes 36 tests across domain, contracts and PostgreSQL, including the existing 2,120-input legacy date parity suite. Repository typechecking and changed-file formatting/lint pass. This does not cover native execution or the separate in-progress Expo configuration edit.

PR #51 merged as `5d6ac42` after GPT-5.6 Sol medium independently signed off on `fd59c43e54fb1b1c0c7a6e415179f6deaa3613d1` and passed all 36 focused checks. Both exact-head CI checks passed and no conversations remained. Greptile reported the trial cap. Local main also includes the separately merged #50 app identity; its configuration matched the concurrent local edit, whose original bytes were preserved at `/tmp/nest-app-config-before-main-sync.json` during synchronization.

**Current `codex/routine-creation-api` work:** authorized GET `/v1/routines` and POST `/v1/routines/create` share strict contracts and the real database transaction. List reads verify complete bounded snapshots, unique IDs, household/assignment ownership and the current roster. Creation binds actor/household/operation/action receipts. Historical titles retain the legacy 120-code-point read allowance without widening new-form input. PostgreSQL timestamps with explicit offsets convert to canonical UTC while retaining microseconds. Real transport tests caught and fixed an initial UTC-only assumption. Three actual HTTP → API → PostgREST journeys pass for shared reads, lost acknowledgment/later edit recovery, revocation and malformed commands. Four API service tests and four existing availability regressions pass after extracting the shared roster reader. A 600-timestamp PostgreSQL parity case across six timezones passes. Native editor and corresponding AI creation action remain next; current API changes have not yet passed CI or independent review.

Final routine API verification passes all 81 affected API/database/real HTTP checks. Repository types, lint and formatting pass. CI and GPT-5.6 Sol medium exact-head review are required before merge; no native routine screen or live-provider execution is claimed yet.

### Calendar chore read — candidate

The authorized calendar service reads stored current and next-preview occurrences for one explicit civil date, excluding closed, paused and archived work. The native endpoint and `readCalendarChores` AI tool share the same strict Effect service. Preview results remain explicitly tentative, with no invented time, future recurrence expansion or EventKit writes. Exact PostgREST count/range validation rejects truncation, and record/date/household checks reject inconsistent results rather than showing a partial empty day. Existing chore commands remain the action boundary.

Two focused boundary cases and one real API/AI/PostgREST/PostgreSQL journey pass. They cover duplicate/date/tenant/schema/count rejection, accepted-assignee projection, current versus preview, partner access, revoked membership and inactive routines. API typechecking passed. Merged as `2a103933aa7a29f35884d267cf53f5115618dd6b` after successful exact-commit CI `35587412629` and clean Sol medium signoff (seven independently passing focused checks). Local main was fast-forwarded and pushed without a PR. The optional native layer control, account-scoped loading and agenda presentation are next; this read boundary does not complete the chore layer. No production migration or device execution occurred.

### Native Calendar chore layer — candidate

The agenda now has an optional native Show chores switch and virtualized, untimed current/preview rows. The display choice lasts for this mounted account session; it makes no sharing or calendar permission changes. Due-date rows are separated from timed personal/partner blocks, with explicit tentative-preview wording and a working Manage chores handoff. No event is written to iCloud. The shared calendar client binds household/date responses and uses the existing Expo authenticated transport. Account leases are verified before and after reads. Date changes, toggle-off, blur/background and disposal cancel in-flight work; access loss hides rows. Network return, foreground, manual refresh and the existing minute refresh recheck the selected day.

Five new native client/controller/SQLite cases pass for opt-in, date races, cancellation, error/retry, foreign/date/duplicate responses, account lease changes and owner disposal. Two real API/AI/native HTTP/PostgREST/PostgreSQL journeys pass, including current-to-preview date navigation and revoked partner access. Mobile typechecking and scoped lint pass. The iOS export succeeds at `/tmp/nest-calendar-chore-layer-export`, bundle `8fa19b402f7c5918de1df0edf4f42803`; this is packaging only. Merged as `dbb12fd597c58629dac48e665638b9224352a009` after successful exact-commit CI `35588222384` and clean Sol medium review (16 independently passing checks). Main was fast-forwarded and pushed without a PR. Native toggle rendering, focus/AppState transitions and VoiceOver require an iPhone; the renewals layer still depends on M8. No production data or migrations were touched.

### Calendar production navigation — candidate

Inspection found that the implemented Calendar screen and assistant handoff were reachable only through the development-only tab group. A protected `/agenda` route now exposes the same account-bound screen in release navigation, with a Calendar link from the real Today screen and a fixed assistant destination. The development entry explains that Calendar uses actual account/device data rather than claiming all preview screens are fictional. The final production four-tab shell still needs integration as Money lands; this route does not claim that shell is complete. Device navigation remains unverified. Twelve focused assistant result/handoff cases, mobile typechecking, scoped lint and formatting pass. Production iOS export succeeds at `/tmp/nest-calendar-production-route-export`, bundle `f56c60d531df575c5b9d84386ec9de18`; this proves packaging only. Merged as `15eb3ce2279ced18c1abb87cd381716920a24639` after successful exact-commit CI `35588558121` and clean Sol medium review (21 independently passing checks). Main was fast-forwarded and pushed without a PR.

### Money ledger audit and fixture — candidate

A deliberately audited subset of the legacy ledger tables, read RLS and append-only/zero-sum triggers is now available in disposable fixtures, with provenance and exclusions in [the Money audit](native-rewrite/money-ledger-audit.md). No financial writer or production migration is introduced. The audit reproduces a zero-sum limitation: one zero-valued row can satisfy the legacy trigger without forming a complete member pair; Nest's existing domain validation rejects it. New reads must validate completeness explicitly.

Four PostgreSQL cases pass, including 500 generated exact-centime expense vectors, tenant/anonymous/direct-write denial, immutable history, opening/reversal uniqueness and atomic rollback on an unbalanced insert. Initial fixture setup redundantly declared the existing UUID extension function; it was removed. The first large vector command exceeded the OS argument limit; bounded SQL batches now execute all vectors. No production history, hosted financial API or device was used. Merged as `dd84dfa303a1ddb9df9d46b1ba420cd8082d829a` after successful exact-commit CI `35589291981` and clean Sol medium rereview. The reviewer independently passed all four cases and matched 31 extracted SQL statements to the pinned reference; two EOF blank lines were removed and provenance hashes refreshed before signoff. Main was fast-forwarded and pushed without a PR. Money UI/API/AI, approvals, receipts and recurring automation remain incomplete.

### Authorized Money balance read — candidate

Gated migration `20260921103207_native_money_balance_read.sql` reads all retained ledger entries in one STABLE statement snapshot. It authorizes current membership, requires two members, rejects missing/incomplete/non-zero-sum event pairs, and includes original opening balances and correction history. Exact numeric totals and event counts are decimal strings; the strict Effect boundary rejects unsafe centime totals, duplicate/missing actors, inconsistent zero histories and foreign households. Authenticated GET `/v1/money/balance` and the corresponding private assistant read tool share this service. No financial write, approval bypass, scheduler or opening-balance reset is exposed. The API dispatcher uses a bounded route map to retain the complexity limit.

Eight focused contract/API/PostgreSQL/real PostgREST checks pass, including 1,000 centime decoding vectors, a 603-event history, incomplete pairs, unsafe aggregate rejection and a blocked-statement concurrency test proving counts/balances share a snapshot. All 235 API regressions pass. The first broad suite was stopped after sandbox restrictions stalled existing local HTTP setup; the permitted rerun passed. API/contracts types, scoped lint and disposable security advisors pass. FK-only fixture support tables now enable RLS explicitly without granting access. Native balance/history UI, event detail, all money writes and actual-device journeys remain incomplete. Merged as `8d34d0bbbd82288be338dcc8a7cf1174a54bea3a` after successful exact-commit CI `35589983069` and clean Sol medium signoff (eight targeted plus 21 route regressions independently passed). Main was fast-forwarded and pushed without a PR; no production migration occurred.

### Retained Money history read — candidate

Gated `20260921104133_native_money_history_read.sql` returns bounded 50-event pages ordered by occurrence date, full PostgreSQL creation microseconds and UUID. A tenant-authorized event ID anchors keyset paging, so equal timestamps are not skipped and a foreign/missing cursor has the same unavailable result. Original events, reversals, replacements and relationship IDs are preserved. Receipt presence is metadata only; paths/content are not returned. Authenticated GET `/v1/money/history` and `readMoneyHistory` use one strict Effect service. No balance is computed from page contents. Pages are live reads, not a frozen multi-request snapshot; refresh from the first page to see newly inserted history.

Fourteen affected contract/API/PostgreSQL/PostgREST checks pass, including six new history cases for 123 records across date/microsecond/UUID ties, exact 50-row endings, intervening new entries, correction relationships, authorization and strict response validation. All 236 API regressions, API typechecking, scoped lint and disposable security advisors pass. A synthetic integration fixture initially used arbitrary MD5 UUID bits; canonical UUID fixtures now exercise the strict transport successfully. Native history/detail UI, financial mutations and physical-device verification remain unfinished. Sol found that a legal retained date outside the native range (including infinity) made an entire history page unavailable. The revised contract preserves full PostgreSQL date/time literals and uses exact day/microsecond ordering keys, so BC, distant future and infinite records remain reachable without coercing them into native dates. A real API/AI fixture now walks 147 retained records across those ranges; all seven focused history checks pass. Merged as `a9076c38e2532fa83ee12cb91edc4820740bd1e7` after successful exact-commit CI `35591559838` and clean Sol medium rereview (14 focused cases independently passed). Main was fast-forwarded and pushed without a PR. No production migration or financial command was run.

### Retained Money detail read — candidate

Gated `20260921105214_native_money_detail_read.sql` reads an authorized retained event, payer, per-member allocations and ledger changes, note, current category label (including archived categories), related original and reverse correction link. Exact centimes remain strings; full retained dates use the history representation. Receipt paths and content stay out of these reads. The database rejects incomplete/incorrect reversal projections; the strict Effect boundary validates identity, complete zero-sum member pairs and type-specific allocation/delta arithmetic. GET `/v1/money/detail` and `readMoneyDetail` use the same service and no financial writer.

Six focused API/database/actual PostgREST cases pass, including 120 expense/refund allocation vectors, safe-integer endpoints, all six event kinds, absent/foreign/anonymous denial, inconsistent projections and retained correction/receipt metadata. All 238 API regressions, API types, scoped lint, formatting and disposable database security advisors pass. The additional malformed-ledger integration assertion initially used the wrong tool error shape; it now verifies the shared finite `unavailable` result. Native detail/history/balance screens, receipt access and all financial commands remain outstanding. Merged as `3e99164a0c3595ea933bf66dd4727dfa38c3823b` after successful exact-commit CI `35592047074` and clean Sol medium signoff (12 focused detail/history cases independently passed). Main was fast-forwarded and pushed without a PR; no production data or physical device was used.

### Native Money read client — candidate

The account-bound Effect client now reads balance, history pages and event details through the shared real API. It validates credentials, household, member, cursor and event identity; uses the existing bounded request timeout/cancellation and finite failures; and exposes no mutation or offline-money queue. The session provider supplies the Expo fetch adapter for all three reads. Native client tests join routine CI.

Two focused transport cases plus one actual native-client → HTTP → PostgreSQL/PostgREST journey pass, including safe centime endpoints, retained infinite dates, linked details, response loss/retry without a write, malformed input and foreign reads. Mobile types pass. This is transport plumbing: the existing Money screen is still an explicitly fictional design preview. Native runtime, durable offline snapshots, real screen interactions and device verification remain outstanding. Merged as `ec2332e7aa3665e97fb928413d771c52c17326a5` after successful exact-commit CI `35592518043` and clean Sol medium signoff (all three focused cases independently passed). Main was fast-forwarded and pushed without a PR.

### Saved Money views — candidate

The account/household/lease-scoped SQLite store now supports validated read-only balance, history pages and event details with a local saved-time label. It retains one balance, the first history page plus 19 continuation pages, and 50 details. Expired cache entries require an online reload; retention never touches server history. Invalid or mis-keyed data produces an explicit failure rather than a false empty view. Cancelled/stale request generations roll back their transaction, and access-denial recovery can clear only the active account's Money cache. No financial operation enters the offline queue.

Five focused real SQLite cases pass for process reopen, safe centime/infinite-date preservation, account leases, late writes, transactional cancellation, corruption/identity checks, retention and absence of money queue entries. All 29 directly affected offline tests, mobile types and scoped lint pass. Screen/runtime integration, cache-age presentation and actual iPhone verification remain outstanding. Merged as `0edacce927fbf569924009f1f3c6345279b5a3cf` after successful exact-commit CI `35593019094` and clean Sol medium signoff. Main was fast-forwarded and pushed without a PR.

### Native Money read screens and runtime — candidate

The fictional Money preview is replaced by account-bound balance, bounded history-page navigation and financial-event details. Protected `/finances` and `/money-event` routes are reachable from real Today and the development Money tab. The screen explains exact per-member allocations/deltas, original/reversal links, note/category and retained date literals. It explicitly marks stale/previously loaded values and unavailable receipt attachment viewing; no expense/settlement/correction button claims a writer exists. Full production four-tab integration and financial writes remain outstanding.

The shared Effect client, lease-checked runtime and SQLite cache now form a real read path. A corrupt cache can repair online; a failed save retains the fresh in-memory result with an explicit offline-storage warning. Target/focus/background changes cancel reads, and access denial hides both overview sections, cancels sibling reads and clears scoped cache. Strict-mode owner resubscription creates fresh runtimes. An initial denial-coordination test exposed recursive notifications; atomic denial publication fixes it.

Fourteen focused native runtime/client/cache/display cases pass, including 1,000 exact-centime formatting vectors, stale responses, lease replacement, failure recovery, denial propagation and owner cleanup. One actual native-runtime → HTTP → PostgreSQL/PostgREST → SQLite journey passes for all three read kinds, restart during server unavailability and denied credentials. Types and scoped lint pass. These are runtime/storage tests, not executed iPhone gestures or accessibility verification. iOS export succeeds at `/tmp/nest-money-read-screen-final-export`, bundle `cb866d753fae2f1299f55bdafeba1bd4`; this proves packaging only. Both actual native-client/runtime HTTP integration journeys pass. Merged as `5a7558d2f8f4c3f28d1f0a447b83974e1745556c` after successful exact-commit CI `35594408629` and clean Sol medium signoff (16 focused cases independently passed). Main was fast-forwarded and pushed without a PR. No production data, migration or financial command was used.

### Native routine lifecycle — implementation in review

PR #61 merged as `d3cf22c` after both exact-head CI checks passed, no review conversations, and clean GPT-5.6 Sol medium fallback review of `ad5ad6ab9206640469ae083d34375329ac79121b`; Greptile explicitly reported its credit cap. Independent verification covered 19 contract/API cases and all 8 actual routine HTTP/PostgreSQL/PostgREST journeys. [Merge evidence](https://github.com/drrius/nest/pull/61#issuecomment-5749225862).

The native routines screen now implements an iPhone SwiftUI overflow menu with Edit, Pause/Resume and archive confirmation that explains retained history. The account-bound Effect client validates the action and complete receipt identity. Runtime retries keep the original operation, action and exact baseline; uncertain attempts block unrelated changes, confirmed writes with failed refresh reload without resending, and access denial clears scoped data. Leaving a pending lifecycle action warns that its retry details will be lost. Today already refreshes on focus. Web preview explicitly identifies the lifecycle controls as iPhone-only.

Local verification: 23 focused native routine unit cases passed; all three native client/runtime → actual HTTP/PostgreSQL/PostgREST journeys passed, including committed pause response loss, partner resume, immutable retry, stale archive conflict and archive retaining its current occurrence. Mobile TypeScript and scoped lint passed (Effect advisory suggestions remain). Menu interactions, archive confirmation, navigation warning, VoiceOver and Today refresh on a real iPhone remain unverified. Lifecycle AI tools are still outstanding; M4 remains incomplete. CI and exact-head independent review for this native change are pending.

The lifecycle iOS Metro export succeeded at `/tmp/nest-native-state-export` (`8cc82da4a20a3e61fdd50655081fcac0`, 1,839 modules). This is compilation/packaging evidence only, not native interaction verification.

Sol review of initial native lifecycle head `5a4c0af` found that the leave warning incorrectly used generic loading state. The fix introduces an explicit unresolved-write signal: initial/reload reads and post-acknowledgment refreshes never trigger that warning; in-flight and uncertain writes do. A controlled pending-read/pending-write regression verifies those transitions. Updated-head review and CI remain required.

### AI routine lifecycle — implementation in review

PR #62 merged as `7a4e218` after both exact-head CI checks passed, no review conversations, and clean GPT-5.6 Sol medium rereview of `639963923f1b63a60dee8f92269c3f6190d1ae7d`. Independent verification passed 24 native unit cases plus all three real native routine database journeys; the overly broad loading warning was fixed before signoff. [Merge evidence](https://github.com/drrius/nest/pull/62#issuecomment-5749269367).

Assistant `setRoutineState` now shares the strict native lifecycle schema and `nest_set_routine_state` transaction through the private AI journal. The model cannot supply actor, household or operation identity. Receipt matching checks household, actor, routine and exact requested action. Pause/resume/archive are ordinary explicitly requested actions, not financial approvals or memory consent; tool guidance preserves history and asks for clarification on ambiguous deletion. Canonical history replaces invented lifecycle tool results, and native cards show paused/resumed/archived with a link to `/routines`.

The gated additive migration extends the finite tool vocabulary, strict input validation, dispatcher and canonical journal response while preserving existing branches. Local verification covers concurrent replay, partner resume before retry, stale/archived durable conflicts, malformed/hidden inputs, journal failure rolling back archive/occurrences/reminders/activity/native receipt, revoked/private transcript access, and actual SDK/PostgREST committed-response loss. Security advisors reported no issues on the disposable routine fixture. No production database, live model or physical device was used. CI and exact-head independent review remain pending; M4 is still incomplete.

### Actual occurrence closure engine — audit and fixture

PR #63 merged as `58af803` after exact-head CI, no review conversations and clean GPT-5.6 Sol medium fallback signoff for `4746f06d976e7f6a910c01bb57620ab233653f28`. Independent verification passed 109 checks (35 focused and 74 prior AI database regressions). [Merge evidence](https://github.com/drrius/nest/pull/63#issuecomment-5749302780).

The next M4 slice imports only the deliberately audited legacy closure function, public wrappers and receipt schema into disposable test fixtures; see [closure audit](native-rewrite/routine-closure-audit.md). Three local actual-engine tests pass for complete/skip succession and replay, retained history, reschedule identity/anchor/reminder date/partner notice, and foreign-member/preview denial. Native completion contention, skip/reschedule wrappers/UI/AI and takeover acceptance remain incomplete. This fixture is not a production migration or a completed native vertical slice.

### Native completion against the actual closure engine

PR #64 merged as `219d932`, with both exact-head CI checks, no review conversations and clean Sol medium signoff on `d54ce6c5a196b9d5fe148323a6c46674fcc1e763`; provenance was checked byte-for-byte against the pinned legacy reference and all three actual-engine tests passed. [Merge evidence](https://github.com/drrius/nest/pull/64#issuecomment-5749328896).

A real-engine probe reproduced queued completion succeeding after archive. The current gated additive fix rechecks active/unpaused routine state under the closure locks; stale archived/paused requests conflict, while prior committed receipts still replay unchanged. It also checks a target that disappeared while waiting, rejects unsupported dates/successors atomically and uses an unpredictable legacy key behind the immutable native retry receipt.

Local verification: eight actual-engine database tests, including 24 completion races against reschedule/native edit/archive, plus one real PostgREST API journey passed. Coverage includes partner completion convergence, no successor substitution, RLS/foreign/anonymous access, preseeded legacy keys, full receipt-failure rollback and overflowing recurrence rollback. Scoped lint/format/diff checks passed; disposable security advisors reported no issues. CI and exact-head review are pending. Actual iPhone queue replay, native skip/reschedule and takeover acceptance remain incomplete; M4 is not complete.

Sol review of initial completion-guard head `2f9f84f` found that `NOWAIT` lock errors escaped the AI journal without storing its reported conflict. The wrapper now normalizes `55P03` to `40001`, so the existing atomic journal records a terminal conflict. A real routine-row lock handshake regression proves that the same AI tool call still returns the saved conflict after the lock is released, with no completion or native receipt. All ten focused database/API cases pass after the fix; updated-head CI and rereview are required.

### Online chore skip and reschedule — storage/API slice

PR #65 merged as `d9968dc` after both exact-head CI checks, no review conversations and clean GPT-5.6 Sol medium rereview of `3ce8af1b47698746f5903e71e600ba04ccf7cc80`. The lock-contention AI journal finding was fixed and independently verified before merge. [Merge evidence](https://github.com/drrius/nest/pull/65#issuecomment-5750166348).

The current branch adds gated `nest_change_chore` storage and authenticated `/v1/chores/skip` and `/v1/chores/reschedule` routes. Strict Effect input/receipt schemas bind the original occurrence/date, requested action, actor, household, operation and resulting date/status. These are online-only commands; no offline queue expansion. The audited closure engine preserves history/recurrence identity and updates opted-in reminders. Receipt replay is authorized before lookup and survives a later definition rebuild deleting the original target. New writes require the exact current open occurrence and active/unpaused routine. Internal legacy keys are server-generated; lock conflicts normalize to `40001`.

Local verification: seven real-engine database cases pass, including 12 skip/completion races, immutable concurrent replay, rebuild deletion, stale/paused/archived/preview conflicts, revoked/foreign/anonymous denial, private receipt RLS, malformed dates and transactional rollback with actual reminder candidates. Two actual PostgREST API journeys pass, including committed response loss and partner rebuild before retry. All 95 API cases pass; workspace types, scoped lint/format/diff checks and isolated security advisors pass. CI/review are pending. Native overflow/date selection, calendar-overlap warning, exact native retry and corresponding AI tools remain outstanding; M4 remains incomplete.

### Actual expense transaction — audited fixture candidate

The next M7 slice retains the final legacy expense engine, its ledger lock, immutable retry receipts and actual activity/partner-notice transaction in a disposable fixture. The [expense audit](native-rewrite/money-expense-audit.md) records exact source provenance and the decision to wrap this engine with a strict actor-bound native command. The legacy household-wide retry key can replay another member’s receipt with the same payload; a regression records this limitation so it cannot be mistaken for native authorization.

Six focused database cases pass, including eight concurrent retries, 160 allocation vectors, tenant/anonymous denial, shared ledger locking, and complete rollback after an outbox failure followed by successful retry. Native expense API/UI, actor-bound retry receipts and actual AI approval consumption are not implemented by this fixture. No production database or device was used. Merged as `5d2d517f582bcdcb3754c609f7bc55a182b0b7cf` after successful exact-commit CI `35595501940` and clean Sol medium signoff (six cases independently passed). Main was fast-forwarded and pushed without a PR.

### Native expense transaction and approval boundary — candidate

Gated `20260921114330_native_expense_command.sql` wraps the audited engine with actor/household/operation-bound immutable receipts, strict canonical centime/allocation/date/member validation and unpredictable internal legacy keys. Native Save is direct member authorization. The separate approved execution path requires the exact server approval, bound to operation/invocation, actor, household, command version and payload; approval consumption, actual expense, legacy activity/notice and native receipt commit together. A receipt replays before checking consumed/expired approval, and a changed payload or approval cannot reuse it. Ledger lock acquisition precedes approval expiry validation.

Seven focused real PostgreSQL cases and two contract cases pass, including concurrent native/approved retries, private receipt reads, invalid/foreign/denied/expired/changed approval, failed native receipt rolling back all writes and consumption, approval expiring during ledger lock contention, category archive replay and 1,000 exact percentage-allocation contract vectors. Contract types, scoped lint and disposable security advisors pass. A first standalone contract-test run lacked root-level Effect resolution; it now uses the established API-package resolver and passes. This is storage/contract implementation only: the authenticated Effect API, actual AI proposal/approval flow, native expense form, optional receipts and physical-device verification remain outstanding. Exact-commit CI and Sol review remain pending.

Sol found an active-category race on initial `bf7129117010c903f37e3ae4957b130c2efbc830`: an archive could commit while a new expense waited on the ledger lock. The revision acquires the ledger lock first and holds a share lock on the active category through commit; a real two-session regression now rejects the stale selection. Sol also found note-limit and Unicode validation mismatches. Both boundaries now enforce the retained 4,000-codepoint note limit, 200-codepoint description limit and the same Unicode blank-character rule. The bounded SQL payload allowance includes worst-case valid Unicode text. All 12 focused cases pass after these corrections; updated-head review and CI are required.

### Authenticated expense API — candidate

POST `/v1/money/expense/save` and `/v1/money/expense/execute` now invoke the same strict Effect adapter over the real transaction. The approved route requires approval identity; the direct route rejects hidden approval/origin/actor fields. The API checks complete receipt identity and exact expense content, including normalized invocation/approval IDs, and returns finite failures with no-store responses. The bounded request body accommodates valid Unicode notes. These routes are not yet exposed as model tools; AI proposal/decision UI integration remains outstanding.

Two focused service cases and two actual HTTP → PostgREST → PostgreSQL journeys pass. Both journeys drop the response after commit and retry the same operation, proving one event/receipt and one approval consumption. The direct journey verifies exact derived balance and maximum valid Unicode text; the approved journey rejects pending/missing/wrong-member approval. All 243 API regression cases pass; API types and scoped lint pass. Sol has signed off on corrected transaction commit `dc50c3510ad9ea938b985af05368171e5d11eba9` after independently rerunning 12 tests; its CI is still pending. This API candidate requires its own exact-head CI/review. Native expense form, receipt selection and real AI approval cards remain unfinished; no production or device verification is claimed.

Sol found that initial API head `d7e8c4208747a19c4129c09293552f8b9807434a` accepted uppercase UUIDs in the shared schema but forwarded payer/allocation IDs to SQL unchanged. The revision canonicalizes every expense UUID before dispatch and receipt comparison. A real HTTP fixture uses alphabetic partner/category IDs and proves uppercase input followed by lowercase retry yields the same receipt and one event. All five focused service/HTTP checks pass; updated-head review and CI are required.

### Durable expense approval read and decision — candidate

The approved expense transaction merged as `dc50c3510ad9ea938b985af05368171e5d11eba9` after successful exact-commit CI `35596511057` and clean Sol medium rereview. No production migration was applied.

Gated `20260921120149_native_expense_approval.sql` adds a private, tenant-authorized read of the exact expense approval and its committed receipt, plus atomic native confirm/deny. Confirmation, approval consumption, expense and receipts share one transaction; failure restores the pending proposal. Duplicate confirmation recovers after expiry, duplicate denial stays denied, and concurrent confirm/deny produces one outcome. GET `/v1/money/approval` and POST `/v1/money/approval/decide` use strict Effect schemas and validate owner, approval, operation, exact expense and committed receipt. No decision capability is registered as a model tool.

Five focused PostgreSQL cases, one actual HTTP/PostgREST response-loss/reopen journey and one strict response-validation case pass. These cover private reads, owner/operation/payload denial, concurrent decisions, rollback, durable denial and expired committed retries. API types/scoped lint and disposable advisors pass. AI proposal journal integration and native approval UI remain unfinished; this is not a device-verified financial workflow. Exact-commit CI and Sol review are pending.

### Private AI expense proposal — candidate

The expense API merged as `a314fffe2363873a98607fe5479ce6221e87c1ea` after successful exact-commit CI `35597027934` and clean Sol medium rereview. All five focused service/real-HTTP checks independently passed. No production migration was applied.

`proposeExpense` now uses the private AI command journal to create an exact pending `expenses.record` approval. Actor, household and operation identity remain server-owned; the model receives no Save/execute/confirm/deny tool. Strict member/allocation/centime validation and canonical UUIDs precede proposal creation. The journal commits proposal plus canonical result together, recovers after interruption and replaces invented tool outcomes. Stored proposal results stay historical; actual current status is read through the approval endpoint. The interim native result label says “Expense proposal created · this action posted no money” and links to existing Money reads; it does not provide the still-unimplemented approval screen.

Four focused database cases, one real AI SDK/PostgREST response-loss journey, one receipt-validation case and one native result-label case pass, alongside three expense-contract properties/boundaries. They verify concurrent replay, journal-failure rollback, private/revoked access, hidden approval and malformed direct-RPC identity rejection, invalid allocations, archived-category conflict and canonical interrupted transcript recovery. All 245 API regressions, 28 AI SDK tests, workspace types, scoped lint and disposable security advisors pass. An initial fixture run caught unchanged proposal helper definitions inadvertently included in the new migration; only the three intended registry/dispatcher/history functions are now replaced. Native expense entry, native approval interaction and real-provider/device verification remain outstanding. Exact-commit CI and Sol review are pending.

The durable approval slice merged as `019ca25cd2b773c43fa4f7d5892ab363c1e506f2` after successful exact-commit CI `35597659604` and clean Sol medium review (seven independently passing cases). Main was fast-forwarded and pushed. Sol found that the initial AI proposal card at `dd9e909` asserted a current pending/unposted status even after later confirmation. The revised label describes only the historical proposal action; current approval status remains a separate server read. The focused native card test passes after the correction. Updated-head CI and Sol rereview are required.

### Native expense approval transport — candidate

The account-bound Money client now reads private expense approvals and sends exact confirm/deny commands using expo/fetch. It validates owner, household, approval, operation, complete expense content and the resulting status/receipt; all UUIDs are canonicalized without changing financial values. A lease-checked operation adapter verifies the local account both before dispatch and after the response. It creates no offline financial queue.

Four focused native boundary/lease cases and two real native → HTTP → API → PostgREST → PostgreSQL journeys pass, alongside two Money read-client regressions. Confirmation and denial both recover lost committed responses; reopening reads the canonical terminal result, duplicate confirmation creates one event, and partner/revoked access fails. A revocation test initially attempted to delete a member referenced by the retained ledger; that foreign-key protection correctly rejected the fixture setup. Revocation is now tested on the denied proposal without financial history. Mobile types and scoped lint pass (nonblocking Effect suggestions only). Native screen, durable decision recovery metadata, confirmation interaction and physical iPhone verification remain unfinished; this transport is not a completed user flow. Exact-head CI and Sol review remain required.

The corrected AI proposal commit `a5126edaa33cdc45e5c68fc0c8268ec4192b5da8` has clean Sol medium rereview. CI `35599027982` remains pending, so it has not been merged. The native transport is a dependent branch.

### Durable native expense decision recovery — candidate

The AI proposal change merged as `a5126edaa33cdc45e5c68fc0c8268ec4192b5da8` after successful CI `35599027982` and clean Sol medium rereview. Native transport commit `2c42e4396d099a2e23ef9db6100fb4e0cae89db2` now has successful CI `35599561774` and clean Sol medium signoff (eight independently passing checks); it is ready for the next main fast-forward.

The native recovery runtime writes only approval ID, operation ID and confirm/deny choice to account/lease-scoped SQLite before dispatch. Opposing local decisions cannot overwrite one another. Reopening reads current server status without automatically sending a financial action; an unresolved decision permits only explicit exact retry. Confirmed outcomes clear only matching metadata. Background cancellation hides the private view while retaining durable intent, and foreground reconciliation recovers late commits. Stale alert captures, expired first attempts, current-account denial, metadata failure and mismatched operations cannot dispatch a different expense. Financial content is never persisted in this recovery table or queued offline.

Eight runtime cases, two SQLite isolation/corruption/concurrency cases, two real native-runtime → HTTP → PostgREST → PostgreSQL restart journeys, and four existing native approval boundary cases pass. Confirm and deny both recover lost committed responses across a real SQLite reopen with zero automatic repeat writes. All eight existing offline test files pass after the additive local-table change. Mobile types, scoped lint and formatting pass; an Effect lint error in the test fixture's untyped behavior callback was corrected with its actual finite error contract. The approval screen, exact native confirmation interaction and AI-card navigation are still unfinished; no physical iPhone verification is claimed. This candidate still requires exact-head CI and Sol review.

### Native expense approval screen and assistant handoff — candidate

Native transport `2c42e4396d099a2e23ef9db6100fb4e0cae89db2` merged after successful CI `35599561774` and clean Sol medium review. Durable recovery `d2de0a7d1929ec29a4dbb33eb1839b878ae2c3ff` has successful CI `35600317466` and clean Sol medium review (16 independently passing cases), ready for main fast-forward.

The protected `/expense-approval` screen now opens the exact private proposal from an assistant card, fetches current server status, and displays description, exact CHF amount, payer, both allocations, date, note and category. Native controls present an explicit confirmation alert; stale captured proposals cannot post. Decline, expired, uncertain, confirmed and denied states remain distinct. Exact retries use the durable decision, and confirmed receipts link to retained financial detail. Focus/background changes hide private content and reconcile on return. A subscription owner recreates a fresh runtime after cleanup, including Strict Mode resubscription.

A shared tenant-authorized category API and corresponding AI read tool expose only the selected category's current name/archive state. The gated migration grants just the four required columns and retains the audited member RLS. Foreign/missing category IDs return no category; unavailable or archived selections block initial confirmation without blocking decline or recovery of an already-attempted decision. No category editing or offline financial queue is added.

Twenty focused native/display/owner/receipt/runtime/API cases and four actual HTTP/PostgREST/PostgreSQL journeys pass, including AI journal → assistant card → native approval owner → exact explicit confirmation, category names/archive/RLS with minimal column grants, and both SQLite restart response-loss recoveries. Disposable security advisors report no issues. Workspace types and scoped lint pass; initial complexity findings were fixed by extracting named validation predicates, and render-time clock access was replaced by an expiry timer. iOS export passes at `/tmp/nest-expense-approval-screen-export` (bundle `963cae7d5126a5f77d890fe2174620a7`), which proves packaging only. Alerts, navigation, focus behavior, large text, VoiceOver and real iPhone interaction remain unverified. Native expense entry, receipts, settlements/corrections/refunds and remaining M7 flows are still incomplete. Exact-head review and CI are required for this candidate.

Final regression verification also passes all 246 API cases, 27 Money cases and 37 assistant cases. The first API regression run was stopped after the restricted environment could not open a test's localhost listener; rerunning with local-socket permission passed. No test failure was bypassed, and no production service was contacted.

### Direct native expense draft and Save transport — candidate

Durable recovery `d2de0a7d1929ec29a4dbb33eb1839b878ae2c3ff` merged after CI `35600317466` and clean Sol medium review. The native approval screen `dae7e66dd89eca6441804f39a2fb505f5c7e56d7` now has successful CI `35601617436` and clean Sol medium review (24 independently passing checks, including four actual integration journeys), ready for main fast-forward.

Direct expense draft parsing now uses the audited shared exact CHF arithmetic for equal, exact and percentage splits. Equal division assigns the odd centime to the payer; percentage input remains tied to the first displayed household member when the payer changes, with largest-remainder allocation and payer tie-breaking. All values become canonical decimal centime strings before the shared ExpenseInput contract. Dates, Unicode text limits, current member choices and optional category identity are validated; unsupported amount notation and incomplete splits remain invalid rather than rounded or inferred.

The account-bound native Save client calls the existing authenticated direct-save route, rejects hidden approval fields, canonicalizes UUIDs through the same helper as native approval, and accepts only an exact actor/household/operation/payload receipt with no approval ID. Eight native/property/receipt regression cases and three actual HTTP/PostgREST/PostgreSQL journeys pass, including 1,000 generated vectors across all split modes/both payers, exact stored allocations, one receipt after response loss, and changed-operation-payload rejection. Initial test assertions used an incorrect retained allocation column and expected conflict instead of the SQL's explicit invalid-command error; both assertions were corrected against the audited schema/transaction and the journeys rerun successfully. Mobile types and scoped lint pass; a function-complexity finding was fixed by extracting exact-share validation.

This increment provides validated draft and transport building blocks, not a completed expense-entry screen. Direct Save restart recovery, category selection, native controls/keyboard and physical iPhone verification remain outstanding, along with optional receipts and later Money workflows. Exact-head CI and Sol review are required.
