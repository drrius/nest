# Nest progress

Updated 26 September 2026. Authority: [product brief](native-rewrite/product-and-design.md), [architecture](native-rewrite/architecture-audit.md), [implementation plan](native-rewrite/implementation-plan.md).

**Most feature code is implemented; no M0–M9 milestone has satisfied all exit criteria. No actual iPhone journey has run.** Database and HTTP verification uses disposable synthetic data. Bundles, mocked providers and Node SQLite tests are not device acceptance.

## Current delivery state

- **Isolated hosted test project:** owner authorized creation of `nest-test` in `drrius's Org`. Supabase quoted USD 0/month; project `tkjixmujjoustdiedfmw` in Zurich (`eu-central-2`) is `ACTIVE_HEALTHY`. The initial 55 legacy and 204 native migrations are installed in recorded batches, followed by eighteen conflict-handling corrections (277 source migrations total) (the hosted setup includes pg_net, excluded from the 54-legacy local rehearsal). Eight legacy cron registration blocks were omitted; search indexes were built without CONCURRENTLY for the empty-project transactional API. Failed initial cron/index batches rolled back before retry. Zero cron jobs were registered, and every public table has RLS. One fictional two-member household and a separate nonmember Auth account are now configured. Source hashes and adjustments are recorded in [the test manifest](native-rewrite/nest-test-migration-manifest.csv). Hosted advisors report 58 informational policy-absence findings and 80 callable SECURITY DEFINER warnings; privilege review is pending. Real password Auth sessions pass the Nest member/nonmember/anonymous checks and hosted household RLS reads; Apple and device acceptance remain pending. Ignored local env files contain only the test URL/publishable key. See [hosted setup evidence](native-rewrite/nest-test-setup.md). Production `household-os` was not changed. Owner confirms access to a Mac, Apple Developer membership and two test iPhones; signed builds and device execution remain pending. GitHub OAuth now includes `workflow`; the prepared core integration CI change is being brought forward for current-commit verification/review.

- **Merged offline cutover implementation:** owner-only frozen rotation enables epoch enforcement without resetting financial history. Receipt-first chore/grocery adapters preserve existing function identities and deny client execution of retained cores. After activation, old signatures cannot create epochless operations. Online AI dispatch uses the guarded path with a control-row lock.
- **Captured native intent:** atomic authorized snapshots return data plus epoch. SQLite retains the displayed epoch, including an additive upgrade of the chore cache; replay never replaces it with newer metadata. Stale unreceived intent becomes a retained conflict requiring explicit review. Previously committed operations recover their original receipts after rotation.
- **Local verification:** all 371 API cases pass (`/tmp/nest-epoch-final-api.log`) and full typechecking passes (`/tmp/nest-epoch-final-types.log`). Thirteen affected HTTP integration cases pass (`/tmp/nest-epoch-affected-integrations.log`). Five grocery HTTP cases cover stale intent and lost-response recovery; two chore HTTP cases cover the equivalent paths. Ten chore-flow cases include cache upgrade without rewriting pending intent. Seven database snapshot cases include real concurrent snapshot/rotation races. The full synthetic schema rehearsal proves post-rotation journaled AI commands and returns empty security advisors (`/tmp/nest-epoch-final-rehearsal.json`).
- **Review follow-up:** The cutover implementation and AI boundary fix are committed on `codex/native-acceptance-continuation`. Sol found that native `offlineEpoch` leaked into two model-facing schemas. The fix omits it from both AI inputs; 11 actual SDK/HTTP integration cases pass, including rejected epoch-bearing calls with no journal entry or queued mutation and valid saved history (`/tmp/nest-epoch-ai-schema-fix.log`). Scoped lint and contract typechecking pass. The AI boundary fix is committed as `47ab04d`; Sol independently repeated all 11 integration cases. The fix is included in merged `4c19bb5`, with clean review and successful CI. Full lint and formatting passed. These are local synthetic and Node SQLite results, not Expo SQLite, live-provider or iPhone acceptance. External worker drainage and complete cutover recovery remain separate unfinished work.

- **Merged epoch implementation:** `4c19bb5` has clean exact-commit Sol review and successful CI run `36189848222`; it is merged. No deployment occurred.
- **Independent chained-replay follow-up:** five grocery HTTP/PostgREST/SQLite cases pass (`/tmp/nest-epoch-dependent-rebase.log`). After a lost acknowledgment, a queued opposite action survives restart and rotation: recovery rebases its expected item version from the predecessor receipt but retains the old epoch, so the server refuses it as a cutover conflict. Later sync sends no retry; explicit discard clears it. The initial extended fixture incorrectly expected full sync success while the mutation RPC remained revoked; correcting that expectation preserves the intended unavailable state for the unreceived dependent action. No application change was needed. Merged in `84abc1d` after clean exact Sol review and successful CI `36190261452`.

- **Prepared legacy RPC callers:** four focused PostgreSQL cases pass (`/tmp/nest-epoch-prepared-caller.log`). For chores and groceries, a session prepares and executes the old public command before adapter installation, commits its receipt, then stays connected across migration/rotation. The original private function OID remains identical; the warmed prepared call returns the exact prior receipt and refuses a new epochless operation. Receipt/completion counts remain one. Scoped lint passes. This verifies cached SQL callers, not hosted PostgREST pool rollout or external-worker drainage. Merged in `84abc1d` after clean exact Sol review and successful CI `36190261452`.

- **Recovery snapshot follow-up:** the disposable recovery freeze now retains the two current authorized epoch snapshot RPCs. The complete 54/204 rehearsal preserves both members' exact nonempty chore/grocery snapshots and epochs, rejects outsider/anonymous/service-role reads and reconciles financial history after all recovery checks. Report: `/tmp/nest-epoch-snapshot-recovery.json`; security advisors are empty and scoped lint passes. This is SQL fixture evidence, not native screen/device acceptance. `9fe6d16` is merged after clean exact Sol review and successful CI `36190514662`.

- **Combined control rehearsal:** the epoch AI probe now pauses native recurring execution and eight known legacy job entry points, checks the pause guards, freezes household writes, fences legacy public APIs/table writes, rotates and resumes before exercising authenticated journaled chore/grocery commands. Exact replay remains single-effect. The full 54/204 fixture passes with empty advisors (`/tmp/nest-combined-cutover.json`); rollback restores epoch, recurring/job controls and public function/table/column ACLs. Scoped lint passes. This single-transaction fixture verifies composition, not a committed hosted activation or external request drainage. The recovery snapshot change `9fe6d16` is merged after clean Sol review and successful CI `36190514662`. `2c201a8` is merged after clean exact Sol review and successful CI `36190805156`.

- **Committed cutover/recovery fixture:** a disposable owner-only activation now commits recurring/legacy job pauses, the legacy API/table fence and epoch rotation before new native history is written. Chore/grocery fixture commands use the captured epoch. The existing 24-new-financial-event recovery matrix then commits its freeze and retains exact financial history, receipts, approvals, private journal and member snapshots. Full 54/204 rehearsal passes (`/tmp/nest-committed-cutover-recovery.json`), with empty security advisors and scoped lint. This closes the local committed-control composition gap, not external HTTP/Storage drainage, actual scheduler inspection, hosted activation or device acceptance. Merged through `4eded66` after clean exact review and successful CI `36192211844`.

- Merged cutover implementation: `4eded663ecad8e2741f4eb56a4ab4af4de3dab70`, with clean exact-commit Sol review and successful CI `36192211844`. Later documentation commits do not change this implementation evidence. Feature branch: `codex/native-acceptance-continuation`.
- Main includes account/notification assistant handoffs, settlement approval and direct-save recovery, the mobile CI selection guard, and the corrected meal-context privacy assertion. The failed substring assertion could match digits in an opaque hash; its replacement checks the decoded requester goal and exact profile field allowlist.
- Receipt device handoffs `54566d2` are merged after clean exact Sol review and successful CI `36176773233`. Migration 195's owner-only barrier, its maintenance-error fix and financial-detail recovery are also on main. No hosted migration was applied.
- Refund/correction approval recovery `cb37b93` is merged after clean exact review and successful CI `36177812713`. Initial recurring recovery `a7231ce` is also merged after successful CI `36178100012`; all recurring variants `0b3ec94` merged after successful CI `36178393036`; legacy approvals `37faf50` merged after successful CI `36178712336`; direct legacy recovery `488127d`, native Save recovery `2162d1c`, terminal approval recovery `93c93f2`, and pending approval recovery `b6a1a93` are now merged after their exact commits passed CI and clean Sol review.
- The latest GitHub PR check found no open PRs. The owner authorized reviewed feature branches and fast-forward main without PRs. Exact-commit CI and clean Sol medium review still gate merging. No deployment follows a merge.
- The continuation automation remains removed. No purchases, production migrations, scheduler activation, deployment, app retirement or release publication have occurred.

## Latest local verification

- **Refund/correction conflicts:** four cancellation/stale-review guards now use PT412. Thirteen local HTTP/native-runtime cases pass, covering event-history preservation, approvals, replay and SQLite restart recovery. Installed on nest-test; catalog confirms guards and financial events remain zero. Hosted execution/device checks remain outstanding.

- **AI turn conflicts:** seven terminal ownership/revision/deadline guards now use PT412. Two new local HTTP cases and the existing maintenance recovery case pass, covering no-write conflict rejection and exact interruption recovery. Installed on nest-test; catalog checks and a rolled-back hosted SQL probe pass. Hosted HTTP/live AI/device verification remains pending.

- **Calendar consent conflicts:** five terminal stale/expired guards across consent/capture/publication now return PT412. Six local HTTP cases pass, including delayed calls after opt-out, personal metadata rejection, membership reincarnation and replay. Hosted stale identities return promptly; enabled consents and snapshots both remain zero. EventKit/device acceptance remains pending; other command families still need audit.

- **Preference conflicts:** explicit stale versions for food, cooking and notification settings now use PT412. Ten local real-HTTP cases pass, including privacy/tenant/revocation and lost-response replay. All three hosted stale requests returned promptly (0.283/0.081/0.077 seconds), with no preference or notification opt-in changes. AI, meal and other conflict families remain under audit.

- **Combined conflict verification:** the full disposable rehearsal applies 54 legacy and 211 native migrations (pg_net explicitly excluded), preserves financial fingerprints and committed recovery receipts, and returns empty local security advisors. Report: `/tmp/nest-conflict-rehearsal-advisors.json`. This is synthetic infrastructure, not hosted/device acceptance. CI `36233299007` failed because the expanded AI test reached 403 lines; fixture setup is now extracted, all 16 AI command cases pass, and full lint/format checks pass. No assertions were removed; fresh exact-commit CI is required.

- **Expense conflict follow-up:** all three unavailable-receipt/category and cancelled-Save branches return PT412 without posting in a real local PostgREST regression. Hosted unavailable-category Save returned 409 conflict in 0.333 seconds; financial-event count remains zero. Eighteen focused AI/receipt/expense cases pass after correcting shared-fixture leakage of grocery-only migrations. That fixture regression was introduced by the earlier grocery test updates and is fixed on the branch.

- **Hosted settlement conflict:** a stale expected balance returns 409 conflict in 0.336 seconds; hosted financial-event count remains zero. The additive correction covers stale balance and cancelled Save only, preserving authorization, locks, approvals and receipts. Eleven local HTTP/native-runtime cases pass, including no-post conflict checks and lost-response Save/Cancel recovery. Production and device acceptance remain gated.

- **Hosted chore journey:** fictional daily routine creation/replay, stale completion conflict (0.102 seconds), valid completion/replay, and outsider rejection pass against nest-test. The additive retained-core correction preserves receipt/epoch handling and changes only three explicit business-conflict raises. Local PostgREST regression asserts raw PT412 and zero receipts for stale intent. Review also found a differently formatted paused/archived routine guard; its precise follow-up preserves lock-contention retries, and the current guarded HTTP fixture verifies raw PT412 with no receipt. Actual iPhone execution remains pending.

- **Hosted grocery journey:** real test Auth users exposed stale-edit serialization retries and an API timeout. An additive migration changes explicit grocery business conflicts to PT412, mapped to the existing 409 conflict contract. Applied only to nest-test; add/replay, partner check/replay, outsider denial, stale conflict and final state now pass through the actual hosted backend. Focused local HTTP tests and API typecheck pass. The AI journal recognizes PT412 and preserves a single replayable conflict result; all 16 focused AI command database cases pass. Stale hosted check/uncheck now returns conflict in 0.108 seconds after a further additive test-only migration; 17 focused database/HTTP cases pass. Catalog audit found explicit 40001 business conflicts throughout other commands; those need further correction without breaking internal exception handlers. no phone acceptance is claimed. See [test setup](native-rewrite/nest-test-setup.md).

- **AI turn maintenance/resume:** `tests/integration/ai-turn-freeze-recovery.test.mjs` passes against disposable PostgreSQL and real PostgREST through the API handler. An expired turn remains unchanged when finalization is refused during the write barrier; partner recovery is denied. After unfreeze, interruption advances the conversation once and repeated recovery returns the same result. Scoped lint/format pass. This narrow fixture has no journaled action, external worker or device; it does not establish full cutover recovery. Merged as `d3a5f0d` after clean exact Sol review and successful CI `36194995433`.
- **Recovery rehearsal:** all 54 legacy and 204 native migrations apply to disposable fixtures. The latest full report is `/tmp/nest-committed-cutover-recovery.json`; security advisors report no findings. Both members retain identical balance/history/expense detail after the committed freeze; outsiders are refused. The final all-household financial snapshot reconciles after all recovery probes. This does not prove hosted infrastructure or complete cutover recovery.
- **Write barrier:** the default-open migration protects current public/private tables; only the database owner can freeze/resume. Coverage validation rejects missing/disabled guards. Five database cases exercise in-flight transaction drainage, retained reads, denied writes/truncation, role restrictions and pre-existing transaction isolation. Auth/Storage, external requests, owner jobs, concurrent DDL and actual device reconciliation remain outside this barrier-only proof. The epoch tests above separately cover queued intent.
- **Offline maintenance handling:** a real HTTP/PostgREST/SQLite test reproduced that generic SQLSTATE 55000 became a grocery conflict. The unapplied migration now raises `PT503`, classified as unavailable. Grocery and chore intent remains pending through freeze/restart and resumes without duplicate effects. Ten focused cases pass across grocery, chore and barrier tests: `/tmp/nest-freeze-grocery-after.log` and `/tmp/nest-freeze-chore-after.log`.
- **Settlement recovery:** five HTTP/PostgREST/SQLite cases cover consumed/denied approval restart, unresolved staged approval, and direct Save/Cancel restart. Terminal authoritative reads clear metadata without repeating writes; unknown intent remains durable and cannot be replaced by its opposite. Reports: `/tmp/nest-settlement-unresolved-http.log` and `/tmp/nest-settlement-save-freeze-http.log`.
- **Receipt handoffs:** registered tools reauthorize membership and return fixed links to expense entry, grocery expense entry and upload review. They do not choose files, upload/read/remove receipts, grant approvals or post money. Twenty-two focused cases, API/mobile typechecks and scoped lint/format pass. Report: `/tmp/nest-receipt-handoff-tests.log`. Live model selection, actual picker/navigation and device file handling remain unverified.

All reports above use synthetic data. `completeRecovery`, `externalRequestsDrained` and `ownerJobsStopped` remain false. Detailed preceding candidate evidence is preserved at `54566d2651ea8a9c077f7aedccc136608d08a08f:docs/progress.md`; its pending statuses are historical.

## Earlier recovery evidence

The merged recovery work covers direct financial Save/Cancel receipts, pending/consumed/denied financial and legacy approvals, private AI journal preservation, meal reservation/proposal/edit recovery, and receipt upload inventory. Real local HTTP/PostgREST/SQLite journeys distinguish committed recovery from unknown requests requiring explicit retry. Storage bytes, live models and physical-device behavior remain unverified.

Detailed per-family reports and assertions are preserved at `2c201a88950e0bd51e8b6c745ae1f41204e7114f:docs/progress.md`. Pending statuses in that historical log describe their original verification time; use this checklist for current delivery status.

## Next work

1. Continue end-to-end acceptance against the current [action source map](native-rewrite/action-inventory.md). The registry/routing audit now links action families to native entries, registered tools/shared boundaries and representative tests; it does not establish live execution or device acceptance.
2. Complete hosted cutover validation and remaining online pending-command reconciliation, including external writer drainage. The committed lifecycle is locally rehearsed with synthetic data. Offline epoch enforcement and receipt recovery are implemented and locally verified; preserve new financial history during recovery. The bounded fixture below is not complete recovery.
3. Configure an approved isolated backend/provider and run live integration once configuration is supplied.
4. Execute real iPhone/two-member acceptance and a release-mode build, fix findings, then prepare a concrete release/cutover approval package.

## Milestone checklist

- [ ] **M0 — Decisions and execution route.** Tooling, delivery ADR, action inventory, exact dependency pins and linked EAS app identity implemented. CI enforces formatting, lint, typechecking and focused tests. Native smoke procedure exists but has not run. Exit still needs an executable iPhone development build and recorded native evidence.
- [ ] **M1 — Quiet native interactions.** Four-tab development preview, generated artwork, sample chore/checklist interactions, meal replacement and calendar toggles implemented. Every preview uses identified fictional data; release guards prevent presenting it as working data. Owner review, large text, VoiceOver, Reduce Motion and real keyboard/navigation checks remain.
- [ ] **M2 — Authenticated offline/AI slice.** Bearer membership API, chore read/completion HTTP and shared AI tools, transactional receipts, SQLite journal, Effect/AI SDK boundary, durable approval records and owner-private transcript persistence implemented. Native Today → outbox → API integration is merged in PR #15. Cold-start identity recovery is merged in PR #23 and locally verified. Private native chat and SDK/server lifecycle are merged in #25–#27; atomic household command journaling is merged in #28. Mutating assistant tools and canonical native receipt links are merged in #29. Live provider/structured generation, approval-resume integration and an actual native retry/restart journey remain.
- [ ] **M3 — Identity, onboarding and settings.** Native Apple sign-in, SecureStore session adapter, refresh/membership verification and retryable logout merged in PR #14. Private food-profile storage and API are merged in #30–#31; the native form and exact save recovery are merged in #32. Journaled AI food settings are merged in #33; shared cooking storage/API is merged in #34; the native cooking form is merged in #35 and corresponding journaled AI actions are merged in #36. Private memory storage/API/native management/AI actions are merged in #37–#40. Calendar consent/API/native sharing/AI availability are merged in #41–#43. Notification preference storage/API/native controls are merged in #44–#45; corresponding AI actions are merged in #46. Independent quick-start/comprehensive setup and assistant handoff are merged in #47. Meal-entry setup prompts and links from all five reminder editors to notification enrollment are merged. Actual OS permission/enrollment and onboarding acceptance remain unverified. Existing-member Apple linkage and Keychain behavior need device verification.
- [ ] **M4 — Today, chores and groceries.** Real authenticated Today chores, online routine creation/edit/pause/resume/archive/skip/reschedule and corresponding journaled AI actions are merged. Native handover request/recipient acceptance/decline, one-statement coherent reads and readonly offline cache are merged through #71; corresponding private AI handover tools are merged in #72. Groceries support online add/edit/remove, inline add, checked collapse and offline checks. Account-level reconnect and cold-start identity recovery are merged. Grocery grouping is merged in #74; queue/receipt retention bounds are merged in #73. Today now composes real meals, renewals, calendar, variable bills and private financial confirmations; saved-summary discovery is merged. Final aggregation usability and actual two-device fault/acceptance journeys remain. Node SQLite and real PostgreSQL/HTTP evidence do not establish device execution.
- [ ] **M5 — Meals and week planning.** Week board, manual placement/move/replacement/removal, saved-recipe CRUD/selection, leftovers and linked preparation have storage, authenticated API, native UI and private AI implementations. Generated private proposals and explicit exact-revision approval are merged across storage/API/native. Single-suggestion replacement and exact saved-choice generation, durable edits and API are merged; native controls are merged after CI and independent review. Assistant proposal actions and native preview handoff are merged after CI and independent review; separate ingredient review, pantry exclusion and idempotent grocery addition are also merged with API/native/AI coverage. Real provider compatibility/dietary suitability and the full seven-day journey on an iPhone remain unverified.
- [ ] **M6 — Calendar and availability.** Read-only Expo adapter and consent-bound sanitized server snapshots implemented separately. Consent incarnation invalidates delayed requests across membership removal/rejoin. Authorized API, native selection/consent/publisher and assistant availability reads are merged in #41–#43. Local selections and pending consent are account-leased; foreground/reconnect publishing uses a 31-day horizon and 15-minute expiry. Authorized busy snapshots now feed proposal generation and preparation overlap warnings. Personal agenda and partner busy UI are merged with local/controller/HTTP verification. Production Calendar route integration and optional chore/renewal layers are implemented and merged with local verification. Two-device privacy/permission inspection remains; foreground refresh is the implemented behavior and background freshness is not promised.
- [ ] **M7 — Money and recurring automation.** Balance/history/detail, equal/exact/percentage expenses, full/partial settlements, append-only corrections/refunds, receipt selection/upload/recovery/removal, private AI proposals/confirmation and durable financial recovery have local implementations merged after CI/review. Recurring configuration, pause/cancel/resume and their private AI approval flows are merged. Fixed-cycle transaction invariants and explicit variable-cycle entry/Save recovery are merged; variable private approvals and AI proposal handoff are merged after CI/review. Explicit manual linkage, bounded scheduler execution/checkpoints and native cycle history are merged. Legacy rule/draft inventory, direct dismissal and private AI dismissal approvals are merged. Retained draft confirmation and explicit native/AI adoption with old-writer fences are merged; full migration reconciliation remains. Hosted scheduler activation, full migration reconciliation, hosted Storage/provider validation and native device acceptance remain. No scheduled production posting is active.
- [ ] **M8 — Renewals, reminders and push.** Renewal CRUD and reminder editing, corresponding AI actions, all six reminder sources, deterministic summaries, bounded scheduling/checkpoints, push enrollment/recovery/logout handling, delivery tickets/receipts/invalid-token handling and protected notification links are implemented with local evidence. Saved-summary discovery is merged. Hosted workers, real permission/token lifecycle, APNs delivery and cold-launch navigation on an iPhone remain unverified. No actual device enrollment/delivery is claimed.
- [ ] **M9 — Migration and release rehearsal.** The full legacy/native chain and representative synthetic reconciliation are locally verified with explicit infrastructure exclusions. Immutable renewal conversion provenance, retry/rollback and partial shopping/retention cutover are implemented; the latter preserve exact financial history after rehearsal rollback. Complete writer drainage/cutover, committed activation/recovery across the implemented offline epoch, durable rollback preserving post-cutover financial events, authorized existing-data rehearsal, both-member usability acceptance and a release-mode native build remain. Production migration/cutover and TestFlight/App Store publication require separate owner approval.

## Verification evidence and limits

| Area                          | Evidence                                                                                                                                                                                                                                                          | Remaining gap                                                                                                   |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| Required CI                   | Merged cutover implementation `4eded66`: successful run `36192211844`, clean Sol review. Existing workflow checks format, lint, types, focused tests and database suite.                                                                                          | Later branch commits require their own passing CI.                                                              |
| Mobile unit selection         | Expanded notification/push/reminder command: 47 cases; session/sign-out: 33 cases locally passed. Guard candidate checks every root mobile unit file is selected exactly once, including a negative omission probe.                                               | Nested HTTP integration tests are separate; selection does not prove runtime behavior.                          |
| Account/notification handoffs | 18 focused cases passed at `d6e9144`; API/mobile types and scoped lint/format pass. Missing/revoked membership denied, only identity/member GETs, fixed successful destination required. Account settings reaches explicit native sign-out via Household account. | Live model selection, native navigation and device permissions unverified.                                      |
| Setup entry                   | Meal setup: five lifecycle cases plus real HTTP/PostgREST journey. Reminder links: destination enrollment/client tests; Sol repeated 12 relevant cases.                                                                                                           | Actual navigation, form-return preservation, keyboard and accessibility unverified.                             |
| Offline recovery              | Real SQLite/native runtime → HTTP → PostgREST cases cover lost responses, restart, actor isolation and recovery while writes are suspended for chores/groceries and expense Save/Cancel.                                                                          | Expo SQLite, actual process death/network reconnection and two-device races unverified.                         |
| Money/auth/privacy            | Focused domain properties, SQL/RLS, concurrency, rollback, approval and transport tests exist across implemented slices; required CI runs them.                                                                                                                   | Synthetic Auth does not establish Apple linkage; local Storage metadata does not establish hosted bytes/access. |
| Migration                     | Full 54-legacy/204-native chain locally rehearsed; exact financial snapshots, retained records, public API fence, eight legacy job pause entry points and recovery probes.                                                                                        | One explicit `pg_net` exclusion, simulated Auth/Storage, no actual cron or dispatched-HTTP drainage.            |
| Native                        | iOS exports and prepared smoke/manual protocols exist in [native verification](native-rewrite/native-verification.md).                                                                                                                                            | Zero physical-iPhone, EventKit, Keychain, APNs, usability or release-binary acceptance.                         |

The committed recovery fixture preserves direct expense, settlement, refund, correction, recurring configuration/state/variable-cycle receipts and pending/consumed expense approval reads. Original owners recover exact results; partner/outsider behavior is checked; new financial writes remain refused. Chore/grocery receipts and full routine/financial snapshots survive the freeze. Final financial comparison runs after recovery probes.

Latest full fixture report: `/tmp/nest-committed-cutover-recovery.json`; account tests: `/tmp/nest-account-handoff-tests.log`; typechecks: `/tmp/nest-account-api-types.log` and `/tmp/nest-account-mobile-types.log`. Temporary reports are local evidence, not durable release artifacts. Reproduction and bounded coverage are documented in [migration rehearsal](native-rewrite/migration-rehearsal.md).

`completeRecovery`, `externalRequestsDrained` and `ownerJobsStopped` remain false. The financial and legacy receipt/approval families described above now have bounded recovery coverage. Offline epoch enforcement, receipt recovery and a committed synthetic cutover lifecycle are implemented and locally verified. Remaining online command/context recovery, attachment bytes and external worker drainage still require reconciliation. Private AI journal rows survive the freeze, but running-turn finalization remains suspended. Refusing writes and retaining receipts alone does not prove complete rollback.

## Exact external blockers / owner inputs

- **Development backend and AI:** owner-authorized Free project `nest-test` (`tkjixmujjoustdiedfmw`, Zurich) is healthy with all 284 source migrations installed and fictional Auth/member fixtures verified. Configure existing-member Apple test identities, server-only planning credentials and live streaming/structured generation. Never target production `fdtqmcfwhbddswdpnmcq` during test setup.
- **iPhone execution:** owner confirms a Mac, Apple Developer membership and both partners' iPhones are available. This Linux host still lacks `xcrun`/Maestro, and EAS Simulator reports unavailable for `drrius-dev`. Need a signed development build and Mac/device execution, with Apple/APNs configuration. No release publication or paid service is authorized.
- **Hosted workers/Storage:** isolated provider execution, receipt upload/read, scheduled cycles, notification ticket/receipt handling and actual APNs delivery remain unverified. No production scheduler activation is authorized by source merges.
- **CI integration:** GitHub OAuth now includes `workflow`. The prepared pinned PostgREST/core HTTP workflow is restored on the current branch. All ten selected cases pass locally (`/tmp/nest-core-integration-current.log`), after adding the missing current completion RPC migrations to the chore fixture. The original fixture returned unavailable because its RPC was absent; authorization expectations remain unchanged. Commit `159efb8` is merged after clean exact Sol review and successful full CI `36231898809`. Later conflict corrections remain on the feature branch pending their own gates.
- **Existing-data rehearsal and release:** representative synthetic fixtures cannot prove exact real-household reconciliation. Bounded existing-data access, production migration/cutover, old-app retirement and TestFlight/App Store publication need separate explicit approval after concrete preparation.
- **Review:** Greptile exhausted its trial allowance. Owner-authorized GPT-5.6 Sol medium review works and is not a blocker. No credits purchased.

## Historical evidence

Detailed incremental verification/PR history previously in this file is retained at Git commit `d6e91440c0df6316070ec06daec073afd2bd6a22:docs/progress.md`. It includes time-specific candidate and pending states and must not be read as current status. This checklist replaces that chronological log so superseded blockers do not obscure the remaining work.

AI refund/correction conflict audit: the public command journal catches their stale-review exceptions and commits `{ok:false,code:"conflict"}`. A real PostgREST HTTP regression verifies both return HTTP 200 structured conflicts, exact replay produces only two journal rows, no approval is created and event history is byte-for-byte unchanged. Ten existing proposal database cases also pass. No additional SQL replacement is needed for these private proposal helpers.

Historical full synthetic rehearsal (`/tmp/nest-financial-conflict-rehearsal.json`): 54 legacy/216 native migrations, seven events/eight allocations/14 ledger entries and retained receipt reference reconciled, committed recovery passed, local advisors returned no findings. One pg_net exclusion and simulated Auth/Storage remain; external drainage and complete recovery remain unproven.

Private memory conflicts: migration `20260926095607` changes three terminal stale-state/expired-consent/capacity raises to PT412. Six local HTTP cases pass, including raw expired-consent rejection with no memory/receipt writes and approval still pending, privacy, exact replay, stale revision and capacity recovery. Installed on nest-test and catalog counts confirmed; hosted execution and native-device behavior remain unverified.

Routine CI now includes `pnpm test:integration:conflicts`: 20 local PostgreSQL/HTTP cases covering terminal conflicts, approval rollback, AI journal replay, calendar opt-out and memory privacy. The exact selection passed locally in 10.2 seconds with concurrency two; current-commit CI is still required. This does not add phone E2E or hosted-service checks.

Chore edit/handover conflicts: migration `20260926095847` changes nine terminal guards across five retained functions, preserving both chore edit/transfer lock-contention handlers as retryable. Six local HTTP cases pass: stale date/request/version raw PT412 with unchanged occurrence/assignment/pause state, acceptance, exact replay after rebuild, tenant and revoked-access denial. Hosted catalog confirms nine terminal guards and the two preserved contention guards; hosted behavioral/device checks remain outstanding. Both fixture files are included in routine conflict CI (26 cases total).

Renewal/reminder conflicts: migration `20260926100146` changes three terminal stale-baseline guards across the renewal edit and public reminder Save functions. Seven local HTTP/client cases pass, including raw PT412 with unchanged renewal/no reminder or money, tenant denial, cancelled requests, exact lost-response replay and forged-receipt rejection. Installed on nest-test and catalog counts confirmed. Hosted execution/native-device checks and the separate legacy conversion command remain unverified. The raw regression is included in routine conflict CI (27 cases total).

Hosted follow-up verification (after `58e0436`): actual API → nest-test requests for stale chore skip, missing-memory stale removal, stale renewal Save and unavailable-renewal reminder Save returned HTTP 409 in 0.122/0.084/0.096/0.094 seconds. Catalog checks confirm zero financial events, memories, memory receipts, renewals, renewal reminders and chore-change receipts. This proves those negative paths against the hosted backend, not complete successful flows or phone behavior. Local harness: `/tmp/nest-hosted-terminal-check.py`. Sol approved exact `58e0436505b45f5b733cef5d81ab9838ef2960b4`; expanded conflict suite passes 27/27 independently. CI `36233940725` passed the earlier `6576b3f` commit; latest-commit CI remains pending.

Recurring conflicts: migration `20260926100455` changes 23 explicit terminal guards across nine retained configuration/state/resume/variable/fixed/manual functions. Three focused integration cases exercise five stale HTTP command paths, the fixed-cycle SQL boundary, four cancelled Save paths, exact manual-link replay and valid variable posting with zero ledger sum/no duplicate cycle. Full synthetic 54-legacy/220-native schema, financial reconciliation, committed recovery and local advisor gate pass (`/tmp/nest-recurring-conflict-rehearsal.json`). Installed on nest-test and all 23 guard counts verified; actual hosted recurring execution remains unverified and no scheduler was activated. Added to routine conflict CI (30 cases total). Legacy adoption/conversion and other remaining command families still require audit.

Meal edit conflicts: migration `20260926100816` changes 14 terminal guards across six retained placement/move/replacement/removal/leftover functions, preserving their five original exception-handler translations. A local HTTP journey verifies five stale requests return PT412 with identical meal and linked-preparation rows, fresh placement replays exactly, and a fresh removal closes the linked preparation and replays. The earlier full synthetic rehearsal (`/tmp/nest-meal-conflict-rehearsal.json`, 54 legacy/221 native) passes financial reconciliation, committed recovery and the local advisor gate; prior infrastructure/device limitations still apply. Installed on nest-test; catalog verifies 14 terminal guards and five retained handler guards. Hosted movement, replacement, leftovers and preparation behavior remain unverified. Added to routine conflict CI (31 cases total).

Hosted meal/memory follow-up after `bf044e4`: actual API → nest-test meal placement and exact replay, partner read, stale removal (409 in 0.081s), outsider denial, current-revision removal and replay pass. Original visible meal entries are restored; synthetic tombstone/receipt history remains. Private memory proposal remains inactive before explicit confirmation; confirmation/replay, partner denial and nonvisibility, stale removal (409 in 0.084s), removal/replay and original visible list restoration pass. An initial memory probe used forbidden revision zero and correctly returned 400; it was cleaned up through the normal removal command and rerun with stale positive revision 99. Harnesses: `/tmp/nest-hosted-meal-check.py`, `/tmp/nest-hosted-memory-check.py`. These are real hosted API journeys, not native-device proof.

Recipe conflicts: migration `20260926101436` changes 21 terminal guards across ten retained CRUD/library/capture/selection functions while preserving four existing handler 40001 translations and all numeric-validation tails. A local HTTP journey verifies create replay, five stale writes/selections with exact library/ingredient/entry/snapshot preservation, stale planned reads, and successful archive without changing the already captured plan. Installed on nest-test; catalog confirms all 21 terminal and four retained guards. Hosted recipe behavior and device execution remain unverified. Added to routine conflict CI (32 cases total).

Ingredient review conflicts: migration `20260926101925` changes five terminal guards across review/add/source functions and splits missing/ambiguous-source handling from retryable lock contention. Six terminal guards and two retained retryable handlers are confirmed on nest-test. A local HTTP journey verifies stale review/add, missing meal source, and missing ingredient after valid batch entries return PT412 without any partial grocery/provenance/receipt rows; API returns 409, valid add and exact replay create one receipt. Hosted behavioral/native checks remain outstanding. Added to routine conflict CI (33 cases total). Exact prior `f5fd6db` has clean Sol review and its conflict CI step passed; full CI remains running.

Meal preparation conflicts: migration `20260926102144` changes twelve terminal guards in four preparation functions and the retained public routine edit primitive, preserving two handler translations. A local HTTP journey verifies seven stale create/edit paths with unchanged routines/occurrences/receipts, stale detail reads, and successful create/edit exact replay. Installed on nest-test with all twelve/two catalog counts verified. Actual hosted API create/replay, stale routine edit (409 in 0.080s), current edit/replay, partner read, outsider denial and meal removal pass; original visible meal entries are restored, fictional routine/receipt/history records remain. Harness: `/tmp/nest-hosted-preparation-check.py`. This is hosted API evidence, not native-device proof. Added to routine conflict CI (34 cases total).

Ingredient hosted follow-up: after privately refreshing expired fictional test tokens, the actual API stale ingredient review returned 409 in 0.184s with no data writes. Exact `a002a07` received clean Sol review and independently passed all 33 conflict cases; latest-commit CI/review gates remain required.

Latest full synthetic rehearsal: `/tmp/nest-preparation-conflict-rehearsal.json` passes the accumulated schema, exact financial/receipt reconciliation (seven events, eight allocations, fourteen ledger rows, one retained receipt reference), committed recovery checks and local advisors with no findings. The pg_net exclusion and simulated Auth/Storage remain; `completeRecovery`, `externalRequestsDrained` and `ownerJobsStopped` remain false. No production or device verification is implied.

Meal proposal conflicts: migration `20260926102440` changes 32 terminal guards in 18 reservation/read/edit/worker/approval helpers and adds PT412 handling to both worker validators. Five existing retryable handler translations remain. Three new real HTTP cases prove stale/expired approval, discard and edit preserve complete proposal/receipt/edit/meal snapshots; a concurrent week change during generation or replacement is durably recorded as `constraints_changed`, retains the original preview where applicable, and retries do not rerun the controlled model fixture. All 21 existing generation/approval/edit HTTP cases also pass. Installed on nest-test with 32 guards, two validator catches and five retained handlers verified. The actual hosted API rejected an unavailable proposal approval with 409 in 0.546s without writes. No live model generation or native verification is claimed. Added to routine conflict CI (37 cases total).

Item reminder conflicts: four migrations (`20260926102723`–`20260926102727`) change fifteen terminal guards across chore/meal/grocery/recurring reminder reads and Saves. Seven changed functions retain their lock handlers; the eighth recurring read is unchanged. Four new local HTTP cases prove missing/stale source and stale reminder rejection with exact settings/operation snapshot preservation, successful Save and exact replay; twelve existing client/HTTP authorization, cancellation, forgery and lost-response cases also pass. Installed only on nest-test; catalog counts match fifteen terminal guards and all eight family lock handlers. Actual hosted API requests for unavailable chore/meal/grocery/recurring targets returned 409 in 0.342/0.129/0.145/0.137 seconds. Hosted successful reminder Saves, delivery, scheduler and APNs behavior remain unverified. Added to routine conflict CI (41 cases total).
